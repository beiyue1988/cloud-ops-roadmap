# P0-04B-R2 仓库校验器异常边界独立复审报告

## 1. 独立性声明与复审边界

- **任务编号：** P0-04B-R2
- **复审日期：** 2026-07-11
- **复审角色：** 未参与 P0-04、P0-04-R1、P0-04-R2 实现、测试或 R2 执行报告编写的独立复审会话；不是项目领导者、规格制定者、修复者或最终验收者。
- **工作目录与分支：** `C:\Workspace\Projects\cloud-ops-roadmap`，`main`。
- **唯一写入：** 仅创建本报告；未修改三个工具文件、README、任何执行报告、历史审查报告、任务包、任务看板、设计、计划、PRD、AI 规则、模板、版本基线或其他文件。
- **复审目标：** 独立确认 P0-04-R2 是否把 `IO003` 捕获精确收窄到根路径规范化、检查和 Markdown 发现/排序边界，使非路径 `ValueError/OSError` 从两个公开入口原样逃逸，并确认原 40 项、33 个批准行为、M-01/M-02/S-01 和其他规则无回归。
- **建议结论：** `passed`。本报告不宣布 P0-04、P0-04-R2 或 P0-04B-R2 `accepted`，不解锁 P0-05；最终裁决权属于主会话。

## 2. 固定提交、blob、范围与无漂移

### 2.1 固定提交链

| 对象 | 完整哈希 | 父提交与范围 |
|---|---|---|
| P0-04-R1 修订 | `2efbe21f92a647beecf93dda978855614b2ce827` | R1 工具修订基线 |
| P0-04B-R1 独立复审 | `42e4f510c8fbfa6fcc5e4e013cc79a09c097168d` | 发现 M-R1-01 的历史复审提交 |
| P0-04-R2 修订 | `5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c` | 父提交 `32fdbebb376cfe7ffbcde99629e0cccfd2bdf36d`；精确包含实现与测试两个文件 |
| P0-04-R2 执行报告 | `44b7fed394f5054ff0b4f76d8c9ef2eaf0b1f2d7` | 父提交为 R2 修订；精确包含 `项目管理/执行报告/P0-04-R2-执行报告.md` |

### 2.2 固定工具 blob

| 文件 | 当前 blob | 结论 |
|---|---|---|
| `脚本资源/项目校验/validate_repository.py` | `74b6c75896117f5680195a72dd010767d345652b` | 与固定值一致 |
| `脚本资源/项目校验/tests/test_validate_repository.py` | `1f429b89d041a2788130f2289f55fb3c0dfe51a5` | 与固定值一致 |
| `脚本资源/项目校验/README.md` | `7476e64b6fd4f5ba3bf0a009e3d267c4c0ad87de` | 与固定值一致，R2 未修改 README |

### 2.3 启动门禁与 Git 事实

| 检查 | 新鲜证据 | 结论 |
|---|---|---|
| 工作区、分支、远程、Python | 启动时 `## main` 且无工作区条目；远程为 `https://github.com/beiyue1988/cloud-ops-roadmap.git`；Python 3.11.15 | 通过 |
| 看板状态 | P0-04 `rework`、P0-04-R1 `rework`、P0-04B-R1 `accepted`、P0-04-R2 `submitted`、P0-04B-R2 `ready` | 通过 |
| R2 父链 | 修订父提交与报告父提交均符合任务包固定值 | 通过 |
| 提交范围 | 修订提交为 2 个工具文件；报告提交为 1 份 R2 报告 | 通过 |
| blob 与无漂移 | 三个 blob 与固定值一致；实现与测试相对 R2 修订提交差异退出 0 | 通过 |
| 空白检查 | 两个 R2 提交 `git show --check`、当前 `git diff --check` 均退出 0 | 通过 |
| 历史快照 | R1 执行报告与 P0-04B-R1 报告相对各自固定提交无漂移 | 通过 |

Git 持续提示无法读取用户级 `C:\Users\beiyue\.config\git\ignore`；显式状态、提交集合、blob、漂移和空白检查均正常，该环境警告不改变固定对象判断。

## 3. 自动复验汇总

任务包规定的核心自动检查 **10/10 通过**。

| # | 检查 | 退出码或结果 | 关键证据 |
|---:|---|---|---|
| 1 | 启动状态、提交、父链、范围、blob 与无漂移 | 0 | 固定对象全部符合 |
| 2 | 两个 R2 提交 `git show --check` 与当前空白检查 | 0 | 空白错误 0 |
| 3 | 当前完整 `unittest` | 0 | `Ran 44 tests`，`OK`，44/44 |
| 4 | AST 比较父提交与当前测试，并核对批准映射 | 0 | 父提交 40、当前 44、删除 0、新增 4；33/33 |
| 5 | 唯一系统临时 `PYTHONPYCACHEPREFIX` 的 `py_compile` | 0 | 两文件编译成功；临时缓存已清理；仓库缓存 0 |
| 6 | 当前仓库 CLI 与连续双运行 | 0 | 写报告前摘要为 Markdown 116、知识章节 0、本地链接 335、错误 0；两次退出码 0/0且合并输出逐字符一致 |
| 7 | AST 公开函数、导入与异常语法 | 0 | 公开函数 7/7；第三方依赖 0；bare `except` 0；`except Exception` 0 |
| 8 | R2 父提交旧实现加当前测试重建红灯 | 预期退出 1 | `Ran 44 tests`；仅四个新增非路径异常测试失败；errors 0 |
| 9 | 异常正反探针与原规则回归 | 0 | 路径 `IO003` 4/4；非路径异常逃逸 6/6；基础 IO 2/2；M-01 10/10；其他回归 7/7 |
| 10 | README、R2 报告与历史纠正一致性 | 0 | README 固定 blob；R2 报告字段 10/10；R1 历史文件无漂移 |

PowerShell 捕获原生 Python 合并输出时中文显示乱码；同一捕获链路的两个退出码均为 0且字符串逐字符相同，单次直接 CLI 中文摘要正常。该现象属于控制台编码显示，不影响确定性判断。

## 4. 正确红灯重建与完整绿灯

### 4.1 R2 父提交红灯

在系统临时目录构造完整的 `脚本资源/项目校验/tests/` 层级，放入 R2 父提交 `32fdbebb376cfe7ffbcde99629e0cccfd2bdf36d` 的旧实现与当前测试，再运行同一 `unittest discover`：

- **退出码：** 1，符合预期非 0；
- **总数：** `Ran 44 tests`；
- **摘要：** `FAILED (failures=4)`，errors 0；
- **失败方法：**
  1. `test_validate_repository_does_not_swallow_non_path_value_error`；
  2. `test_main_does_not_swallow_non_path_value_error`；
  3. `test_validate_repository_does_not_swallow_non_path_os_error`；
  4. `test_main_does_not_swallow_non_path_os_error`。
- **有效性：** 原 40 项全部通过；失败精确证明旧实现会吞掉非路径 `ValueError/OSError`。
- **清理：** 临时绝对路径先确认位于系统临时根，再删除；最终 `removed=True`，仓库未产生临时文件。

### 4.2 当前完整绿灯

- **当前测试：** 44/44，退出 0；
- **原测试保留：** 40/40，删除 0；
- **新增测试：** 4；
- **批准行为映射：** 33/33，缺失 0；
- **编译：** 实现与测试 `py_compile` 退出 0，临时缓存清理完成，仓库缓存 0；
- **真实仓库：** CLI 退出 0；
- **确定性：** 双运行退出码 0/0，合并输出逐字符一致；
- **接口与依赖：** 七个公开函数 7/7，第三方依赖 0。

## 5. 33 个批准行为映射

| # | 批准行为 | 当前测试方法 |
|---:|---|---|
| 1 | 合法 Front Matter | `test_extract_valid_front_matter` |
| 2 | 知识正文缺 Front Matter | `test_front_matter_required_for_knowledge_article` |
| 3 | 未闭合分隔符 | `test_front_matter_requires_closing_delimiter` |
| 4 | 缺少键 | `test_front_matter_reports_missing_key` |
| 5 | 重复键 | `test_front_matter_reports_duplicate_key` |
| 6 | 空值与空列表 | `test_front_matter_rejects_empty_values` |
| 7 | 非法 `type` | `test_front_matter_rejects_invalid_type` |
| 8 | 非法 `status` | `test_front_matter_rejects_invalid_status` |
| 9 | 非法日期 | `test_front_matter_rejects_invalid_date` |
| 10 | 列表与 `versions` | `test_front_matter_accepts_lists_and_versions_map` |
| 11 | 超出受控 YAML 子集 | `test_front_matter_rejects_unsupported_yaml` |
| 12 | 重复 `chapter` 两端报错 | `test_duplicate_chapter_reports_both_files` |
| 13 | 文件与图片链接 | `test_valid_relative_links_and_images` |
| 14 | 外链、邮件、纯锚点忽略 | `test_external_mail_and_anchor_are_ignored` |
| 15 | 文件加锚点只检查文件 | `test_file_anchor_checks_file_only` |
| 16 | 失效链接 | `test_broken_relative_link` |
| 17 | 越出根目录 | `test_link_cannot_escape_root` |
| 18 | 绝对路径与盘符 | `test_absolute_and_drive_links_are_rejected` |
| 19 | 大小写不一致 | `test_case_mismatch_is_detected` |
| 20 | 围栏内链接忽略 | `test_link_in_fence_is_ignored` |
| 21 | 引用式与尖括号目标 | `test_reference_and_angle_bracket_links` |
| 22 | 正式内容占位符 | `test_formal_content_placeholder_is_rejected` |
| 23 | 围栏内占位符忽略 | `test_placeholder_in_fence_is_ignored` |
| 24 | 排除目录 | `test_placeholder_excluded_directories` |
| 25 | 模板标记残留 | `test_template_marker_in_formal_content_is_rejected` |
| 26 | 虚构 Secret 检出且脱敏 | `test_synthetic_secret_is_rejected_and_redacted` |
| 27 | 中文路径与 UTF-8 | `test_chinese_paths_and_utf8` |
| 28 | 非法 UTF-8 | `test_invalid_utf8_reports_io_error` |
| 29 | 稳定数字排序 | `test_errors_are_sorted_deterministically` |
| 30 | CLI 成功 | `test_main_returns_zero_on_success` |
| 31 | CLI 失败 | `test_main_returns_one_on_failure` |
| 32 | 根目录不存在且无 traceback | `test_missing_root_returns_one_without_traceback` |
| 33 | 真实仓库通过 | `test_current_repository_passes` |

## 6. 异常边界独立探针

独立探针使用 `python -B -`、`tempfile` 与 `unittest.mock`，只观察公开返回值、异常传播和渲染结果，不调用被测私有函数生成期望。运行时唯一标记和虚构敏感文本均分段构造，未在仓库或报告中保存原值。

### 6.1 批准路径异常正例

| 场景 | 公开观察 | 结论 |
|---|---|---|
| `validate_repository(Path(chr(0)))` | 返回唯一 `.:0: IO003`，固定消息不回显非法根路径 | 通过 |
| `main([chr(0)])` | 返回 1；stdout 为空；stderr 含 `IO003`；无 traceback且不回显根路径 | 通过 |
| mock `Path.resolve` 抛 `OSError` 后调用 `validate_repository()` | 返回唯一脱敏 `IO003`，异常原文未出现 | 通过 |
| 同一根 `OSError` 通过 `main()` | 返回 1；stdout 为空；stderr 含脱敏 `IO003`；无 traceback | 通过 |

批准路径场景 **4/4** 通过，脱敏 **4/4**。缺失根目录独立返回 `IO001`；非法 UTF-8 Markdown 独立返回 `IO002`，基础 IO **2/2**。

### 6.2 非路径异常负例

在合法临时仓库的 `学习路线/入口.md` 处理过程中，对公开 `validate_placeholders` 注入三种异常，并分别从 `validate_repository()` 与 `main()` 观察：

| 注入异常 | `validate_repository()` | `main()` | stdout/stderr |
|---|---|---|---|
| `ValueError` | 原类型、原唯一标记逃逸 | 原类型、原唯一标记逃逸 | 空，无伪造 `IO003` |
| `OSError` | 原类型、原唯一标记逃逸 | 原类型、原唯一标记逃逸 | 空，无伪造 `IO003` |
| `RuntimeError` | 原类型、原唯一标记逃逸 | 原类型、原唯一标记逃逸 | 空，无伪造 `IO003` |

负向场景 **6/6** 通过；异常类型与唯一标记 **6/6** 保持，伪造 `IO003` 为 0。

### 6.3 探针自身异常与根因区分

首次组合探针在动态加载模块时失败，原始错误为 `dataclass` 无法从 `sys.modules[cls.__module__]` 取得模块命名空间。失败发生在调用任何被测行为之前。按 `systematic-debugging` 对照当前测试加载器后确认根因为审查探针遗漏 `sys.modules[spec.name] = module`，属于探针加载器缺陷，不是交付缺陷。只修正该加载步骤后从头复跑，全部上述断言通过；未修改被审工具。

## 7. AST 与人工异常边界

### 7.1 静态证据

- 捕获 `(OSError, ValueError)` 的聚合根发现 `try` 只有一处，位于实现第 604 行起；其调用集合只涉及 `resolve`、`exists`、`is_dir`、`rglob`、`is_file`、`relative_to`、`sorted`、`as_posix` 及诊断构造。
- 该 `try` 不包含 `_aggregate_repository_checked`、任何 `validate_*`、`_front_matter_diagnostics` 或内容聚合调用。
- `_aggregate_repository_checked(root, markdown_files)` 在捕获块之后调用；内容读取、链接、敏感信息、占位符、Front Matter、重复章节和最终诊断聚合不在根发现捕获范围内。
- `main()` 中 `try` 数量为 0，不重复捕获 `_aggregate_repository()` 或内容聚合异常。
- bare `except` 0；`except Exception` 0；第三方依赖 0。

### 7.2 四个新增测试的独立性

四个新增测试都在合法临时仓库中对 `validate_placeholders` 注入异常，并通过公开 `validate_repository()` 或 `main()` 使用 `assertRaisesRegex` 观察异常类型和唯一标记；不调用私有聚合辅助函数，也不把 `IO003` 返回视为成功。

结论：路径发现和内容聚合已真正分离；捕获位置而非异常字符串或异常子类包装决定转换边界，符合 R2 精确修复合同。

## 8. 原缺陷与其他规则回归

| 回归项 | 独立结果 | 结论 |
|---|---|---|
| M-01 非法 quoted scalar | 未闭合双/单引号、首尾错配、闭合后尾随、列表项、`versions` 映射共 6/6 报真实行号 `FM008` | 通过 |
| M-01 合法受控标量 | plain、成对单引号、成对双引号、合法列表/映射 4/4 无诊断 | 通过 |
| M-02 批准根路径异常 | 两个公开入口的 `ValueError/OSError` 共 4/4 转脱敏 `IO003` | 通过 |
| S-01 数字排序 | 同一文件第 2 行先于第 10 行；两次结果一致 | 通过 |
| 路径安全 | 越界 `LINK002`、大小写不一致 `LINK004` 各自区分 | 通过 |
| 围栏 | 围栏内链接与占位符均不误报 | 通过 |
| 中文路径 | 中文文件与相对链接临时仓库返回空错误列表 | 通过 |
| Secret 脱敏 | 运行时虚构值被检测，诊断不回显原值 | 通过 |
| 确定性 | 临时错误列表与真实仓库双运行均一致 | 通过 |

上述额外回归断言合计 **7/7**；完整 44 项测试同时覆盖重复章节、失效链接、引用式链接、占位符适用范围、非法 UTF-8、CLI 成败和真实仓库等批准行为。

## 9. README、R2 报告与历史纠正一致性

- README blob 精确为 `7476e64b6fd4f5ba3bf0a009e3d267c4c0ad87de`，R2 未修改 README。
- README 将 `IO003` 语义限定为根路径规范化、目录发现或相关遍历的预期路径/文件系统错误，与当前捕获位置和正反探针一致。
- README 继续准确说明 `IO001/IO002/IO003`、固定脱敏消息、无 traceback、受控 YAML/Markdown 子集、路径/围栏/大小写/Secret 边界与明确限制，不夸大为完整解析器或安全审计。
- R2 报告中的四个固定哈希、44/44、原 40/40、33/33、四项红灯、路径正例、非路径负例、README blob、提交范围、历史纠正、风险和远程操作与 Git/运行事实一致。
- R2 报告准确纠正 R1 报告中“只按异常类型即可保证不吞编程错误”的过宽结论，改为以捕获位置限定根路径阶段；R1 执行报告和 P0-04B-R1 复审报告均保持历史快照不变。
- R2 报告记录的 Markdown 115 与本次写报告前的 116 是后续治理 Markdown 增加导致的统计时点差异；工具实现和测试无漂移，不构成报告失真。

## 10. 十二项人工审查

| # | 审查项 | 结论 | 直接证据与判断 |
|---:|---|---|---|
| 1 | 路径发现与内容聚合是否真正分离 | 通过 | `_aggregate_repository()` 先在受控 `try` 内生成文件列表，再在块外调用 `_aggregate_repository_checked()` |
| 2 | 捕获范围是否只覆盖批准的根路径操作 | 通过 | AST 只见根规范化、状态检查、发现、相对化与排序调用，无内容规则调用 |
| 3 | `validate_repository()` 是否保留路径 `IO003` 且暴露非路径异常 | 通过 | 路径正例 2/2；非路径三类型 3/3 原样逃逸 |
| 4 | `main()` 是否保留路径 `IO003` 且不重复吞聚合异常 | 通过 | 路径正例 2/2；非路径三类型 3/3；`main()` 无 `try` |
| 5 | `IO001/IO002/IO003` 边界是否真实 | 通过 | 缺失根、非法 UTF-8、非法根/根 `OSError` 分别命中对应编号 |
| 6 | 四个新测试是否观察公开接口且不依赖私有函数 | 通过 | 公开入口、公开规则函数注入和 `assertRaisesRegex` 直接观察 |
| 7 | 旧实现加当前测试是否产生精确红灯 | 通过 | 44 项中仅新增四项失败，原 40 项通过 |
| 8 | 原 40 项、33 个行为及 M-01/M-02/S-01 是否无回归 | 通过 | 44/44、父 40零删除、33/33及独立回归全部通过 |
| 9 | README 是否保持不变且与实现一致 | 通过 | 固定 blob 与当前异常位置、输出和限制一致 |
| 10 | R2 报告是否准确纠正 R1 且保持历史不可变 | 通过 | 报告字段与 Git 事实一致；两份历史文件无漂移 |
| 11 | 两个 R2 提交是否满足范围、顺序和不可变性 | 通过 | 父链正确，2/1 文件范围，`git show --check` 通过，工具无漂移 |
| 12 | 是否存在新安全风险、敏感信息、范围漂移或未决问题 | 通过 | 未发现真实凭据、敏感值回显、范围漂移或实现未决问题；临时数据均已清理 |

人工审查汇总：**通过 12、需说明 0、不通过 0，共 12 项。**

## 11. 问题分级、风险、未决问题与建议结论

### 11.1 阻塞（0）

无。固定对象明确、工具无漂移、可在只读边界独立复验，未发现敏感信息或临时数据残留。

### 11.2 必须修改（0）

无。非路径 `ValueError/OSError` 未被吞掉，批准路径异常、捕获位置和原行为均未发现回归。

### 11.3 建议修改（0）

无。本次未发现不阻塞异常合同但需要记录的维护性或诊断体验问题。

### 11.4 通过项（12）

与第 10 节十二项人工审查一一对应，均有 Git、AST、完整测试、临时红灯或公开接口探针支持。

问题数量：**阻塞 0、必须修改 0、建议修改 0、通过项 12。**

- **风险：** 本工具有意只把批准根路径阶段的 `ValueError/OSError` 转成 `IO003`；内容层同类型异常会暴露给调用方。这是发现程序缺陷所需的保守边界，不是通用异常恢复机制。除此之外，本次证据范围内未发现新增风险。
- **未决问题：** P0-04B-R2 范围内无技术未决问题；仍需主会话独立裁决最终验收和后续状态，不由本复审更新任务看板或解锁 P0-05。
- **建议结论：** `passed`。依据：无阻塞、无必须修改、无建议修改；自动复验、十二项人工审查和异常正反边界均通过。

## 12. 远程操作与责任边界

- 未执行 `push`；
- 未创建 Pull Request；
- 未发布网站或制品；
- 未修改 GitHub 仓库设置；
- 未执行 P0-05、P0-06 或其他任务；
- 未创建分支或 Git worktree；
- 未修改、重建、amend、rebase 或 reset 任何被审提交；
- 本报告只建议 `passed`，未宣布任何相关任务 `accepted`，最终验收权属于主会话。
