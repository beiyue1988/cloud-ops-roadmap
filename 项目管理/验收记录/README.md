# 验收记录

本目录保存主会话验收和独立审查结论。任务验收记录应包含任务编号；阶段审查使用 `Phase-<编号>-独立审查报告.md` 等可追溯名称。

## 允许编辑者

主会话负责最终验收记录；独立审查会话只有在任务包明确授权时才可创建指定审查报告。执行会话不得为自己的交付物写最终验收结论。

## 必需字段

每份记录必须包含审查对象、范围、提交哈希、逐项证据、运行命令与退出码、发现、风险、结论、复验条件和日期。

## 状态责任

独立审查会话只能给出建议结论；`accepted`、`rework`、`blocked` 等任务状态最终由主会话写入任务看板。
