# P0-04B 仓库校验工具独立审查报告

## 1. 独立性声明、范围与固定对象

- **任务编号：** P0-04B
- **审查日期：** 2026-07-11
- **审查角色：** 未参与 P0-04 校验器实现、测试、README 或执行报告创建的独立审查会话；不是项目领导者、缺陷修复者或最终验收者。
- **工作目录与分支：** `C:\Workspace\Projects\cloud-ops-roadmap`，`main`。
- **实现提交：** `08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3`
- **报告提交：** `d2952cf3ea5216169deaeaa9e320cf3f6d409363`
- **审查边界：** 只审查上述固定提交、当前无漂移的三个工具文件及其批准规格；未修改校验器、测试、README、P0-04 执行报告、任务包、任务看板、设计或计划。

## 2. 启动门禁与固定提交

| 检查 | 实际证据 | 退出码 | 结论 |
|---|---|---:|---|
| `git status --short --branch` | `## main`，无工作区条目 | 0 | 通过 |
| `git branch --show-current` | `main` | 0 | 通过 |
| `git remote get-url origin` | `https://github.com/beiyue1988/cloud-ops-roadmap.git` | 0 | 通过 |
| `python --version` | `Python 3.11.15` | 0 | 通过 |
| 看板状态 | P0-04 为 `submitted`，P0-04B 为 `ready` | 0 | 通过 |
| 实现提交 | 完整哈希可达；父提交 `eeb309e5c0043b8bf8935e3505d7d0af64b66a88`；主题 `test: add repository content validator` | 0 | 通过 |
| 报告提交 | 完整哈希可达；父提交精确为实现提交；主题 `docs: add P0-04 execution report` | 0 | 通过 |
| 提交文件集合 | 实现提交 3 个工具文件；报告提交 1 份执行报告 | 0 | 通过 |
| 工具漂移 | `git diff --exit-code <实现提交> HEAD -- '脚本资源/项目校验'` 无输出 | 0 | 通过 |

Git 命令持续出现无法读取用户级 `C:\Users\beiyue\.config\git\ignore` 的警告，但显式提交集合、漂移、状态和空白检查均正常；该环境警告不改变固定对象。

## 3. 自动复验

自动复验按任务包七类机械证据计数，结果为 **7/7 通过**。

| # | 命令或检查 | 退出码 | 关键结果 |
|---:|---|---:|---|
| 1 | 启动状态、提交、文件集合与漂移检查 | 0 | 分支、远程、Python、状态、父提交、3/1 文件集合与无漂移全部符合 |
| 2 | `python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v` | 0 | `Ran 34 tests`，`OK`，34/34 通过 |
| 3 | 临时 `PYTHONPYCACHEPREFIX` 的 `python -m py_compile` | 0 | 实现与测试编译成功；临时缓存已安全删除；仓库 `__pycache__` 为 0 |
| 4 | `python -B '脚本资源/项目校验/validate_repository.py' .` 与连续双运行 | 0 | 写报告前单次/两次退出码 0/0/0；两次合并输出逐字符相同；写报告前摘要为 Markdown 108、知识章节 0、本地链接 322、错误 0；报告写入后的提交前新鲜复验为 Markdown 109、知识章节 0、本地链接 322、错误 0 |
| 5 | AST 公开函数、测试方法和两文件导入统计 | 0 | 公开函数 7/7；测试方法 34；第三方依赖 0 |
| 6 | 从 P0-04 任务包提取规定测试名并与实际测试、执行报告比较 | 0 | 实际覆盖 33/33、报告映射 33/33、缺失 0 |
| 7 | `git show --check`、`git diff --check`、提交集合、漂移、缓存和状态检查 | 0 | 两个固定提交检查通过；缓存 0；写报告前状态条目 0 |

PowerShell 捕获原生进程合并输出时中文显示发生乱码，但比较使用同一捕获字符串，两个退出码均为 0 且逐字符比较为 `True`；单次直接 CLI 中文摘要正常。

## 4. 测试方法与 33 个行为追溯

- **实际测试方法：** 34。
- **现有测试运行：** 34/34 通过。
- **规定行为：** 33/33 在实际测试中存在，33/33 在 P0-04 执行报告映射中存在，缺失 0。
- **额外测试：** `test_link_in_inline_code_is_ignored`。

| # | 规定行为 | 实际测试方法 | 追溯 |
|---:|---|---|---|
| 1 | 合法 Front Matter | `test_extract_valid_front_matter` | 存在且通过 |
| 2 | 知识正文缺 Front Matter | `test_front_matter_required_for_knowledge_article` | 存在且通过 |
| 3 | 未闭合分隔符 | `test_front_matter_requires_closing_delimiter` | 存在且通过 |
| 4 | 缺少键 | `test_front_matter_reports_missing_key` | 存在且通过 |
| 5 | 重复键 | `test_front_matter_reports_duplicate_key` | 存在且通过 |
| 6 | 空值与空列表 | `test_front_matter_rejects_empty_values` | 存在且通过 |
| 7 | 非法 `type` | `test_front_matter_rejects_invalid_type` | 存在且通过 |
| 8 | 非法 `status` | `test_front_matter_rejects_invalid_status` | 存在且通过 |
| 9 | 非法日期 | `test_front_matter_rejects_invalid_date` | 存在且通过 |
| 10 | 列表与 `versions` | `test_front_matter_accepts_lists_and_versions_map` | 存在且通过 |
| 11 | 超出 YAML 子集 | `test_front_matter_rejects_unsupported_yaml` | 存在且通过；但仅覆盖块标量，未覆盖畸形引号 |
| 12 | 重复 `chapter` 两端报错 | `test_duplicate_chapter_reports_both_files` | 存在且通过 |
| 13 | 文件和图片链接 | `test_valid_relative_links_and_images` | 存在且通过 |
| 14 | 外链、邮件和纯锚点忽略 | `test_external_mail_and_anchor_are_ignored` | 存在且通过 |
| 15 | 文件加锚点只检查文件 | `test_file_anchor_checks_file_only` | 存在且通过 |
| 16 | 失效链接 | `test_broken_relative_link` | 存在且通过 |
| 17 | 越出根目录 | `test_link_cannot_escape_root` | 存在且通过 |
| 18 | 绝对路径和盘符 | `test_absolute_and_drive_links_are_rejected` | 存在且通过 |
| 19 | 大小写不一致 | `test_case_mismatch_is_detected` | 存在且通过 |
| 20 | 围栏内链接忽略 | `test_link_in_fence_is_ignored` | 存在且通过 |
| 21 | 引用式与尖括号目标 | `test_reference_and_angle_bracket_links` | 存在且通过 |
| 22 | 正式正文占位符 | `test_formal_content_placeholder_is_rejected` | 存在且通过 |
| 23 | 围栏内占位符忽略 | `test_placeholder_in_fence_is_ignored` | 存在且通过 |
| 24 | 排除目录 | `test_placeholder_excluded_directories` | 存在且通过 |
| 25 | 模板标记残留正式内容 | `test_template_marker_in_formal_content_is_rejected` | 存在且通过 |
| 26 | 虚构 Secret 与脱敏 | `test_synthetic_secret_is_rejected_and_redacted` | 存在且通过 |
| 27 | 中文目录与 UTF-8 | `test_chinese_paths_and_utf8` | 存在且通过 |
| 28 | 非法 UTF-8 | `test_invalid_utf8_reports_io_error` | 存在且通过 |
| 29 | 稳定数字排序 | `test_errors_are_sorted_deterministically` | 存在且通过；断言部分依赖实现私有排序函数 |
| 30 | CLI 成功 | `test_main_returns_zero_on_success` | 存在且通过 |
| 31 | CLI 失败 | `test_main_returns_one_on_failure` | 存在且通过 |
| 32 | 根目录不存在且无 traceback | `test_missing_root_returns_one_without_traceback` | 存在且通过；未覆盖根路径处理自身抛出的异常 |
| 33 | 真实仓库通过 | `test_current_repository_passes` | 存在且通过 |

“测试名存在并通过”证明规定映射没有缺失，但不能覆盖批准设计的所有假阴性和异常边界；第 6、7 节记录了独立发现。

## 5. 六组独立临时场景

所有临时仓库均由不落盘 Python 进程在系统临时目录创建并自动清理，未写入项目仓库。虚构敏感值只在运行时分段拼接；命令输出和本报告均未保存原值。

| # | 独立场景 | 预期 | 实际证据 | 结果 |
|---:|---|---|---|---|
| 1 | 完整合法知识章节 | 无错误 | 错误 0 | 通过 |
| 2 | 同库重复 `chapter`、失效链接、正文占位符 | 两端 `CH001`、`LINK001`、对应 `PH` | `CH001=2`、`LINK001=1`、`PH001=1` | 通过 |
| 3 | 越界链接与大小写不一致 | `LINK002`、`LINK004` | 两类各 1 | 通过 |
| 4 | 围栏内链接/占位符忽略，围栏外运行时虚构 Secret 检出且脱敏 | 无 `LINK`/`PH`；有 `SEC`；不回显 | `SEC=2`、`LINK=0`、`PH=0`、脱敏为真 | 通过 |
| 5 | 非法 UTF-8 且同库仍有另一条正文诊断 | `IO002`，继续检查，结果稳定 | `IO002=1`、继续得到 `PH001=1`、两次结果一致 | 通过 |
| 6 | 同一失败仓库连续运行两次 | 退出码与输出相同；数字行号排序；敏感值不回显 | 退出码 1/1、输出逐字符相同；行号序列 `[2, 6, 6, 10]`；脱敏为真 | 通过 |

**独立场景：6/6 通过。**

## 6. 十二项人工审查

| # | 审查项 | 结论 | 直接证据与判断 |
|---:|---|---|---|
| 1 | 规则适用目录 | 通过 | 聚合层对全部 Markdown 运行链接和敏感信息；占位符只按九个正式目录第一段；Front Matter 只对 `知识库/` 下非 README；仅排除 `.git`。独立场景验证目录边界。 |
| 2 | Front Matter 受控子集与假阴性/假阳性 | 不通过 | `_strip_scalar` 把只有成对首尾引号的文本去引号，其余一律当普通标量；将合法章节的标题改为未闭合双引号后得到诊断 0、`FM008=0`。畸形 quoted scalar 被错误接受，违反只接受批准子集并拒绝未知结构的要求。 |
| 3 | 路径逃逸 | 通过 | 先对解析候选执行 `resolve(strict=False)`，再比较根目录/父路径；独立场景得到 `LINK002`。绝对路径和盘符另由 `LINK003` 区分。 |
| 4 | Windows 大小写 | 通过 | `_case_status` 每段先精确匹配，再用 `casefold` 识别仅大小写不同的实际项；正确路径无诊断，错误大小写得到 `LINK004`。 |
| 5 | 围栏与行号 | 通过 | 围栏屏蔽保留换行；反引号/波浪线围栏均被测试；独立场景中的围栏示例未误报，围栏外诊断行号正确。 |
| 6 | 敏感信息脱敏 | 通过 | `SEC001`–`SEC003` 使用固定消息，不拼接匹配值；现有测试和两个独立场景均做原值否定断言并通过。 |
| 7 | I/O 和无 traceback | 不通过 | 缺失根目录和非法 UTF-8 能稳定返回 `IO001/IO002`；但 `main()` 外层没有异常边界。向公开 `main(argv)` 传入使根路径处理抛异常的非法路径时，返回码为空并直接抛 `ValueError`，未转换为 `IO` 诊断。默认脚本入口在可触发的同类路径异常下会打印 traceback。 |
| 8 | 数字排序 | 通过 | `_error_sort_key` 把行号转为 `int`；独立场景在同一文件得到 `[2, 6, 6, 10]`，两次顺序一致。 |
| 9 | README 真实性 | 不通过 | 环境、范围、规则和限制总体与实现一致；但 README 宣称根目录或读取失败会转换为 `IO` 且不打印 traceback，未说明根路径解析/检查自身的异常缺口，与第 7 项证据不一致。 |
| 10 | 测试是否自证或弱断言 | 需说明 | 33 个规定方法均存在且多数断言具体规则。排序测试使用 `sorted(first, key=validator._error_sort_key)`，部分以被测实现自己的私有函数证明排序，并且直接断言没有在同一文件比较第 2/10 行；独立场景补证了实际行为，但现有回归测试仍偏弱。Front Matter 与异常根路径也存在遗漏。 |
| 11 | 标准库与全局状态 | 通过 | AST 显示实现和测试第三方依赖 0；实现公开函数 7/7；无用于跨运行累积诊断的全局可变状态，双运行一致。 |
| 12 | 执行报告和 Git 一致性 | 需说明 | 固定哈希、父提交、3/1 文件集合、34 个测试、33/33 映射、公开函数和依赖数字一致。报告把“读取失败不会产生 traceback”列为 10/10 通过，但证据只覆盖 UTF-8/常规 CLI 失败，没有覆盖根路径处理异常，因此该人工结论范围过宽。报告的 Markdown 107 与本次写报告前 108 属于后续治理文件增加造成的正常时点差异。 |

人工审查汇总：**通过 7，需说明 2，不通过 3，共 12 项。**

## 7. 问题分级

### 7.1 阻塞（0）

无。固定对象清楚、无工具漂移、可独立复验，未发现路径逃逸或敏感值回显。

### 7.2 必须修改（2）

1. **M-01：受控 Front Matter 接受畸形引号标量。**
   - 证据：将合法章节 `title` 改为未闭合双引号后，`validate_front_matter()` 返回 0 条诊断，`FM008=0`。
   - 影响：机械门禁会把超出批准子集、通用 YAML 也无法正常解析的 Front Matter 判为有效，形成结构假阴性；现有 `test_front_matter_rejects_unsupported_yaml` 只覆盖块标量，不能防止该回归。
   - 建议：返工任务应明确 quoted scalar 的配对与非法尾随语法，并增加不依赖实现内部解析逻辑的负面测试；本审查未修改实现。

2. **M-02：CLI 缺少覆盖根路径处理异常的稳定 `IO` 边界，README 与执行报告过度声明。**
   - 证据：公开 `main(argv)` 的非法根路径探针得到 `code=None`、直接抛出 `ValueError`，未生成 `IO` 诊断。`_aggregate_repository()` 在 `root.resolve(strict=False)` 等根路径处理外围没有异常转换。
   - 影响：一旦根路径解析或目录发现触发未捕获的 `ValueError/OSError`，脚本入口会违背“任何异常转换为稳定错误、默认无 traceback”的批准设计；README 和 P0-04 执行报告相应结论不完整。
   - 建议：返工任务应定义允许捕获的文件系统异常边界、稳定 `IO` 编号和 CLI 回归测试，并同步收紧 README/报告事实；本审查未修改任何被审查文件。

### 7.3 建议修改（1）

1. **S-01：强化数字排序测试的独立观察断言。**
   - 现有测试部分调用实现自身 `_error_sort_key` 证明排序，且直接断言只跨文件验证 `A.md:10` 排在 `B.md:3` 前，不能单独证明同一路径第 2 行在第 10 行前。
   - 独立场景已经证明当前实现行为正确；建议返工时将同文件第 2/10 行的固定期望直接写入测试，避免自证。

### 7.4 通过项（9）

1. 固定提交、父提交、3/1 文件范围及工具无漂移；
2. 34/34 现有测试通过，33/33 规定测试名与报告映射完整；
3. 两文件编译通过，临时缓存清理，仓库缓存为 0；
4. 公开函数 7/7、第三方依赖 0、无跨运行全局污染；
5. 当前仓库 CLI 通过且双运行输出与退出码一致；
6. 链接越界、绝对路径、缺失和 Windows 大小写区分符合设计；
7. 围栏屏蔽、中文路径、UTF-8 诊断和数字行号实际行为稳定；
8. 正式目录占位符范围与排除目录符合设计；
9. 运行时虚构敏感值被检出且所有诊断均未回显原值。

问题数量：**阻塞 0、必须修改 2、建议修改 1、通过项 9。**

## 8. 执行报告一致性

- **一致：** 两个固定哈希与父子链；实现/报告提交文件集合；34 个测试；33/33 行为映射；实现公开函数 7/7；第三方依赖 0；编译、真实仓库、双运行、缓存和远程操作事实。
- **需收紧：** P0-04 执行报告的“读取失败不会产生 traceback”和十项人工检查 10/10 通过只由非法 UTF-8、缺失根目录及常规失败 CLI 支撑，不能覆盖根路径解析/检查自身抛出的异常；独立探针证明存在未捕获边界。
- **时点差异：** 执行报告记录 Markdown 107，本次独立审查写报告前记录 108；工具文件无漂移，差异来自后续治理 Markdown，非报告造假或实现变化。

## 9. 风险、未决问题与建议结论

- **风险：** 畸形 Front Matter 可绕过结构门禁；少数根路径/目录处理异常可能产生未捕获异常和 traceback；排序回归测试存在自证成分。链接路径逃逸、敏感值回显和确定性在本次证据范围内未发现风险。
- **未决问题：** 主会话需要裁决并签发返工范围：至少覆盖 quoted scalar 语法边界、顶层 I/O 异常转换、相应 README/执行报告真实性和回归测试；本审查不授权或实施修复。
- **建议结论：** `changes-required`。依据：存在 2 项必须修改；无权宣布 P0-04 或 P0-04B `accepted`。
- **最终验收：** 最终验收权属于主会话，本报告只提供独立审查证据和建议结论。

## 10. 远程操作确认

- 未执行 `push`；
- 未创建 Pull Request；
- 未发布网站或制品；
- 未修改 GitHub 仓库设置；
- 未执行 P0-05、P0-06 或其他任务。
