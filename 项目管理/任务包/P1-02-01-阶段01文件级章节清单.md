# P1-02-01 阶段 01 文件级章节清单任务包

## 新会话启动指令

```text
请执行 cloud-ops-roadmap 的 P1-02-01“阶段 01 文件级章节清单”任务。

你是新的独立大纲执行会话，不是项目领导者、规格制定者、审查者、正式正文作者或最终验收者。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap，分支固定为 main。只执行本任务，不得委派、扩大范围或与冲突写任务并行。

写前完整阅读：AGENTS.md、CODEX-PRD.md、本任务包、Phase 1 导航与完整大纲的设计说明和实施计划、AI-CODEX-RULES.md、软件版本基线、来源优先级、校验器 README、知识库 README、阶段 00 README、P1-02-00 执行报告和阶段 01 README。

唯一目标：把阶段 01 README 扩充为连续、可学习、可机械校验的文件级章节清单，使零基础学习者能够读懂服务器的 CPU、内存、存储、RAID、互连、固件、机架和 GPU 服务器基础规格。

唯一写入白名单：
- C:\Workspace\Projects\cloud-ops-roadmap\知识库\01-计算机与服务器基础\README.md；
- C:\Workspace\Projects\cloud-ops-roadmap\项目管理\执行报告\P1-02-01-执行报告.md。

禁止创建清单所列章节或任何正式正文、实验、项目实现、故障、面试、命令速查、脚本、配置或图表；禁止修改阶段 00、其他阶段、路线、索引、校验器、任务包、看板、PRD、计划、ADR、版本基线或项目记忆；GPU 只做版本中立概览，不得选择实例、GPU 型号、显存、地域、价格、模型或软件组合；不得自动调用 superpowers-zh；不得进行任何远程写。

启动门禁：main、origin 精确、工作区干净；P1-02-00 产品 b9e8eb8610f46f4423bf9f959fd75760095eed48 和报告 5d76e82832516a697909c4a6c6b0ef578c6febb9 在当前历史；看板 P1-02-00 为 accepted、P1-02-01 为 ready；本任务包提交可由 Git 查询且在历史；CPython 3.11.15 下 133/133、默认校验和 partial 均退出 0。默认 python 不符时使用 C:\Users\beiyue\AppData\Local\Temp\cloud-ops-roadmap-uv-0.11.28\python\cpython-3.11.15-windows-x86_64-none\python.exe；该路径不可用则停止，不安装运行时。

交付一张精确九列章节清单和一份独立执行报告。产品提交只含阶段 01 README，信息为 docs: outline stage 01 server foundations；报告提交只含 P1-02-01 执行报告，信息为 docs: report P1-02-01 outline。禁止 amend/rebase。

必须验证：Python 3.11.15、133/133、默认校验、--outline-gate partial、git diff --check 全部退出 0；partial 阶段清单 2；非 README 正式学习 Markdown 仍为 0；两个提交范围各一文件。发现门禁不符、未知修改、规格/来源冲突、白名单外需求、动态 GPU 决策或无法在范围内修复的失败时立即停止。

完成回复列出两个完整提交、章节/标签/依赖/锚点统计、八类范围覆盖、自动和人工验证、来源、异常、风险、未决问题及远程操作；只能建议 submitted、rework 或 blocked，不得宣布 accepted。
```

## 门禁 1：任务信息

- **编号/名称：** P1-02-01 / 阶段 01 文件级章节清单
- **所属阶段：** Phase 1，波次 2A
- **状态/方式：** `ready`，串行
- **制定者：** Phase 1 主会话

## 门禁 2：目标与学习者价值

- **唯一目标：** 建立阶段 01 的权威文件级章节清单。
- **学习者价值：** 能把服务器规格中的资源名称、容量、拓扑、接口和可靠性信息映射到真实硬件职责，为 Linux、虚拟化、容器和 AI Infra 学习建立共同底座。

## 门禁 3：范围与非范围

### 必须覆盖

1. 计算机执行、存储和互连的整体资源模型；
2. CPU 的处理器、核心/线程、频率、缓存与基础拓扑；
3. 内存容量、通道、ECC 与 NUMA 的入门定位；
4. HDD、SSD/NVMe 等存储介质和容量、吞吐、延迟、耐久/可靠性指标的区别；
5. RAID 的冗余、容量、性能和故障域权衡，不写配置步骤；
6. 主板、总线与 PCI Express 扩展关系；
7. BIOS/UEFI、固件、启动与硬件清单的高层关系；
8. 机架式服务器、供电/散热、冗余部件和 GPU 服务器的版本中立概览。

### 合同边界

- 在唯一 `## 章节清单` 下使用精确九列表头；章节从 `01.01` 连续编号。
- 每章一个未来文件、一个主要目标、30–120 分钟；直接前置只列真正必需的阶段 00 或更早阶段 01 章节。
- 就业标签作为三个月路线派生输入；GPU 服务器概览可按就业主线价值独立判断，不得因“完整覆盖”强行标为必须学。
- 实践锚点只可引用未来 `CP-01`、`LAB-L1`；不得定义锚点或引用项目里程碑。

### 非范围

- 不写硬件安装、拆装、采购、选型报价、基准测试或故障处置；
- 不深入 Linux 性能调优、存储系统管理、数据中心网络或 GPU 软件栈；
- 不在标题或目标中锁定 PCIe、UEFI、SMBIOS、RAID、CPU 或 GPU 的动态版本/型号；
- 不修改任何白名单外文件。

## 门禁 4：允许文件

```text
知识库/01-计算机与服务器基础/README.md
项目管理/执行报告/P1-02-01-执行报告.md
```

## 门禁 5：前置与基线

- **前置：** P1-02-00 产品 `b9e8eb8610f46f4423bf9f959fd75760095eed48`、报告 `5d76e82832516a697909c4a6c6b0ef578c6febb9`，并由主会话接受。
- **工具：** CPython 3.11.15、现有标准库校验器；不修改工具。
- **内容：** PRD 阶段 01 和 Phase 1 章节合同；VitePress、许可证、Kubernetes、RuoYi 与云 GPU 全局基线保持不变，本任务不依赖其动态选择。
- **必读：** 以启动指令列出的文件为准；内容大纲任务必须完整阅读 `AI-CODEX-RULES.md`。

## 门禁 6：一手资料

核验日期均为 2026-07-15。来源只用于界定章节范围，支持关系与限制写入执行报告，README 不复制外部正文。

| 范围 | 入口 | 支持边界 |
|---|---|---|
| 服务器硬件清单 | DMTF SMBIOS：<https://www.dmtf.org/standards/smbios> | 支持处理器、内存、系统插槽等管理信息的分类定位；不证明具体机器配置 |
| CPU/内存/I/O 指标 | RHEL 9 性能文档：<https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/9/htmlsingle/monitoring_and_managing_system_status_and_performance/configuring-pmda-openmetrics> | 支持 CPU、内存、NUMA、块 I/O 等资源维度；不授权提前写 Linux 调优正文 |
| RAID | SNIA Common RAID DDF：<https://www.snia.org/tech_activities/standards/curr_standards/ddf> | 支持 RAID 组和跨磁盘数据结构定位；不单独证明所有 RAID 级别的教学结论 |
| PCI Express | PCI-SIG PCI Express Base：<https://pcisig.com/specification-overview/pci-express-base> | 支持互连、架构、设备扩展与编程接口定位；不要求锁定当前修订版 |
| 固件与启动 | UEFI Forum Specifications：<https://uefi.org/specifications> | 支持 UEFI、平台初始化和启动管理的职责边界；不得复制受限规范文本 |
| GPU 运维定位 | NVIDIA DCGM Overview：<https://docs.nvidia.com/datacenter/dcgm/latest/user-guide/> | 支持数据中心 GPU 需要面向管理员进行分组管理和观测；不证明具体型号、价格、显存或模型组合 |

搜索摘要、百科、论坛、培训博客、采购软文和 AI 文本不得作为关键范围的唯一依据。若动态入口变化，不把版本号写进章节 ID、标题或路线标签。

## 门禁 7：交付物

| # | 交付物 | 路径 | 边界 |
|---:|---|---|---|
| 1 | 阶段 01 文件级章节清单 | `知识库/01-计算机与服务器基础/README.md` | 八类范围、九列合同，无正文和步骤 |
| 2 | 执行报告 | `项目管理/执行报告/P1-02-01-执行报告.md` | 来源、统计、验证、风险和状态建议 |

## 门禁 8：验收标准

1. 唯一章节表、精确九列、`01.01` 起连续，预定文件与 ID 对齐。
2. 八类范围 8/8 覆盖，CPU/内存/存储/RAID/互连/固件/机架/GPU 边界清楚。
3. 每章一个目标且 30–120 分钟，无采购清单、型号列表、空章节或多主题捆绑。
4. 直接前置真实、无前向/自引用/循环；跨阶段引用只使用已接受的 `00.*`。
5. 三类就业标签合理，不隐藏必须前置；锚点只用 `CP-01`/`LAB-L1`。
6. 六个一手入口均记录支持和不能证明的内容，不把来源核验冒充环境验证。
7. 没有正式正文或白名单外变更；非 README 学习 Markdown 为 0。
8. 133/133、默认、partial 和 Git 检查全部通过；产品/报告提交各一文件。

## 门禁 9：验证

```powershell
python --version
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
python -B '脚本资源/项目校验/validate_repository.py' .
python -B '脚本资源/项目校验/validate_repository.py' . --outline-gate partial
git diff --check
git status --short
```

预期：Python 3.11.15；133/133；默认错误 0；`partial` 阶段清单 2、错误 0；非 README 正式学习 Markdown 0。报告记录阶段 01 章节数、三类标签数、依赖边数、锚点引用数和八类覆盖结果。

## 门禁 10：执行报告与提交

报告固定为 `项目管理/执行报告/P1-02-01-执行报告.md`，必须记录角色、启动门禁、两个文件、产品完整哈希、范围统计、八类覆盖、来源支持边界、命令/退出码、正式内容统计、异常、风险、未决、远程操作和状态建议；不得宣布 `accepted`。

| 顺序 | 唯一文件 | 提交信息 |
|---:|---|---|
| 1 | 阶段 01 README | `docs: outline stage 01 server foundations` |
| 2 | P1-02-01 执行报告 | `docs: report P1-02-01 outline` |

禁止 amend、rebase、reset、push、PR、发布或 GitHub 设置变更。

## 门禁 11：停止与回复

分支/origin/工作区/固定提交/状态/Python 不符，存在文件所有权冲突，需要白名单外写入、动态 GPU 决策、上层规格修改或验证无法在范围内修复时立即停止。

完成回复格式：任务和建议状态；两个文件与完整提交；章节/标签/依赖/锚点/八类覆盖统计；133/133、默认、partial、正式内容与人工检查；六类来源；异常、风险、未决和远程操作。不得自行更新看板或宣布 `accepted`。
