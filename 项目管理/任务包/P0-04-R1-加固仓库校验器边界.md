# P0-04-R1 加固仓库校验器边界任务包

## 任务信息

- **任务编号：** P0-04-R1
- **任务名称：** 加固仓库校验器边界
- **所属 Phase：** Phase 0
- **状态：** `ready`
- **执行方式：** 最小范围返工；优先交给原 P0-04 执行会话，也可交给未参与 P0-04B 审查的新执行会话；禁止由 P0-04B 审查会话修复
- **制定者：** 主会话
- **原实现提交：** `08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3`
- **原执行报告提交：** `d2952cf3ea5216169deaeaa9e320cf3f6d409363`
- **初审解锁提交：** `57f8434342b0f1c91d1fee83c4c2b21c195b4cbe`
- **独立审查提交：** `0bdc8410dec6833c88bdf7afdbf77b29937bdcde`
- **独立审查报告：** [P0-04B 独立审查报告](../验收记录/P0-04B-独立审查报告.md)
- **批准设计：** [P0-04 仓库校验工具设计说明](../执行计划/2026-07-11-P0-04-仓库校验工具-设计说明.md)
- **批准计划：** [P0-04 仓库校验工具实施计划](../执行计划/2026-07-11-P0-04-仓库校验工具-实施计划.md)

## 主会话裁决

P0-04B 建议 `changes-required`，报告 2 项必须修改和 1 项建议修改。主会话未直接采信报告结论，而是对固定实现独立复现：

1. **M-01 成立：** 合法章节的 `title` 改为只有开头双引号的标量后，`validate_front_matter()` 返回 `diagnostics=0`、`FM008=0`；当前受控解析器把畸形 quoted scalar 当作普通标量接受，违反批准设计“只接受约定子集、超出结构报 `FM008`”的要求。
2. **M-02 成立：** 调用公开 `main([chr(0)])` 时得到 `code=None`、直接抛出 `ValueError`、stdout/stderr 均为空；根路径规范化异常没有转换为稳定 `IO` 诊断，违反批准设计“根目录/读取异常返回 1、默认无 traceback”的要求。
3. **S-01 纳入本次返工：** 当前排序测试部分调用实现自身 `_error_sort_key` 证明排序，虽然独立探针确认现有排序行为正确，但应改成同一路径第 2 行和第 10 行的固定外部观察断言，消除自证成分。
4. P0-04B 审查交付物本身范围准确、证据可复现，因此审查任务可以接受；P0-04 工具保持 `rework`，必须完成 R1 和独立复审后才能最终验收。

本任务只修复以上边界和相应事实说明，不授权重构单文件架构、扩大 Markdown/YAML 支持、增加第三方依赖、修改原执行报告或接入 CI。

## 新会话启动指令

把下面整段原样发送给原 P0-04 执行会话，或一个未参与 P0-04B 审查的新 Codex 执行会话：

```text
请执行 cloud-ops-roadmap 的 P0-04-R1“加固仓库校验器边界”返工任务。

你的身份是返工执行会话，不是项目领导者、P0-04B 独立审查者、P0-04B-R1 复审者或最终验收者。你不得是提交 0bdc8410dec6833c88bdf7afdbf77b29937bdcde 的 P0-04B 审查会话；若你参与过该审查，立即停止。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap。你只负责 P0-04-R1，不得执行 P0-04B-R1、P0-05、P0-06 或其他任务，不得委派或与冲突写任务并行。

开始前必须使用 executing-plans 技能；修改实现前必须使用 test-driven-development 技能，先增加规定回归测试并取得能够复现 M-01/M-02 的正确红灯；每次声称通过、提交或结束前必须使用 verification-before-completion 技能取得新鲜证据。本任务由主会话基于独立审查证据签发，明确在当前 main 分支和固定工作目录串行执行；不得另建分支或 Git worktree。若技能的一般建议与运行时更高层指令、根 AGENTS.md、本任务包或批准计划冲突，以更高层指令、根 AGENTS.md、本任务包和批准计划为准。

采取任何写操作前，必须完整阅读：
1. 根目录 AGENTS.md；
2. 根目录 CODEX-PRD.md；
3. 根目录 AI-CODEX-RULES.md；
4. 项目管理/任务包/P0-04-R1-加固仓库校验器边界.md 全文；
5. 项目管理/任务包/P0-04-仓库校验工具.md；
6. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-设计说明.md；
7. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-实施计划.md；
8. 项目管理/执行报告/P0-04-执行报告.md；
9. 项目管理/验收记录/P0-04B-独立审查报告.md；
10. 项目管理/任务看板.md；
11. 三个当前工具文件全文。

唯一目标：以最小改动关闭 M-01、M-02 和 S-01，使受控 Front Matter 拒绝畸形 quoted scalar，使根路径 ValueError/OSError 稳定转换为脱敏 IO 诊断且 CLI 返回 1、无 traceback，并用独立观察断言强化数字排序回归测试。

唯一写入白名单只有以下 4 个文件：
1. 脚本资源/项目校验/validate_repository.py；
2. 脚本资源/项目校验/tests/test_validate_repository.py；
3. 脚本资源/项目校验/README.md；
4. 项目管理/执行报告/P0-04-R1-执行报告.md。

禁止修改原 P0-04 执行报告、P0-04B 审查报告、任务包、任务看板、设计、计划、PRD、AI 规则、模板、版本基线或任何白名单外文件。原执行报告是历史快照，不得回填；本次 R1 报告必须显式纠正其“读取失败不会 traceback”结论的适用范围并记录修复后的新证据。所有写入使用 apply_patch，不得创建依赖文件、CI、配置、临时脚本、缓存或仓库内临时输出。

启动时必须实际运行并确认：
git status --short --branch
git branch --show-current
git remote get-url origin
python --version
Select-String -LiteralPath '项目管理\任务看板.md' -Pattern '^\| P0-04 .*`rework`','^\| P0-04B .*`accepted`','^\| P0-04-R1 .*`ready`','^\| P0-04B-R1 .*`planned`'
git show -s --format='%H%n%P%n%s' 08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3
git show -s --format='%H%n%P%n%s' 0bdc8410dec6833c88bdf7afdbf77b29937bdcde
git hash-object '脚本资源/项目校验/validate_repository.py'
git hash-object '脚本资源/项目校验/tests/test_validate_repository.py'
git hash-object '脚本资源/项目校验/README.md'

预期：工作区干净、main、远程为 https://github.com/beiyue1988/cloud-ops-roadmap.git、Python 3.11.15、状态正确；三个当前 blob 依次为 1cb8dbe7247f95ef80b1db45f2af47984150ee6b、35cdf00775bddb9d1ed61bd826e0d186c045261c、d5688f514c74ccc40dae988f4ecd11913e62854a。任一条件不符立即停止，不得覆盖漂移实现或自行修改基线。

第一步只修改测试，增加并先运行至少以下 5 个新测试方法：
1. test_front_matter_rejects_unclosed_double_quoted_scalar；
2. test_front_matter_rejects_unclosed_single_quoted_scalar；
3. test_front_matter_rejects_mismatched_or_trailing_quoted_scalar；
4. test_main_converts_invalid_root_path_to_io_error；
5. test_main_converts_root_resolution_os_error_to_io_error。

M-01 测试必须覆盖顶层标量的未闭合双引号、未闭合单引号、首尾引号不匹配、闭合引号后尾随内容，并至少用子测试覆盖列表或 versions 映射中的畸形 quoted scalar。每个案例都必须得到 FM008 和真实行号；合法普通标量、成对单引号、成对双引号、列表和 versions 映射必须继续通过。

M-02 测试必须覆盖 ValueError 与 OSError 两类根路径规范化/发现异常。至少证明 main([chr(0)]) 返回 1、stderr 含 .:0: IO003、stdout 为空且 stdout/stderr 均无 Traceback；OSError 可用标准库 unittest.mock 在根路径处理边界注入，但断言必须观察公开 main() 的返回码和输出，不能只测试私有辅助函数。错误消息不得回显非法根路径或异常中的敏感原文。

S-01 必须修改现有排序测试或增加新测试：在同一个 Markdown 文件制造第 2 行和第 10 行诊断，直接断言渲染后的顺序是 2、10；不得调用 _error_sort_key、sorted(..., key=实现函数) 或其他被测私有函数作为期望值来源。

完成上述测试后运行完整 unittest，必须取得退出码非 0，且失败原因精确对应 M-01/M-02 尚未修复；测试语法、fixture、mock 泄漏或原有测试失败不算正确红灯。记录失败测试名、退出码和关键断言。

随后以最小实现修复：
1. 受控 scalar 解析必须区分 plain、合法成对单/双引号和非法 quoted scalar；任何只有开头/结尾引号、首尾不同、闭合引号后还有尾随内容的结构都生成 FM008。相同规则应覆盖顶层、列表项和 versions 映射值。不得宣称或实现完整 YAML 转义语法；遇到约定外复杂引号应稳定拒绝。
2. 根路径 resolve、exists/is_dir、Markdown 发现或相关路径处理抛出的 ValueError/OSError 必须转换为稳定、脱敏的 .:0: IO003 诊断。公开 validate_repository() 与 main() 都不得让这些预期文件系统/路径异常逃逸；main() 返回 1并默认无 traceback。只捕获已批准的路径/文件系统异常，不得用 bare except 或 catch Exception 吞掉编程错误。
3. README 将 IO 范围更新为 IO001–IO003，准确解释缺失/非目录、文件 UTF-8 读取和根路径解析/遍历错误；继续明确工具不是完整 YAML/Markdown 或安全审计。
4. 关闭 S-01 的测试自证，但不得改变当前正确的数字排序行为或公开函数签名。

修复后必须运行：完整 unittest（测试方法至少 39，原 34 项全部保留，规定行为仍 33/33）；临时 PYTHONPYCACHEPREFIX 的 py_compile；当前仓库 CLI；双运行逐字符比较；七个公开函数与 AST 第三方依赖 0；33 个规定测试名与实际测试映射；M-01/M-02/S-01 独立探针；README 字段；敏感信息脱敏；精确白名单；git diff --check；提交后完整复验。测试不得通过删除、跳过、弱化原断言或改变批准规则取得绿灯。

验证通过后形成两个提交。第一个提交只能包含三个工具文件，提交信息固定为 fix: harden repository validator boundaries。记录完整修订提交哈希。随后创建 项目管理/执行报告/P0-04-R1-执行报告.md，只提交该报告，提交信息固定为 docs: add P0-04-R1 execution report。R1 报告正文记录原实现、原报告、独立审查和 R1 修订提交完整哈希，不预测自身报告提交哈希；最终回复提供两个新提交哈希。禁止 amend、rebase、reset 或重建既有提交。

R1 报告必须包含：角色与范围；M-01/M-02/S-01 根因和关闭方式；三个原固定提交/审查哈希；新增测试及正确红灯；最终测试数和 33/33 映射；IO003、FM008、排序独立断言；编译、CLI、双运行、公开函数、依赖、脱敏、README、白名单、格式和提交证据；对原执行报告过宽结论的纠正说明；异常、权限、风险、未决问题和远程操作；建议状态 submitted。不得宣布 accepted 或启动 P0-04B-R1。

不得 push、创建 Pull Request、发布网站或修改 GitHub 设置。结束前确认 git status --short 无输出。最终回复必须给出 R1 报告路径、修订提交完整哈希、报告提交完整哈希、新增和总测试数、红灯与绿灯、M-01/M-02/S-01 关闭证据、33/33 映射、编译/CLI/双运行、依赖、白名单、异常、风险、未决问题、远程操作和建议状态 submitted。

遇到以下任一情况必须停止：当前会话是 P0-04B 审查者；状态、工作区、分支、远程、Python、固定提交或 blob 不符；需要白名单外写入；无法用最小标准库改动关闭 M-01/M-02；需要完整 YAML/Markdown 解析器或第三方依赖；新测试红灯不是目标缺陷；原有测试回归且无法在白名单内解释；无法保证 IO 脱敏或无 traceback；发现真实疑似凭据；无法形成两个规定提交；同一外部阻塞连续三次。
```

## 唯一写入白名单

```text
脚本资源/项目校验/validate_repository.py
脚本资源/项目校验/tests/test_validate_repository.py
脚本资源/项目校验/README.md
项目管理/执行报告/P0-04-R1-执行报告.md
```

除 Git 元数据外不得创建或修改其他文件。原 P0-04 执行报告和 P0-04B 审查报告保持不可变。

## 精确修复合同

| 问题 | 必须修复 | 不得扩大 |
|---|---|---|
| M-01 | 非法 quoted scalar → `FM008`；顶层及一层列表/映射一致 | 不实现完整 YAML、转义、锚点、标签或多行标量 |
| M-02 | 根路径 `ValueError/OSError` → `.:0: IO003`，CLI 返回 1且无 traceback | 不捕获所有异常，不隐藏程序错误 |
| S-01 | 同文件第 2/10 行直接期望顺序，不依赖私有排序函数 | 不改变当前诊断排序合同 |
| 文档事实 | README 说明 `IO001`–`IO003` 与限制；R1 报告纠正原报告过宽结论 | 不修改原执行报告历史快照 |

## 验收标准

1. 五个规定新测试方法存在，并覆盖 M-01/M-02 细分边界；
2. 先取得目标缺陷导致的正确红灯，再取得完整绿灯；
3. 测试方法至少 39，原 34 项保持，33 个批准行为仍完整；
4. 畸形顶层/列表/映射 quoted scalar 均报 `FM008`，合法受控标量不误报；
5. `ValueError` 与 `OSError` 均转换为 `IO003`，公开 `main()` 返回 1、无 traceback、无原值泄漏；
6. 排序测试不调用被测私有排序函数，直接证明同路径第 2 行先于第 10 行；
7. 七个公开函数、零第三方依赖、编译、真实仓库、双运行和脱敏全部回归通过；
8. README 与 R1 报告准确，不修改原报告；
9. 修订提交只含三个工具文件，R1 报告提交只含一份报告；
10. 工作区干净，未执行远程写操作。

## 提交要求

| 顺序 | 精确范围 | 固定提交信息 |
|---:|---|---|
| 1 | 三个工具文件 | `fix: harden repository validator boundaries` |
| 2 | 仅 `项目管理/执行报告/P0-04-R1-执行报告.md` | `docs: add P0-04-R1 execution report` |

不得 amend、rebase、reset、push 或重建被审历史。

## 停止条件

- 会话参与过 P0-04B 独立审查；
- 启动状态、固定提交、blob、工作区、分支、远程或 Python 不符；
- 需要修改白名单外文件或原历史报告；
- 需要第三方依赖、完整 YAML/Markdown、CI 或配置文件；
- 红灯不对应 M-01/M-02，或原有测试发生无法解释的回归；
- 无法保证路径异常脱敏、稳定 IO 和无 traceback；
- 发现真实疑似凭据或无法形成规定的两个提交；
- 同一外部阻塞连续出现三次。

## 完成后的回复

```text
P0-04-R1 已提交审查。
返工报告：项目管理/执行报告/P0-04-R1-执行报告.md
修订提交：填写 fix: harden repository validator boundaries 的完整哈希
报告提交：填写 docs: add P0-04-R1 execution report 的完整哈希
新增测试：填写实际数量
总测试：填写通过数/总数
行为映射：填写覆盖数/33
红灯：填写失败测试、退出码和目标根因
绿灯：填写测试数和退出码
M-01：填写 FM008 关闭证据
M-02：填写 IO003、返回码、无 traceback 和脱敏证据
S-01：填写同文件 2/10 行直接断言证据
回归：填写编译、CLI、双运行、公开函数和依赖结果
白名单：填写缺失数/越界数
异常：填写“无”或具体异常
风险：填写“无”或具体风险
未决问题：填写“无”或具体问题
远程操作：确认未 push、未创建 PR、未发布网站、未修改 GitHub 设置
建议状态：submitted；未宣布 accepted；未启动 P0-04B-R1
```
