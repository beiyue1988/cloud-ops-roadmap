# P1-02C-R 阶段 11 至 15 章节树独立审查任务包

## 新会话启动指令

```text
请执行 cloud-ops-roadmap 的 P1-02C-R 阶段 11 至 15 章节树独立审查任务。

你是新的独立组级审查会话，不是 P1-02-11 至 P1-02-15 的执行者、规格制定者、项目领导者或最终验收者。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap，分支固定为 main。你只审查固定组 C 成果及 00–15 累计图并写独立审查报告，不得修复产品或扩大范围。

任何写操作前按顺序完整阅读：AGENTS.md、CODEX-PRD.md、本任务包、Phase 1 导航与完整大纲设计说明和实施计划、Kubernetes 分层版本策略设计说明、AI-CODEX-RULES.md、来源优先级、校验器 README、任务看板、版本基线、P1-02-11 至 P1-02-15 的任务包与执行报告、阶段 00–15 README、P1-02A/A-R1/B 独立审查报告、ADR-0001 至 ADR-0005。

固定审查输入为主会话接受 P1-02-15 的提交 `b715748438926e05a4a6c10ea16339667ff2c050`；确认位于当前历史。再用 Git 查询本任务包签发提交，确认它在固定输入之后且当前 HEAD 只新增本任务包与看板分配。

唯一目标：独立判断阶段 11–15 作为组 C 是否覆盖自动化协作、虚拟化与阿里云、Docker、Kubernetes、DevOps/CI-CD，并与阶段 00–10 形成零基础连续、文件级可签发、职责不重复、前置闭合且最小、就业主线与就业后成长线隔离清楚的 00–15 累计章节树。

唯一仓库写入白名单：
- 项目管理/验收记录/P1-02C-阶段章节树独立审查报告.md

禁止修改任何 README、执行报告、任务包、看板、计划、PRD、ADR、版本基线、项目记忆或正式内容；禁止顺手修复；禁止仓库内临时脚本/缓存、正文、实验、配置、图表或新任务包；禁止调用项目禁用技能；禁止 fetch、pull、push、PR、tag、Release、发布、外部设置或云资源操作。

启动门禁：main、origin 正确、工作区干净；固定输入与签发提交可达且顺序正确；P1-02-11 至 P1-02-15 均 accepted、P1-02C-R assigned；固定 CPython 3.11.15；16 张九列表存在；正式内容目录非 README Markdown 为 0。任一不符立即停止并提供证据。

必须独立验证而非复述执行报告：133/133；默认错误 0；partial 精确 16 阶段、448 章、600 边、错误 0；catalogs 只因阶段 16–20 缺失而返回 5 个 OL001，不得有其他 OL 错误或 traceback；组 C 各阶段章节/边精确为 11 29/31、12 31/42、13 35/49、14 44/66、15 36/40；累计内部/跨阶段边 556/44；缺失、自引用、前向引用、环、传递冗余均 0。

就业标签表面统计必须为 291/76/81；批准的派生合同要求全部必须学递归闭包为 293 章，其中 291 必须学、仅建议学 `02.18` 与 `07.12`、就业后补学 0。必须确认阶段 12 的虚拟化/IaC 建议支线、阶段 13 的八个建议支线、阶段 14/15 全部就业后补学均未错误进入三个月就业主线。

必须人工审查：11–15 合计 175/175 覆盖合同；每章单一目标和 30–120 分钟粒度；阶段 11 核心 `11.20` 与 IaC `11.29`；阶段 12 阿里云就业闭环 `12.30` 与完整闭环 `12.31`；阶段 13 Compose 核心 `13.31` 与完整闭环 `13.35`；阶段 14 `14.44` 全量闭环和四层版本策略；阶段 15 `15.36` 全量闭环、平台中立主干与三个并列适配器。重点检查 Docker 不硬依赖云、Kubernetes 只接 `13.31`、通用 CI/CD 不硬依赖 Kubernetes、阶段 16 可稳定接入；动态组件保持阶段门；source-verified 未冒充 environment-verified；无 GPU/模型/云资源或外部平台选择依赖。

版本和来源抽查必须覆盖：Docker Engine 29 新装/升级存储差异与衍生发行版限制；Kubernetes 1.36.2 正式、1.36.3 upcoming 的签发事实和四层版本角色；containerd 2.3 LTS/CRI v1/cgroup driver；Calico 3.32 测试矩阵与遗留 PodSecurityPolicy 文本不得进入教学；Jenkins/GitLab/GitHub Actions 只作适配器；runner/credential/supply-chain 高信任边界。若动态事实在审查日变化，只报告并建议独立维护任务，不修改基线或产品。

报告必须列出固定输入、签发、实际命令与退出码、结构统计、全图与跨阶段边、标签闭包、覆盖、连续性、重复和职责边界、版本与来源抽查、执行报告一致性、Critical/Important/Minor 发现、风险、证据限制和 passed/changes-required/blocked 建议。无问题也明确写 0。最终 accepted 只能由 Phase 1 主会话裁决。

报告提交信息固定为 `docs: review group C stage outlines`，提交只能包含唯一报告。提交后重跑默认、partial、catalogs、正式正文、git show --check 和工作区检查。

停止条件：固定对象、分支、origin、状态或范围不符；测试/校验失败；正式正文不为 0；发现 Critical/Important；需要修改产品；权威规格冲突；需要外部写或动态版本/GPU/模型决策；审查独立性受损。

完成回复必须包含建议状态、固定输入/签发/报告完整哈希、自动验证、结构与图、标签闭包、人工审查、Critical/Important/Minor 数量及 file:line 摘要、风险、未决和远程操作确认。不得宣布组 C accepted。
```

## 1. 任务信息

- **任务编号：** `P1-02C-R`
- **任务名称：** 阶段 11 至 15 章节树独立审查
- **范围：** Phase 1 波次 2，组 C 集成门
- **状态：** `assigned`
- **执行方式：** 新的独立审查会话
- **最终验收者：** Phase 1 主会话

## 2. 固定输入与成果历史

- **固定输入：** `b715748438926e05a4a6c10ea16339667ff2c050`
- **当前权威范围：** 固定输入提交树中的阶段 00–15 README。
- **签发提交：** 审查会话用 Git 查询本任务包最近提交；该提交只能新增本任务包并更新任务看板。

| 任务 | 产品提交 | 报告提交 |
|---|---|---|
| P1-02-11 | `4b89e116aee547c5178fd1dc516703b10ddd0e50` | `1ea454c8d342fabfcb273f5dbf1c21bb88cce2b5` |
| P1-02-12 | `99d2b5b54ba8c8338161f3ebdbe2d0f5dedddaf0` | `bdb3c7df6fff4312f8ecb17a8014c5af2f8e9929` |
| P1-02-13 | `27783bb1e17f55007c3f249452d16d7a88447ca8` | `018df6d7e83bf8e5a73c830062aae5c8f6ee581b` |
| P1-02-14 | `d016d25f04840158efe012bc697ca0dc9e6bca2a` | `3af23ec02483b6f6051008d0bbb3af255711f661` |
| P1-02-15 | `ae0a902171b669413012bd39fef9af4ed9c9225f` | `d0d373eede402782ab6406f94c331906a852cd24` |

## 3. 唯一允许文件

```text
项目管理/验收记录/P1-02C-阶段章节树独立审查报告.md
```

只读审计脚本或中间数据只能放系统临时目录，并在结束前清理。

## 4. 固定结构与聚合预期

| 阶段 | 章节 | 直接前置边 | 内部边 | 跨阶段边 |
|---:|---:|---:|---:|---:|
| 11 | 29 | 31 | 28 | 3 |
| 12 | 31 | 42 | 37 | 5 |
| 13 | 35 | 49 | 47 | 2 |
| 14 | 44 | 66 | 65 | 1 |
| 15 | 36 | 40 | 37 | 3 |
| **组 C** | **175** | **228** | **214** | **14** |

累计固定预期：

- 16 阶段、448 章、600 边；内部边 556、跨阶段边 44；
- 标签 291/76/81；必须学递归闭包 293 = 291 必须学 + `02.18`/`07.12` 两个建议学 + 0 就业后补学；
- type：principle 147、methodology 162、service 34、language 17、concept 21、tool 28、hardware 14、cloud-service 19、platform 6；
- 投入：30–60 分钟 46、60–90 分钟 224、90–120 分钟 178；
- `CP-*` 448、`LAB-L1` 211、`LAB-L2` 166、`LAB-L3` 31；不得出现 `LAB-L4` 或 `PRJ-*`；
- 组 C 覆盖合同：11 为 29/29、12 为 31/31、13 为 35/35、14 为 44/44、15 为 36/36，合计 175/175。

组 C 新增跨阶段边必须精确为：

```text
11.01 -> 10.28
11.10 -> 03.16
11.21 -> 08.32
12.01 -> 03.20
12.02 -> 05.28
12.04 -> 07.13
12.08 -> 11.20
12.13 -> 11.29
13.01 -> 11.20
13.05 -> 07.12
14.01 -> 13.31
15.01 -> 11.20
15.16 -> 13.31
15.30 -> 14.44
```

加上组 A/B 已接受的 30 条跨阶段边，累计必须为 44。机械匹配不等于认可，审查者仍须逐边做移除后替代路径搜索和教学必要性判断。

## 5. 人工审查重点

- 阶段 11：Git/Ansible 核心不被 API/IaC 支线阻塞，动态工具组合不冒充已验证环境。
- 阶段 12：阿里云就业主线不依赖虚拟化/KVM/IaC 完整支线，不选择云 SKU、价格、GPU 或模型。
- 阶段 13：Docker 主线不依赖云阶段，Compose 核心不被 OCI/Buildx/rootless/扫描建议支线阻塞。
- 阶段 14：全部为就业后补学；概念中立、1.36.x 主实验、1.35.x 兼容、1.31.14 历史角色清楚；不教授 PodSecurityPolicy。
- 阶段 15：全部为就业后补学；平台中立语义先于三平台适配器；runner、凭据、制品与供应链边界安全。
- 00–15 从零基础到云原生交付连续，不依赖隐藏的阶段 16–20、GPU、模型或尚未选择的动态平台组合。

## 6. 必须运行的验证

```powershell
$py = 'C:\Users\beiyue\AppData\Local\Temp\cloud-ops-roadmap-uv-0.11.28\python\cpython-3.11.15-windows-x86_64-none\python.exe'
git status --short
git branch --show-current
git remote get-url origin
$input = git rev-parse b715748438926e05a4a6c10ea16339667ff2c050
$issue = git log -1 --format=%H -- '项目管理/任务包/P1-02C-R-阶段11至15章节树独立审查.md'
git merge-base --is-ancestor $input $issue
git merge-base --is-ancestor $issue HEAD
git show --check $issue
git diff-tree --no-commit-id --name-only -r $issue
& $py --version
& $py -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
& $py -B '脚本资源/项目校验/validate_repository.py' .
& $py -B '脚本资源/项目校验/validate_repository.py' . --outline-gate partial
$catalogs = & $py -B '脚本资源/项目校验/validate_repository.py' . --outline-gate catalogs 2>&1
$catalogsCode = $LASTEXITCODE
$catalogText = [string]::Join("`n", [string[]]$catalogs)
$ol001 = ([regex]::Matches($catalogText, ' OL001 ')).Count
Write-Output "catalogs_exit=$catalogsCode ol001=$ol001"
if ($catalogsCode -ne 1 -or $ol001 -ne 5 -or $catalogText -match ' OL00[2-9] | OL01[0-2] |Traceback') { exit 1 }
$roots = @('知识库','实验手册','项目实战','故障案例','面试体系','运维思维训练','企业规范')
$formal = @(Get-ChildItem -LiteralPath $roots -Recurse -File -Filter '*.md' | Where-Object { $_.Name -ne 'README.md' })
Write-Output "formal_non_readme=$($formal.Count)"
git diff --check
git status --short
```

另用审查者自己的只读逻辑解析 16 张九列表，验证固定统计、跨阶段边、可达性、五类图错误、逐边传递冗余、标签闭包和各阶段闭环隔离。不得调用执行报告结果代替复算。

## 7. 发现分级与报告

- **Critical：** 图或标签闭包错误、安全/版本边界破坏、正式正文越界、固定历史不可信；
- **Important：** 隐藏前置、职责重复、明显缺项、不可签发粒度、错误来源声明、报告与产品矛盾；
- **Minor：** 不影响连续性或签发的措辞、排序、证据呈现改进；
- **passed：** Critical/Important 均为 0 且门禁全部通过；
- **changes-required：** 至少一个 Critical/Important；
- **blocked：** 固定对象、环境或规格冲突使审查无法完成。

报告路径：`项目管理/验收记录/P1-02C-阶段章节树独立审查报告.md`。

报告必须包含角色与固定哈希、Git 证据、结构/覆盖/图、连续性与粒度、职责/重复/边界、标签/锚点、版本/来源、执行报告一致性、自动验证、发现、风险、未决、远程操作和状态建议。提交信息固定为 `docs: review group C stage outlines`，只含报告；不得宣布 accepted。
