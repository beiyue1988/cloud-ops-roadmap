# P0-04 仓库校验工具任务包

## 新会话启动指令

把下面整段原样发送给一个新的 Codex 会话：

```text
请执行 cloud-ops-roadmap 的 P0-04“仓库校验工具”任务。

你的身份是独立执行会话，不是项目领导者、规格制定者、独立审查者或最终验收者。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap。你只负责 P0-04，不得执行 P0-04B、P0-05、P0-06 或任何后续任务，不得把任务委派给其他会话，也不得与其他写任务并行。

开始前必须使用 executing-plans 技能，严格按已批准实施计划的任务 2 至任务 10 执行，并用复选框跟踪进度。实现任何功能前必须使用 test-driven-development 技能，先建立完整测试并取得实现缺失导致的正确红灯；每次声称通过、提交或结束前必须使用 verification-before-completion 技能取得新鲜证据。本任务已由用户批准并由主会话签发，明确在当前 main 分支和固定工作目录串行执行；不得另建分支或 Git worktree。若技能的一般建议与运行时更高层指令、根 AGENTS.md、本任务包或批准计划冲突，以更高层指令、根 AGENTS.md、本任务包和批准计划为准。

采取任何写操作前，必须完整阅读：
1. 根目录 AGENTS.md；
2. 根目录 CODEX-PRD.md；
3. 根目录 AI-CODEX-RULES.md；
4. 项目管理/任务包/P0-04-仓库校验工具.md 全文；
5. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-设计说明.md 全文；
6. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-实施计划.md 全文；
7. 项目管理/验收记录/P0-03-验收报告.md；
8. 模板/README.md；
9. 模板/执行报告模板.md；
10. 项目管理/任务看板.md。

唯一目标：创建一个仅依赖 Python 3.11 标准库、具有稳定错误编号和完整测试的本地 Markdown 仓库校验器，为后续零基础学习内容提供可重复、可解释的机械门禁。不得编写或修改任何学习章节、实验、项目、故障、面试、图表、版本基线、CI 或网站内容。

唯一写入白名单只有以下 4 个文件：
1. 脚本资源/项目校验/validate_repository.py；
2. 脚本资源/项目校验/tests/test_validate_repository.py；
3. 脚本资源/项目校验/README.md；
4. 项目管理/执行报告/P0-04-执行报告.md。

除 Git 元数据外，不得创建或修改任何白名单外文件。所有文件写入使用 apply_patch。不得创建 __init__.py、requirements.txt、pyproject.toml、CI 配置、临时脚本、测试快照、缓存或仓库内临时输出。不得修改现有 Markdown 以让校验器通过；真实仓库校验失败且根因位于白名单外时必须停止并报告。

启动时必须实际运行：
git status --short --branch
git branch --show-current
git remote get-url origin
python --version
Select-String -LiteralPath '项目管理\任务看板.md' -Pattern '^\| P0-04 .*`ready`'
Test-Path -LiteralPath '脚本资源\项目校验'
Test-Path -LiteralPath '项目管理\执行报告\P0-04-执行报告.md'

预期：工作区干净、分支 main、远程为 https://github.com/beiyue1988/cloud-ops-roadmap.git、Python 为 3.11.15、P0-04 为 ready，后两个目标均不存在。任一条件不符立即停止，不得覆盖、合并既有实现或自行调整任务包。

必须先创建完整测试文件，在 validate_repository.py 尚不存在时运行：
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
必须得到退出码非 0，且失败原因只能是实现模块不存在或无法加载；测试语法错误、fixture 错误或其他原因不算正确红灯。

测试必须至少包含 24 个测试方法，并覆盖实施计划规定的 33 个行为：合法 Front Matter；知识正文缺 Front Matter；未闭合分隔符；缺少键；重复键；空值与空列表；非法 type；非法 status；非法日期；合法列表与 versions；超出受控 YAML 子集；重复 chapter 两端报错；有效文件与图片链接；外链、邮件与纯锚点忽略；文件加锚点只检查文件；失效链接；越出根目录；绝对路径与盘符；大小写不一致；围栏内链接忽略；引用式与尖括号目标；正式内容占位符；围栏内占位符忽略；排除目录；模板标记残留；虚构 Secret 检出且不回显；中文路径与 UTF-8；非法 UTF-8；稳定数字排序；CLI 成功；CLI 失败；根目录不存在且无 traceback；真实仓库通过。每个行为必须断言具体规则编号或可观察结果，不得只断言错误列表非空。

实现必须保留实施计划第 2 节的七个公开函数及集中常量，内部按规则常量、文本预处理、单文件规则、仓库聚合和 CLI 五层组织。仅允许 Python 3.11.15 标准库，不得安装或引入 PyYAML、pytest、markdown 解析器或其他第三方依赖。Front Matter 只实现批准的受控子集，不声称支持完整 YAML。诊断必须使用相对 POSIX 路径、真实数字行号、稳定规则编号和确定性排序；根错误使用 .:0；敏感信息错误不得回显匹配值。

适用范围必须精确：全部 Markdown（排除 .git）检查本地链接和高置信度敏感信息；只有 学习路线、技术索引、知识库、实验手册、项目实战、故障案例、面试体系、运维思维训练、企业规范 检查围栏外占位符；只有 知识库/ 下非 README 的 Markdown 检查 13 键 Front Matter；成功解析 chapter 后才做跨文件重复检查。外链、邮件和纯锚点忽略；带锚点本地链接只验证文件部分；绝对路径、盘符、越界、缺失和大小写不一致必须区分；Windows 上也必须按实际文件名做大小写检查。

按 Front Matter、链接、占位符与敏感信息、仓库聚合与 CLI 的顺序逐步实现。每一步都运行完整 unittest；不得跳过测试、降低断言、删除规定行为或先写实现再补测试。虚构凭据必须在测试运行时分段拼接，错误输出和报告不得包含其原值。

最终必须完整运行并记录：
1. python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v；
2. 使用临时 PYTHONPYCACHEPREFIX 运行 python -m py_compile，完成后删除临时缓存；
3. python -B '脚本资源/项目校验/validate_repository.py' .；
4. 连续运行真实仓库校验两次，比较合并输出和退出码完全一致；
5. 使用 ast 检查导入只来自标准库和 __future__；
6. 精确白名单检查、git diff --check 和人工十项检查；
7. 提交后再次运行完整 unittest、真实仓库校验和 git status --short。

完整命令、预期和人工检查以批准实施计划任务 8 及第 5 节为准，不能用本段摘要替代。最终绿灯必须是 unittest 退出 0、测试方法不少于 24、33 个行为全部映射、py_compile 退出 0、真实仓库两次退出 0 且输出一致、第三方依赖 0、白名单缺失/越界 0/0、git diff --check 通过。

README 必须准确说明 Python 3.11、零安装、命令、--help、五类规则、适用与排除目录、受控 Front Matter/Markdown 子集、规则编号、输出和退出码、大小写/越界/围栏/脱敏行为、测试命令及明确限制。不得宣称检查外链可达性、锚点正确性、完整 YAML、完整 Markdown、安全审计或替代人工审查；本任务不接入 CI。

验证通过后必须形成两个提交。第一个提交只能包含三个工具目录文件，提交信息固定为 test: add repository content validator。随后创建执行报告并单独提交，第二个提交只能包含 项目管理/执行报告/P0-04-执行报告.md，提交信息固定为 docs: add P0-04 execution report。执行报告正文只记录第一个实现提交完整哈希，不预测或回填报告提交自身哈希；第二个完整哈希只在最终回复中给出。禁止 amend、rebase 或重建已被报告引用的提交。

执行报告必须包含：任务、日期、执行角色、建议状态；四个白名单文件；正确红灯与最终绿灯；33 个行为到实际测试方法的映射；Python 3.11.15、标准库导入、py_compile、真实仓库、双运行、白名单、格式和十项人工检查的实际命令、退出码和关键输出；实现提交完整哈希；异常、权限请求、风险、未决问题和远程操作。只能建议 submitted，不得宣布 accepted，不得更新任务看板或启动 P0-04B。

遇到以下任一情况必须立即停止：启动状态、分支、远程或 Python 版本不符；目标文件已存在；工作区不干净；需要白名单外写入；设计、计划、任务包、模板、PRD 或 AI 规则冲突；需要第三方依赖、CI 或配置文件；红灯原因不是实现缺失；真实仓库失败根因在白名单外；发现真实疑似凭据；无法保证脱敏、路径不逃逸或确定性输出；同一失败两次仍无法判断实现或测试缺陷；无法形成规定的两个提交；同一外部阻塞连续出现三次。

不得 push、创建 Pull Request、发布网站或修改 GitHub 设置。结束前确认 git status --short 无输出。最终回复必须给出：执行报告路径；实现提交完整哈希；报告提交完整哈希；测试方法数；33 个行为映射数；红灯和绿灯结果；py_compile、真实仓库与双运行结果；第三方依赖数；白名单缺失/越界数；异常、风险、未决问题；远程操作确认；建议状态 submitted。不能宣布 accepted。
```

## 门禁 1：任务信息

- **任务编号：** P0-04
- **任务名称：** 仓库校验工具
- **所属 Phase：** Phase 0
- **当前状态：** `ready`
- **执行方式：** 独立新会话，当前 `main` 分支串行执行，禁止委派或与其他写任务并行
- **制定者：** 主会话
- **批准设计：** [P0-04 仓库校验工具设计说明](../执行计划/2026-07-11-P0-04-仓库校验工具-设计说明.md)
- **批准计划：** [P0-04 仓库校验工具实施计划](../执行计划/2026-07-11-P0-04-仓库校验工具-实施计划.md)
- **前置验收：** [P0-03 验收报告](../验收记录/P0-03-验收报告.md)

## 门禁 2：唯一目标与学习者价值

- **唯一目标：** 建立 Python 3.11 标准库本地 Markdown 校验器、测试、使用说明和执行证据。
- **学习者价值：** 在正式内容生产前自动发现结构、链接、占位符和高置信度敏感信息问题，使零基础学习材料具有一致、可追溯的基础门禁。

## 门禁 3：范围与非范围

### 范围

- 先测试后实现，覆盖批准的 33 个行为；
- 实现 Front Matter、本地链接、占位符、敏感信息、跨文件章节编号和 CLI 校验；
- 编写准确的本地使用说明；
- 运行批准计划规定的自动与人工验证；
- 形成实现提交和执行报告提交。

### 非范围

- 不修改现有 Markdown 以迎合校验器；
- 不实现完整 YAML、完整 Markdown、外链可达性或锚点有效性；
- 不实现自动修复、安全审计、CI、VitePress 或 GitHub 工作流；
- 不编写正式学习内容、实验、项目、故障、面试或图表；
- 不执行 P0-04B、P0-05、P0-06 或远程写操作。

## 门禁 4：唯一写入白名单

```text
脚本资源/项目校验/validate_repository.py
脚本资源/项目校验/tests/test_validate_repository.py
脚本资源/项目校验/README.md
项目管理/执行报告/P0-04-执行报告.md
```

除 Git 元数据外，不允许创建或修改清单外文件。缓存、临时输出和编译产物不得进入仓库。

## 门禁 5：前置依赖、版本基线与必读文件

- **前置依赖：** P0-03、P0-03B 均为 `accepted`；P0-04 设计与实施计划已批准并提交。
- **固定治理提交：** P0-03 最终验收 `7b274d41926d44b6cf9633d8dfe193457203d551`；P0-04 批准计划 `a656e2ce9cd59d6bbf798720ceab625b8b5f6e7c`。
- **运行版本：** Python 3.11.15，仅标准库；不增加依赖清单。
- **必读文件：** 启动指令列出的十项文件，必须全文读取后再写入。
- **AI 规则：** 必须完整阅读 `AI-CODEX-RULES.md`，因为本任务创建脚本、测试和面向后续内容生产的机械门禁。

## 门禁 6：官方资料要求

- 规则范围和验收口径以本任务包、批准设计、批准计划、PRD、AGENTS.md 与 AI 规则为权威来源。
- 若对标准库行为存在疑问，只能查阅 Python 3.11 官方文档，例如 `unittest`、`pathlib`、`datetime`、`urllib.parse` 和 `argparse`；记录直接链接与核验日期。
- 本任务不需要核验产品动态版本，不得引入与批准规则无关的博客实现或第三方解析库。
- 搜索摘要、培训机构文章和 AI 生成结论不能作为唯一依据。

## 门禁 7：逐项交付物

| # | 交付物 | 路径 | 内容边界 | 所有者 |
|---:|---|---|---|---|
| 1 | 仓库校验器 | `脚本资源/项目校验/validate_repository.py` | 七个公开函数、稳定规则、CLI；仅标准库 | P0-04 执行会话 |
| 2 | 单元与集成测试 | `脚本资源/项目校验/tests/test_validate_repository.py` | 不少于 24 个方法，覆盖 33 个行为和真实仓库 | P0-04 执行会话 |
| 3 | 使用说明 | `脚本资源/项目校验/README.md` | 真实功能、命令、输出、适用范围和限制 | P0-04 执行会话 |
| 4 | 执行报告 | `项目管理/执行报告/P0-04-执行报告.md` | 红绿证据、行为映射、验证、提交、风险与建议状态 | P0-04 执行会话 |

## 门禁 8：33 个规定行为

| # | 行为 | 对应测试方法 |
|---:|---|---|
| 1 | 合法 Front Matter | `test_extract_valid_front_matter` |
| 2 | 知识正文缺 Front Matter | `test_front_matter_required_for_knowledge_article` |
| 3 | 未闭合分隔符 | `test_front_matter_requires_closing_delimiter` |
| 4 | 缺少键 | `test_front_matter_reports_missing_key` |
| 5 | 重复键 | `test_front_matter_reports_duplicate_key` |
| 6 | 空值与空列表 | `test_front_matter_rejects_empty_values` |
| 7 | 非法 `type` | `test_front_matter_rejects_invalid_type` |
| 8 | 非法 `status` | `test_front_matter_rejects_invalid_status` |
| 9 | 非法日期 | `test_front_matter_rejects_invalid_date` |
| 10 | 列表与 `versions` | `test_front_matter_accepts_lists_and_versions_map` |
| 11 | 超出 YAML 子集 | `test_front_matter_rejects_unsupported_yaml` |
| 12 | 重复 `chapter` 两端报错 | `test_duplicate_chapter_reports_both_files` |
| 13 | 文件和图片链接 | `test_valid_relative_links_and_images` |
| 14 | 外链、邮件、纯锚点 | `test_external_mail_and_anchor_are_ignored` |
| 15 | 文件加锚点 | `test_file_anchor_checks_file_only` |
| 16 | 失效链接 | `test_broken_relative_link` |
| 17 | 越出根目录 | `test_link_cannot_escape_root` |
| 18 | 绝对路径和盘符 | `test_absolute_and_drive_links_are_rejected` |
| 19 | 大小写不一致 | `test_case_mismatch_is_detected` |
| 20 | 围栏内链接 | `test_link_in_fence_is_ignored` |
| 21 | 引用式与尖括号目标 | `test_reference_and_angle_bracket_links` |
| 22 | 正式内容占位符 | `test_formal_content_placeholder_is_rejected` |
| 23 | 围栏内占位符 | `test_placeholder_in_fence_is_ignored` |
| 24 | 排除目录 | `test_placeholder_excluded_directories` |
| 25 | 模板标记残留正式内容 | `test_template_marker_in_formal_content_is_rejected` |
| 26 | 虚构 Secret 与脱敏 | `test_synthetic_secret_is_rejected_and_redacted` |
| 27 | 中文目录与 UTF-8 | `test_chinese_paths_and_utf8` |
| 28 | 无法解码 | `test_invalid_utf8_reports_io_error` |
| 29 | 错误稳定排序 | `test_errors_are_sorted_deterministically` |
| 30 | CLI 成功 | `test_main_returns_zero_on_success` |
| 31 | CLI 失败 | `test_main_returns_one_on_failure` |
| 32 | 根目录不存在且无 traceback | `test_missing_root_returns_one_without_traceback` |
| 33 | 真实仓库通过 | `test_current_repository_passes` |

测试方法可以增加，但不能删除、合并或弱化这些行为的可追溯性。

## 门禁 9：可操作验收标准

| # | 验收项 | 通过条件 | 证据 |
|---:|---|---|---|
| 1 | TDD 红灯 | 实现文件不存在时，完整测试以模块缺失原因失败 | 红灯命令、退出码和关键错误 |
| 2 | 接口与依赖 | 七个公开函数存在，导入仅标准库和 `__future__` | AST 导入清单、人工接口检查 |
| 3 | 行为覆盖 | 不少于 24 个测试方法覆盖 33 个规定行为 | `unittest -v` 和报告映射表 |
| 4 | 完整绿灯 | 全部测试通过，退出码 0 | 完整测试输出摘要 |
| 5 | 编译 | 实现与测试均可编译且无仓库缓存 | `py_compile` 退出码 0、缓存清理 |
| 6 | 真实仓库 | 当前仓库两次校验均退出 0，输出完全一致 | 两次退出码、比较结果和摘要 |
| 7 | 规则边界 | 适用目录、围栏、路径、大小写和脱敏符合设计 | 自动测试和十项人工检查 |
| 8 | README | 功能、限制、命令和测试说明与实现一致 | 文件审查 |
| 9 | 文件边界 | 白名单缺失和越界均为 0 | Git 文件集合检查 |
| 10 | 提交边界 | 实现与报告两个提交各自范围精确 | 完整提交哈希和 `diff-tree` |
| 11 | 远程边界 | 未 push、未建 PR、未发布、未改 GitHub 设置 | 执行报告与最终回复 |

## 门禁 10：验证命令与预期

完整命令以批准实施计划第 5 节和任务 8 至任务 10 为准，执行会话必须逐条运行，不得自行缩减。最低门禁如下：

```powershell
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
python -B '脚本资源/项目校验/validate_repository.py' .
git diff --check
git -c core.quotepath=false status --short
```

另外必须运行实施计划规定的临时 `PYTHONPYCACHEPREFIX` 编译、双运行逐字符比较、AST 标准库导入检查、精确白名单检查、提交后复验和十项人工检查。所有自动命令预期退出 0；最终工作区必须干净。

## 门禁 11：执行报告、提交与回复

执行报告固定为 `项目管理/执行报告/P0-04-执行报告.md`，必须按启动指令和实施计划任务 10 记录，不得把执行会话完成声明当作验收。

| 顺序 | 精确提交范围 | 固定提交信息 |
|---:|---|---|
| 1 | 三个 `脚本资源/项目校验/` 工具文件 | `test: add repository content validator` |
| 2 | 仅 P0-04 执行报告 | `docs: add P0-04 execution report` |

- 报告正文只记录实现提交完整哈希；报告提交哈希由最终回复提供。
- 禁止 amend、rebase、push、创建 Pull Request、发布网站或修改 GitHub 设置。
- 建议状态只允许 `submitted`、`rework` 或 `blocked`；执行会话无权写入 `accepted`。

## 完整启动指令复核

- [x] 明确独立执行角色、固定目录和禁止委派；
- [x] 列出全部必读文件与技能；
- [x] 只有一个主要目标并写明学习者价值；
- [x] 列出四个精确允许文件和非范围；
- [x] 固定分支、远程、Python、状态和目标不存在门禁；
- [x] 固定 33 个行为、红灯、绿灯和验证；
- [x] 固定两个提交、执行报告和完成回复；
- [x] 明确停止条件、远程限制和状态建议边界。

## 停止条件

- 启动状态、工作区、分支、远程或 Python 版本不符；
- 目标文件已存在、文件所有权冲突或需要白名单外写入；
- 规格、版本、模板、任务包、设计或计划存在冲突；
- 需要第三方依赖、CI、配置文件、自动修复或正式内容修改；
- 红灯原因不正确，或真实仓库失败无法在白名单内修复；
- 发现真实疑似凭据，或无法保证错误脱敏、路径边界和稳定输出；
- 同一失败两次仍无法判断根因；
- 无法形成规定的两个提交；
- 同一外部阻塞连续出现三次。

## 完成后的回复

```text
P0-04 已提交审查。
执行报告：项目管理/执行报告/P0-04-执行报告.md
实现提交：填写 test: add repository content validator 的完整哈希
报告提交：填写 docs: add P0-04 execution report 的完整哈希
测试方法：填写实际数量
行为映射：填写实际覆盖数/33
红灯：填写命令、退出码和正确失败原因
绿灯：填写通过数/总数和退出码
编译：填写退出码
真实仓库：填写两次退出码和摘要
双运行：填写是否完全一致
第三方依赖：填写实际数量
白名单：填写缺失数/越界数
异常：填写“无”或具体异常
风险：填写“无”或具体风险
未决问题：填写“无”或具体问题
远程操作：确认未 push、未创建 PR、未发布网站、未修改 GitHub 设置
建议状态：submitted；未宣布 accepted；未启动 P0-04B
```
