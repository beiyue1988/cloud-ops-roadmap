# P1-02B-R 阶段 06 至 10 章节树独立审查任务包

## 新会话启动指令

```text
请执行 cloud-ops-roadmap 的 P1-02B-R 阶段 06 至 10 章节树独立审查任务。

你是新的独立组级审查会话，不是 P1-02-06 至 P1-02-10 的执行者、规格制定者、项目领导者或最终验收者。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap，分支固定为 main。你只审查固定组 B 成果及 00–10 累计图并写独立审查报告，不得修复产品或扩大范围。

任何写操作前按顺序完整阅读：AGENTS.md、CODEX-PRD.md、本任务包、Phase 1 导航与完整大纲设计说明和实施计划、AI-CODEX-RULES.md、来源优先级、校验器 README、任务看板、版本基线、P1-02-06 至 P1-02-10 的任务包与执行报告、阶段 00–10 README、P1-02A 与 P1-02A-R1 独立审查报告、ADR-0001 至 ADR-0005。

固定审查输入为主会话接受 P1-02-10 的提交 `ca3bc04`；必须用 Git 解析完整哈希并确认位于当前历史。再用 Git 查询本任务包签发提交，确认它在固定输入之后且当前 HEAD 只新增本任务包与看板分配。

唯一目标：独立判断阶段 06–10 作为组 B 是否覆盖 Linux 日志、性能安全、Shell/Python、企业基础服务和数据服务，并与阶段 00–05 形成零基础连续、文件级可签发、职责不重复、前置闭合且最小、就业主线与建议支线隔离清楚的 00–10 累计章节树。

唯一仓库写入白名单：
- 项目管理/验收记录/P1-02B-阶段章节树独立审查报告.md

禁止修改任何 README、执行报告、任务包、看板、计划、PRD、ADR、版本基线、项目记忆或正式内容；禁止顺手修复；禁止仓库内临时脚本/缓存、正文、实验、配置、图表或新任务包；禁止调用项目禁用技能；禁止 fetch、pull、push、PR、发布或 GitHub 设置修改。

启动门禁：main、origin 正确、工作区干净；固定输入与签发提交可达且顺序正确；P1-02-06 至 P1-02-10 均 accepted、P1-02B-R assigned；固定 CPython 3.11.15；11 张九列表存在；正式内容目录非 README Markdown 为 0。任一不符立即停止并提供证据。

必须独立验证而非复述执行报告：133/133；默认错误 0；partial 精确 11 阶段、273 章、372 边、错误 0；catalogs 只因阶段 11–20 缺失而返回 10 个 OL001，不得有其他 OL 错误或 traceback；各阶段章节/边精确为 00 10/8、01 16/22、02 24/29、03 27/51、04 31/49、05 28/41、06 26/33、07 26/35、08 32/36、09 25/31、10 28/37；累计内部/跨阶段边 342/30；缺失、自引用、前向引用、环、传递冗余均 0。

就业标签表面统计必须为 222/50/1；批准的派生合同要求全部必须学递归闭包为 223 章，其中 222 必须学、仅建议学 `02.18`、就业后补学 0。不要使用 P1-02A-R 原 C-01 的已纠正低层规则；以 P1-02A-R1 接受结论为准。必须确认阶段 08 Python、阶段 09 可选服务、阶段 10 HA/消息支线均未进入就业主线闭包。

必须人工审查：06–10 合计 135/135 覆盖合同；每章单一目标和 30–120 分钟粒度；06 日志与 07 性能安全边界；08 Shell 核心 `08.17` 与 Python 完整闭环 `08.32`；09 核心 `09.15` 与完整 `09.25`；10 核心 `10.14`/`10.23`/`10.28`；阶段 11 可稳定接 `10.28`；Nginx/JDK/MySQL/Redis 等保持阶段门；RuoYi 固定 v3.9.2/0e2d75c；source-verified 未冒充 environment-verified；无 GPU/模型动态选择依赖。

报告必须列出固定输入、签发、实际命令与退出码、结构统计、全图与跨阶段边、标签闭包、覆盖、连续性、重复和职责边界、版本与来源抽查、执行报告一致性、Critical/Important/Minor 发现、风险、证据限制和 passed/changes-required/blocked 建议。无问题也明确写 0。最终 accepted 只能由 Phase 1 主会话裁决。

报告提交信息固定为 `docs: review group B stage outlines`，提交只能包含唯一报告。提交后重跑默认、partial、正式正文、git show --check 和工作区检查。

停止条件：固定对象、分支、origin、状态或范围不符；测试/校验失败；正式正文不为 0；发现 Critical/Important；需要修改产品；权威规格冲突；需要外部写或动态版本/GPU/模型决策；审查独立性受损。

完成回复必须包含建议状态、固定输入/签发/报告完整哈希、自动验证、结构与图、标签闭包、人工审查、Critical/Important/Minor 数量及 file:line 摘要、风险、未决和远程操作确认。不得宣布组 B accepted。
```

## 1. 任务信息

- **任务编号：** `P1-02B-R`
- **任务名称：** 阶段 06 至 10 章节树独立审查
- **范围：** Phase 1 波次 2，组 B 集成门
- **状态：** `assigned`
- **执行方式：** 新的独立审查会话
- **最终验收者：** Phase 1 主会话

## 2. 固定输入与成果历史

- **固定输入短哈希：** `ca3bc04`
- **当前权威范围：** 固定输入提交树中的阶段 00–10 README。
- **签发提交：** 审查会话用 Git 查询本任务包最近提交；该提交只能新增本任务包并更新任务看板。

组 B 固定产品与报告：

| 任务 | 产品提交 | 报告提交 |
|---|---|---|
| P1-02-06 | `51fc9c149b97646f1cc82be1d35a9f5a3455baba` | `049a7acb3bd43ce999ddc15a64a0d5f6df8cde5a` |
| P1-02-07 | `972a3b01663c1b9cdfce306597ded82f50960d42` | `9ab1b9132da33812baf60ae68e0f9afd2905db70` |
| P1-02-08 | `280bc0c941641a6be43b0c1e971cda493da8b7e6` | `0df29089b3627ce252360d36ad529df995fed3f9` |
| P1-02-09 | `546fa4e3df1e343eb705346b0ea2aee8ebc7ae90` | `02883470414c693459d718848e090d925378bdbf` |
| P1-02-10 | `6937d06558f6713bbe88909519a385cb72573f01` | `9fbf1e1d18b615432cf4fb09e0b3481aa7e927ab` |

## 3. 唯一允许文件

```text
项目管理/验收记录/P1-02B-阶段章节树独立审查报告.md
```

只读审计脚本或中间数据只能放系统临时目录，并在结束前清理。

## 4. 固定结构与聚合预期

| 阶段 | 章节 | 直接前置边 |
|---:|---:|---:|
| 00 | 10 | 8 |
| 01 | 16 | 22 |
| 02 | 24 | 29 |
| 03 | 27 | 51 |
| 04 | 31 | 49 |
| 05 | 28 | 41 |
| 06 | 26 | 33 |
| 07 | 26 | 35 |
| 08 | 32 | 36 |
| 09 | 25 | 31 |
| 10 | 28 | 37 |
| **合计** | **273** | **372** |

累计固定预期：

- 内部边 342、跨阶段边 30；
- 标签 222/50/1；必须学递归闭包 223 = 222 必须学 + `02.18` 建议学 + 0 就业后补学；
- type：principle 92、methodology 82、service 33、language 16、concept 15、tool 15、hardware 14、cloud-service 5、platform 1；
- 投入：30–60 分钟 37、60–90 分钟 150、90–120 分钟 86；
- `CP-*` 273、`LAB-L1` 167、`LAB-L2` 75；不得出现 `LAB-L3/L4` 或 `PRJ-*`；
- 组 B 覆盖合同：06 为 24/24、07 为 26/26、08 为 32/32、09 为 25/25、10 为 28/28，合计 135/135。

组 B 新增跨阶段边必须精确为：

```text
06.01 -> 05.28
07.01 -> 06.26
07.04 -> 01.02
07.06 -> 01.04
07.08 -> 01.07
07.09 -> 02.17
07.15 -> 03.04
08.01 -> 07.26
08.30 -> 03.16
09.01 -> 08.17
10.01 -> 09.15
```

加上组 A 已接受的 19 条跨阶段边，累计必须为 30。机械匹配不等于认可，审查者仍须逐边做移除后替代路径搜索和教学必要性判断。

## 5. 人工审查重点

- 阶段 06：日志身份、journald/rsyslog、轮转、Audit 读取、证据闭环，不抢阶段 07 规则选择或阶段 16 集中日志。
- 阶段 07：性能假设与安全最小权限双闭环，不抢容器资源、持续监控、SRE 容量或合规实施。
- 阶段 08：共同可靠性底座、Shell 就业主线和 Python 建议支线分离；`08.17` 可独立向后供给。
- 阶段 09：Nginx+Java 核心不依赖 Tomcat/DNS/chrony/NFS/Samba 建议支线；动态服务组合保持阶段门。
- 阶段 10：MySQL+Redis 核心不依赖两类 HA、RabbitMQ 或 Kafka；备份、复制、一致性和恢复语义完整。
- 00–10 从零基础到企业服务/数据服务连续，不依赖隐藏的后续 Kubernetes、可观测、AI Infra 或 SRE 知识。

## 6. 必须运行的验证

```powershell
$py = 'C:\Users\beiyue\AppData\Local\Temp\cloud-ops-roadmap-uv-0.11.28\python\cpython-3.11.15-windows-x86_64-none\python.exe'
git status --short
git branch --show-current
git remote get-url origin
$input = git rev-parse ca3bc04
$issue = git log -1 --format=%H -- '项目管理/任务包/P1-02B-R-阶段06至10章节树独立审查.md'
git merge-base --is-ancestor $input $issue
git merge-base --is-ancestor $issue HEAD
git show --check $issue
git diff-tree --no-commit-id --name-only -r $issue
& $py --version
& $py -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
& $py -B '脚本资源/项目校验/validate_repository.py' .
& $py -B '脚本资源/项目校验/validate_repository.py' . --outline-gate partial
$catalogs = & $py -B '脚本资源/项目校验/validate_repository.py' . --outline-gate catalogs 2>&1
$catalogsCode = $LASTEXITCODE
$catalogText = [string]::Join("`n", [string[]]$catalogs)
$ol001 = ([regex]::Matches($catalogText, ' OL001 ')).Count
Write-Output "catalogs_exit=$catalogsCode ol001=$ol001"
if ($catalogsCode -ne 1 -or $ol001 -ne 10 -or $catalogText -match ' OL00[2-9] | OL01[0-2] |Traceback') { exit 1 }
$roots = @('知识库','实验手册','项目实战','故障案例','面试体系','运维思维训练','企业规范')
$formal = @(Get-ChildItem -LiteralPath $roots -Recurse -File -Filter '*.md' | Where-Object { $_.Name -ne 'README.md' })
Write-Output "formal_non_readme=$($formal.Count)"
git diff --check
git status --short
```

另用审查者自己的只读逻辑解析 11 张九列表，验证固定统计、跨阶段边、可达性、五类图错误、逐边传递冗余、标签闭包和双闭环隔离。不得调用执行报告结果代替复算。

## 7. 发现分级与报告

- **Critical：** 图或标签闭包错误、安全/版本边界破坏、正式正文越界、固定历史不可信；
- **Important：** 隐藏前置、职责重复、明显缺项、不可签发粒度、错误来源声明、报告与产品矛盾；
- **Minor：** 不影响连续性或签发的措辞、排序、证据呈现改进；
- **passed：** Critical/Important 均为 0 且门禁全部通过；
- **changes-required：** 至少一个 Critical/Important；
- **blocked：** 固定对象、环境或规格冲突使审查无法完成。

报告路径：`项目管理/验收记录/P1-02B-阶段章节树独立审查报告.md`。

报告必须包含角色与固定哈希、Git 证据、结构/覆盖/图、连续性与粒度、职责/重复/边界、标签/锚点、版本/来源、执行报告一致性、自动验证、发现、风险、未决、远程操作和状态建议。提交信息固定为 `docs: review group B stage outlines`，只含报告；不得宣布 accepted。
