# P0-04B 仓库校验工具独立审查任务包

## 当前状态与固定审查对象

- **状态：** `ready`
- **P0-04 实现提交：** `08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3`
- **P0-04 报告提交：** `d2952cf3ea5216169deaeaa9e320cf3f6d409363`
- **主会话初审日期：** 2026-07-11
- **主会话初审摘要：** 提交范围精确，工具文件无漂移；完整测试 34/34、规定行为 33/33、公开函数 7/7、第三方依赖 0；`py_compile`、真实仓库、双运行、脱敏、README、报告与 Git 检查均通过；工作区干净。
- **固定对象规则：** 审查会话只能审查以上两个提交及当前无漂移的三个工具文件，不得改写、重建或从执行报告推测其他对象。

## 新会话启动指令

把下面整段原样发送给一个未参与 P0-04 实现或执行报告的新 Codex 会话：

```text
请执行 cloud-ops-roadmap 的 P0-04B“仓库校验工具独立审查”任务。

你的身份是独立审查会话，不是项目领导者、P0-04 执行者、缺陷修复者或最终验收者。你必须没有参与 P0-04 校验器实现、测试、README 或执行报告的创建。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap。你只负责 P0-04B，不得修复被审查文件，不得执行 P0-05、P0-06 或其他任务，不得把审查委派给其他会话。

开始前必须使用 executing-plans 技能，严格按已批准实施计划的任务 12 和本任务包执行；提交审查报告前必须使用 verification-before-completion 技能取得新鲜证据。本任务已由主会话解锁，明确在当前 main 分支和固定工作目录串行执行；不得另建分支或 Git worktree。若技能的一般建议与运行时更高层指令、根 AGENTS.md、本任务包或批准计划冲突，以更高层指令、根 AGENTS.md、本任务包和批准计划为准。

采取任何写操作前，必须完整阅读：
1. 根目录 AGENTS.md；
2. 根目录 CODEX-PRD.md；
3. 根目录 AI-CODEX-RULES.md；
4. 项目管理/任务包/P0-04-仓库校验工具.md；
5. 项目管理/任务包/P0-04B-仓库校验工具独立审查.md 全文；
6. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-设计说明.md；
7. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-实施计划.md；
8. 项目管理/执行报告/P0-04-执行报告.md；
9. 项目管理/任务看板.md；
10. 脚本资源/项目校验/validate_repository.py、tests/test_validate_repository.py 和 README.md 全文。

唯一目标：独立判断固定的 P0-04 校验器是否真实满足批准设计、33 个行为、路径与脱敏安全边界、确定性、README 真实性和执行报告一致性，防止有缺陷的机械门禁扩散到后续学习内容。不得信任执行报告或主会话初审数字代替独立证据。

固定审查对象只有：
P0-04 实现提交 08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3
P0-04 报告提交 d2952cf3ea5216169deaeaa9e320cf3f6d409363

唯一允许创建的文件是：
项目管理/验收记录/P0-04B-独立审查报告.md

除此之外只能读取文件和运行不修改仓库的检查。所有报告写入必须使用 apply_patch。禁止修改校验器、测试、README、P0-04 执行报告、任务包、任务看板、设计、计划、PRD、AI 规则、版本记录或其他文件。发现问题只记录证据、分级和建议，不得顺手修复。

启动时必须实际运行：
git status --short --branch
git branch --show-current
git remote get-url origin
python --version
Select-String -LiteralPath '项目管理\任务看板.md' -Pattern '^\| P0-04 .*`submitted`','^\| P0-04B .*`ready`'
git show -s --format='%H%n%P%n%s' 08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3
git show -s --format='%H%n%P%n%s' d2952cf3ea5216169deaeaa9e320cf3f6d409363
git -c core.quotepath=false diff-tree --no-commit-id --name-only -r 08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3
git -c core.quotepath=false diff-tree --no-commit-id --name-only -r d2952cf3ea5216169deaeaa9e320cf3f6d409363
git diff --exit-code 08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3 HEAD -- '脚本资源/项目校验'

预期：工作区干净、分支 main、远程为 https://github.com/beiyue1988/cloud-ops-roadmap.git、Python 为 3.11.15；P0-04 为 submitted、P0-04B 为 ready；实现提交只含三个工具文件，报告提交只含一份执行报告；报告提交的父提交是实现提交；当前三个工具文件相对实现提交无漂移。任一条件不符立即停止，不得自行修复或改选审查对象。

必须独立运行并记录以下机械证据：
1. python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v，预期退出 0、34 个现有测试全部通过且测试方法不少于 24；
2. 使用系统临时目录中的 PYTHONPYCACHEPREFIX 运行 python -m py_compile 检查实现和测试，预期退出 0，随后安全删除临时缓存且仓库不产生 __pycache__；
3. python -B '脚本资源/项目校验/validate_repository.py' .，预期退出 0并输出扫描统计；
4. 连续运行真实仓库校验两次，逐字符比较合并输出与两个退出码，预期退出码 0/0 且完全一致；
5. 使用 ast 统计七个公开函数、实际测试方法和两文件导入，预期公开函数 7/7、测试方法不少于 24、第三方依赖 0；
6. 从 P0-04 任务包提取 33 个规定测试名，与实际测试和执行报告映射逐项比较，预期 33/33、缺失 0；
7. git show --check、git diff --check、提交文件集合、工具漂移、缓存目录和最终 git status 检查全部通过。

不得只复跑现有测试。还必须在系统临时目录或不落盘 Python 进程中独立构造至少六组场景：
1. 完整合法知识章节，预期无错误；
2. 同一临时仓库同时包含重复 chapter、失效链接和正式正文占位符，预期两端 CH001、LINK001 和对应 PH 规则；
3. 链接越出仓库根目录以及链接大小写与实际文件名不一致，预期 LINK002 和 LINK004；
4. 围栏内链接与占位符不误报，但运行时分段拼接的虚构 Secret 仍触发 SEC，且任何输出均不含原值；
5. 非法 UTF-8 Markdown，预期 IO002 且继续返回稳定诊断；
6. 同一临时仓库连续运行两次，错误顺序、退出码和输出完全一致，数字行号排序正确且敏感值不回显。

临时数据不得落入仓库。虚构凭据必须运行时分段拼接，不得在源码、命令输出或报告中保存看似可用的完整值。任何独立场景失败都必须记录原始现象并进入问题分级，不得修改实现。

还必须逐项完成人工审查：规则适用目录；Front Matter 受控子集与假阴性/假阳性；路径逃逸；Windows 大小写；围栏与行号；敏感信息脱敏；I/O 和无 traceback；数字排序；README 真实性；测试是否自证或弱断言；标准库与全局状态；执行报告和 Git 一致性。关键词命中不能代替阅读实现、测试和 README。

问题分为阻塞、必须修改、建议修改和通过项。建议结论只能是 passed、passed-with-notes 或 changes-required：存在阻塞或必须修改必须是 changes-required；仅有建议可以是 passed-with-notes；没有问题才是 passed。你无权宣布 P0-04 或 P0-04B accepted，也不得更新任务看板。

完成后只创建并提交 项目管理/验收记录/P0-04B-独立审查报告.md，提交信息固定为 docs: review repository content validator。报告必须包含独立性声明、两个固定完整哈希、自动复验命令与退出码、测试数与 33 行为追溯、六组独立场景、十二项人工审查、四级问题、执行报告一致性、风险、未决问题、建议结论和远程操作确认。禁止 amend、rebase、push、创建 Pull Request、发布网站或修改 GitHub 设置。

结束前确认审查提交只含一份报告，git status --short 无输出。最终回复必须给出审查报告路径、审查提交完整哈希、两个固定提交、自动复验通过数/总数、测试方法数、行为追溯数/33、独立场景通过数/总数、人工审查结果、四级问题数量、建议结论、风险、未决问题和远程操作确认。只能建议结论，不能宣布 accepted。

遇到以下任一情况必须立即停止：审查会话参与过 P0-04；任务状态、工作区、分支、远程或 Python 版本不符；固定提交缺失、范围不符或工具发生漂移；需要写入唯一报告外文件；无法在只读边界完成复验；发现真实疑似凭据；无法保证临时敏感值不回显；同一外部阻塞连续出现三次。
```

## 任务信息

- **任务编号：** P0-04B
- **任务名称：** 仓库校验工具独立审查
- **所属 Phase：** Phase 0
- **执行方式：** 与 P0-04 不同的新会话，串行、只读复验；原则上只创建一份审查报告
- **制定者：** 主会话
- **批准设计：** [P0-04 仓库校验工具设计说明](../执行计划/2026-07-11-P0-04-仓库校验工具-设计说明.md)
- **批准计划：** [P0-04 仓库校验工具实施计划](../执行计划/2026-07-11-P0-04-仓库校验工具-实施计划.md)

## 解锁门禁结果

主会话已逐项确认：

1. [x] P0-04 三个工具文件已经形成独立实现提交；
2. [x] `项目管理/执行报告/P0-04-执行报告.md` 已形成独立报告提交；
3. [x] 实现提交只含三个工具文件，报告提交只含执行报告；
4. [x] 完整测试、`py_compile`、真实仓库 CLI 和双运行符合批准计划；
5. [x] 34 个测试方法通过，33 个规定行为全部可追溯；
6. [x] 当前仓库通过、没有白名单越界、没有敏感值回显，工作区干净；
7. [x] 两个固定完整哈希、主会话初审摘要和完整启动指令已写入；
8. [x] 任务看板同步更新为 P0-04 `submitted`、P0-04B `ready`。

解锁只授权独立审查，不代表 P0-04 或 P0-04B 已通过最终验收。

## 唯一目标与学习者价值

- **唯一目标：** 由未参与 P0-04 实现和执行报告的会话，独立判断校验器是否真实满足批准设计、行为覆盖、安全边界和可重复性要求。
- **学习者价值：** 防止存在自证测试、路径逃逸、误报漏报、敏感信息回显或不稳定输出的工具成为后续学习内容门禁。

## 审查范围

- 固定的 P0-04 实现提交与执行报告提交；
- 三个工具文件和 P0-04 执行报告；
- 批准设计、实施计划、P0-04 任务包和实际 Git 范围；
- 完整自动复验、至少六组独立临时场景和人工实现审查；
- 执行报告与实际仓库事实的一致性。

## 非范围

- 不修复实现、测试、README 或执行报告；
- 不修改任务包、任务看板、PRD、AI 规则、版本基线或计划；
- 不创建 CI、配置、正式内容或后续任务产物；
- 不执行 P0-05、P0-06 或远程写操作；
- 不宣布 P0-04 或 P0-04B `accepted`。

## 唯一写入白名单

唯一允许创建：

```text
项目管理/验收记录/P0-04B-独立审查报告.md
```

除此之外只能读取和运行不改变仓库内容的检查。发现缺陷只记录证据和建议，不得顺手修复。

## 必读文件

审查会话必须全文阅读：

1. `AGENTS.md`；
2. `CODEX-PRD.md`；
3. `AI-CODEX-RULES.md`；
4. `项目管理/任务包/P0-04-仓库校验工具.md`；
5. 本任务包；
6. 批准设计与实施计划；
7. `项目管理/执行报告/P0-04-执行报告.md`；
8. `项目管理/任务看板.md`；
9. 三个工具文件全文。

## 官方资料要求

- 主要审查依据是批准治理文件、固定 Git 提交和实际运行结果。
- 若对标准库语义存在疑问，只能使用 Python 3.11 官方文档，并在报告中记录直接链接、适用范围和核验日期。
- 不得用执行报告自述、搜索摘要、培训机构文章或 AI 结论替代独立证据。

## 必须复跑的自动检查

审查会话必须独立运行，不得复制 P0-04 报告数字：

1. 工作区、分支、远程、任务状态和两个固定提交检查；
2. 实现提交三个文件、报告提交一个文件的精确范围；
3. 完整 `unittest`，确认方法数不少于 24；
4. 使用临时 `PYTHONPYCACHEPREFIX` 的 `py_compile`；
5. 当前仓库 CLI 校验；
6. 两次运行退出码和输出逐字符一致；
7. AST 标准库导入检查；
8. `git show --check`、`git diff --check` 和最终工作区检查。

精确命令以批准实施计划任务 12 和本任务包启动指令为准；固定提交哈希已经写入，审查会话不得从报告正文改选对象。

## 独立临时场景

不得只调用现有测试。必须在系统临时目录或不落盘进程中另行构造至少六组：

1. 合法知识章节；
2. 同时存在重复 `chapter`、失效链接和正文占位符；
3. 链接越界与实际文件名大小写不一致；
4. 围栏中链接/占位符不误报，但运行时构造的虚构 Secret 仍报错；
5. 非法 UTF-8；
6. 两次运行输出完全一致且虚构敏感值未回显。

临时数据不得写入仓库；报告不得回显虚构敏感值原文。

## 必须完成的人工审查

逐项给出“通过/不通过/需说明”和直接证据：

1. 规则适用目录是否精确，没有误扫治理与模板目录；
2. Front Matter 是否只接受批准子集并拒绝未知结构；
3. 本地路径是否无法逃逸根目录；
4. Windows 大小写检查是否稳定且不误报正确路径；
5. 围栏屏蔽是否保持真实行号；
6. 敏感信息诊断是否始终脱敏；
7. UTF-8、根目录不存在和其他 I/O 异常是否无 traceback；
8. 错误排序是否使用数字行号而非纯字符串；
9. README 是否准确描述真实功能和限制；
10. 测试是否存在按实现逻辑自证、弱断言或遗漏行为；
11. 实现是否只有标准库且没有全局可变状态污染；
12. 执行报告是否与命令、Git 和文件事实一致。

## 问题分级与建议结论

- **阻塞：** 固定对象不明、范围漂移、敏感信息风险、路径逃逸或无法独立复验；
- **必须修改：** 规定行为缺失、测试自证、错误回显敏感值、输出不稳定或 README 严重失真；
- **建议修改：** 不阻塞门禁启用，但可改善可维护性或诊断体验；
- **通过项：** 有独立命令或文件证据支持。

建议结论只允许：

- `passed`：无阻塞、无必须修改、无建议；
- `passed-with-notes`：无阻塞、无必须修改，仅有建议；
- `changes-required`：存在任一阻塞或必须修改。

审查会话无权宣布 `accepted`。

## 审查报告要求

`项目管理/验收记录/P0-04B-独立审查报告.md` 必须包含：

1. 任务编号、日期和未参与 P0-04 的独立性声明；
2. 被审查实现提交与报告提交的完整哈希；
3. 自动复验命令、退出码和关键输出；
4. 测试方法数和 33 个行为追溯结果；
5. 六组以上独立临时场景及结果；
6. 十二项人工审查及证据；
7. 四级问题清单和执行报告一致性；
8. 风险、未决问题和建议结论；
9. 最终验收权属于主会话的声明；
10. 未执行远程写操作的确认。

## 提交与远程限制

只能提交独立审查报告，提交信息固定为：

```text
docs: review repository content validator
```

禁止 amend、rebase、push、创建 Pull Request、发布网站或修改 GitHub 设置。

## 停止条件

- 本任务不是 `ready` 或缺少可直接复制的启动指令；
- 审查会话参与过 P0-04 实现或执行报告；
- P0-04/P0-04B 状态不符，工作区不干净，分支或远程不符；
- 两个固定提交缺失、不可达、范围不符或当前工具文件发生漂移；
- 需要写入唯一报告外文件；
- 发现真实疑似凭据或无法保证临时敏感值不回显；
- 无法在只读边界完成复验；
- 同一外部阻塞连续出现三次。

## 完成回复格式

```text
P0-04B 独立审查报告已提交。
审查报告：项目管理/验收记录/P0-04B-独立审查报告.md
审查提交：填写 docs: review repository content validator 的完整哈希
实现提交：填写任务包锁定的完整哈希
报告提交：填写任务包锁定的完整哈希
自动复验：填写通过项数/总项数
测试方法：填写实际数量
行为追溯：填写覆盖数/33
独立场景：填写通过数/总数
人工审查：填写通过/需说明/不通过数量
问题分级：填写阻塞/必须修改/建议修改/通过项数量
建议结论：passed、passed-with-notes 或 changes-required
风险：填写“无”或具体风险
未决问题：填写“无”或具体问题
远程操作：确认未 push、未创建 PR、未发布网站、未修改 GitHub 设置
最终验收：未宣布 accepted，等待主会话裁决
```
