# P1-02-05 阶段 05 文件级章节清单任务包

## 新会话启动指令

```text
请执行 cloud-ops-roadmap 的 P1-02-05“阶段 05 文件级章节清单”任务。

你是新的独立内容规划执行会话，不是项目领导者、规格制定者、独立审查者、正式正文作者或最终验收者。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap，分支固定为 main。只执行本任务，不得委派、扩大范围或与冲突写任务并行。

任何写操作前完整阅读：AGENTS.md、CODEX-PRD.md、本任务包、Phase 1 导航与完整大纲设计说明和实施计划、AI-CODEX-RULES.md、软件版本基线、来源优先级、校验器 README、阶段 00–08 README、P1-02-04 原任务包与 R1 任务包、阶段 04 两份执行报告、P1-02A-M1 设计/计划/任务包/执行报告和任务看板。

唯一目标：在不创建正式学习正文的前提下，把阶段 05“Linux 系统管理”规划成可机械解析、可直接签发单文件正文任务的完整文件级章节清单，覆盖启动、systemd 深入管理、存储、LVM、文件系统、主机网络、时间、内核参数、备份恢复和服务器维护闭环。

唯一写入白名单：
- C:\Workspace\Projects\cloud-ops-roadmap\知识库\05-Linux系统管理\README.md；
- C:\Workspace\Projects\cloud-ops-roadmap\项目管理\执行报告\P1-02-05-执行报告.md。

README 必须保留现有阶段目标、主要主题、前置阶段、阶段产出、导航与边界，在“阶段产出”和“导航”之间新增唯一“## 章节清单”及九列受控表。章节 ID 从 05.01 连续；预定文件、标题、目标、type、直接前置、投入、就业标签、实践锚点严格遵守大纲合同。标题和目标只写规划语义，不写正式解释、命令、配置、步骤或答案。

必须完整覆盖 22 类：1 系统管理范围、变更安全与恢复证据；2 固件/引导程序/内核/initramfs/systemd 启动链；3 target、默认目标、rescue/emergency 边界；4 unit 加载路径、供应商/本地覆盖和 drop-in；5 unit 依赖、排序与激活关系；6 自定义 service unit 的职责、身份、环境与失败边界；7 systemd timer 与用户 cron 的职责差异；8 块设备拓扑、命名与稳定标识；9 分区表与分区边界；10 LVM 的 PV/VG/LV、容量变更和快照边界；11 XFS/ext4 等文件系统定位与创建边界；12 临时挂载、持久挂载、挂载选项与卸载影响；13 fstab、稳定标识、启动挂载及失败恢复；14 容量、inode、文件系统状态和检查/修复证据；15 swap 角色、形式与生命周期；16 NetworkManager 设备、连接配置档、运行态与持久态；17 主机地址、网关、路由与 DNS 配置变更、验证和回滚；18 主机名与本地/网络名称解析边界；19 系统时钟、RTC、时区与 chrony/NTP 同步证据；20 内核运行时参数、持久化和启动参数边界；21 备份范围、完整/增量、保留、完整性及恢复验证；22 服务器初始化、维护窗口、重启、回滚和最终证据闭环。

阶段边界固定：阶段 04 保留命令行、文件文本、用户权限、进程、RPM/DNF、现有 service 基础生命周期和用户 cron；阶段 05 才负责启动链、unit 依赖/覆盖/自定义、systemd timer、系统存储/网络/时间/内核参数/备份恢复；阶段 06 独占 journald、rsyslog、logrotate、audit 与日志故障；阶段 07 独占性能调优、sudo/ACL/SELinux/firewalld/加固，阶段 05 的 sysctl 只讲机制、持久化、验证和回滚，不写调优配方；阶段 08 独占 Bash/Python 自动化；阶段 09/10 独占具体企业服务和数据服务的安装、配置及应用一致性备份。

依赖图以 04.31 为阶段入口优先，额外跨阶段边必须是完成后学章节的必要直接前置且不得已有替代路径。所有直接前置必须逐条按必要性建模；在 00–05 累计图中缺失、自引用、前向引用、环和传递冗余都必须为 0。不得把 VMware 与 ECS 两条实验路径编码成共同必需前置。

就业标签必须反映初级 Linux/云运维岗位价值：启动、systemd、存储、文件系统、主机网络、时间和备份恢复主干原则上为必须学；仅当内容确属进阶且不是任何必须学章节的必要前置时才标建议学或就业后补学。实践锚点只允许按实际能力使用 CP-05、LAB-L1、LAB-L2；不得提前创建或引用项目里程碑。

来源只使用官方或一手资料并在报告中记录 2026-07-15 的实际核验结果、直接支持和不能证明边界：RHEL 9 Managing storage devices、Managing file systems、Configuring and managing logical volumes、Configuring basic system settings（systemd、time、recovery）、Configuring and managing networking；systemd 上游 bootup/systemd.unit/systemd.service/systemd.target/systemd.timer 手册；Linux Kernel admin-guide 的 sysctl 与 kernel parameters。可用 Rocky Linux 9 官方文档作发行版交叉参考，但不得用博客替代上述来源。

所有来源结论均为 source-verified，不得声称已在 Rocky Linux 9.8 实机验证；不得把 RHEL 文档中的具体工具版本、默认值、Technology Preview、硬件架构限制、云平台覆盖行为或页面更新日期写进章节 ID/标题或外推为 Rocky 9.8 事实。若来源冲突影响章节设计，立即停止交主会话裁决。

启动门禁：main、origin 精确、工作区干净；用 git log -1 --format=%H -- 本任务包查询签发提交并确认它等于启动 HEAD；固定提交 ecf207ff531bb6f7b9f37104afaeaa31c971690c、e4b69832fe7456560e423d63c5ff6742d03f9938、14725ccd220385d1efd076c3a6c61045cba61465 均在历史；看板 P1-02-05 为 ready；固定 CPython 3.11.15 下 133/133、默认和 partial 均退出 0，写前 partial 为 5 阶段、108 章、159 依赖。默认 python 不符时使用 C:\Users\beiyue\AppData\Local\Temp\cloud-ops-roadmap-uv-0.11.28\python\cpython-3.11.15-windows-x86_64-none\python.exe；不可用则停止，不安装运行时。

产品提交只含阶段 05 README，固定信息为 docs: outline stage 05 Linux system administration；报告提交只含 P1-02-05 执行报告，固定信息为 docs: report P1-02-05 outline。禁止 amend、rebase 或 reset。

必须验证：九列表结构、ID 连续、预定文件唯一、受控字段合法；22/22 类覆盖；就业标签闭包合法；CP-05/LAB-L1/LAB-L2 引用合理；阶段 04–10 边界无抢写；累计 00–05 图五类错误与传递冗余均 0；133/133、默认、partial（阶段清单精确 6，章节和依赖按结果实算）、git diff/show --check、正式非 README Markdown 0 和两个提交范围全部通过。

禁止修改其他阶段、原报告、路线、索引、校验器、任务包、看板、PRD、计划、ADR、版本基线或项目记忆；禁止创建正文、命令教程、实验、故障、面试、脚本、配置、图表、主机或云资源；不得自动调用 superpowers-zh；不得 push、PR、发布或其他远程写。

完成回复列出建议状态、两个完整提交、章节数、三类标签、type/投入分布、阶段内/跨阶段/累计边数、五类图错误与传递冗余、锚点计数、22/22 覆盖、来源与不能证明边界、自动/人工验证、异常、风险、未决和远程操作；只能建议 submitted、rework 或 blocked，不得更新看板或宣布 accepted。
```

## 门禁 1：任务信息

- **编号/名称：** P1-02-05 / 阶段 05 文件级章节清单
- **所属范围：** Phase 1 波次 2，组 A
- **状态/方式：** `ready`，串行
- **制定者：** Phase 1 主会话
- **前置：** P1-02-04 与 P1-02A-M1 均已 `accepted`

## 门禁 2：目标、价值与非范围

阶段 05 要把阶段 04 的基础主机操作提升为可规划、验证和恢复的系统管理能力，为日志、安全、自动化和企业服务提供稳定主机底座。清单必须同时支持连续学习和后续单文件任务签发。

非范围包括：正式教学解释、命令参数速查、可复制配置、实验步骤、故障处置答案、脚本、图表；日志平台、安全加固、性能调优、自动化脚本、Nginx/MySQL 等具体服务；云资源、容器与 Kubernetes；内核开发或特定参数调优配方。

## 门禁 3：二十二类覆盖合同

| 类别 | 必须形成可验收章节能力 | 边界提示 |
|---:|---|---|
| 1 | 系统管理范围、变更安全、前后状态与恢复证据 | 不复制企业变更制度正文 |
| 2 | 固件至 systemd 的启动链 | 不做内核开发 |
| 3 | target、默认目标、rescue/emergency | 不写故障案例答案 |
| 4 | unit 加载路径、覆盖与 drop-in | 不复制 unit 配置 |
| 5 | unit 依赖、排序与激活 | 必须区分三类关系 |
| 6 | 自定义 service unit 设计边界 | 身份、环境、重启与失败边界，不写完整配置 |
| 7 | systemd timer 与用户 cron 分层 | cron 基础已归阶段 04 |
| 8 | 块设备拓扑、命名与稳定标识 | 区分设备路径与持久标识 |
| 9 | 分区表和分区变更边界 | 强调破坏性与证据，不写步骤 |
| 10 | PV/VG/LV、容量变更与快照边界 | 快照不等于备份 |
| 11 | 文件系统类型、创建与能力边界 | 以 XFS/ext4 为发行版主线参考，不锁动态版本 |
| 12 | 挂载、选项、卸载和影响 | 区分临时与持久状态 |
| 13 | fstab、稳定标识、启动挂载与恢复 | 与 systemd mount 生成关系可关联 |
| 14 | 容量、inode、状态、检查与修复证据 | 性能调优留阶段 07 |
| 15 | swap 角色、形式和生命周期 | 不把 swap 当 RAM 替代品 |
| 16 | NetworkManager 设备、配置档、运行/持久态 | 不扩展高级 bond/VLAN/VPN |
| 17 | 地址、网关、路由、DNS 变更验证与回滚 | 网络原理/排障基础已在阶段 02 |
| 18 | 主机名与本地/网络名称解析 | 不重写 DNS 服务原理 |
| 19 | 时钟、RTC、时区、chrony/NTP 与同步证据 | 日志关联但日志分析留阶段 06 |
| 20 | 运行时 sysctl、持久化与启动参数边界 | 不写性能/安全调优值 |
| 21 | 备份范围、完整/增量、保留、完整性与恢复验证 | 应用一致性备份留具体服务阶段 |
| 22 | 初始化、维护窗口、重启、回滚和最终闭环 | 汇合启动/存储/网络/时间/备份分支 |

执行报告必须用章节 ID 对二十二类逐项映射，不能只写“已覆盖”。

## 门禁 4：来源矩阵

核验日固定记录为 2026-07-15。至少核验：

- RHEL 9 [Managing storage devices](https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/9/html/managing_storage_devices/index)：块设备、分区、文件系统、挂载、swap；
- RHEL 9 [Managing file systems](https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/9/html/managing_file_systems/index)：文件系统能力、备份与恢复边界；
- RHEL 9 [Configuring and managing logical volumes](https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/9/html/configuring_and_managing_logical_volumes/index)：LVM 模型与生命周期；
- RHEL 9 [Configuring basic system settings](https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/9/html/configuring_basic_system_settings/index)：systemd、时间同步、系统恢复；
- RHEL 9 [Configuring and managing networking](https://docs.redhat.com/en/documentation/red_hat_enterprise_linux/9/html/configuring_and_managing_networking/index)：NetworkManager、主机网络配置与持久态；
- systemd 上游 [bootup](https://www.freedesktop.org/software/systemd/man/latest/bootup.html)、[systemd.unit](https://www.freedesktop.org/software/systemd/man/latest/systemd.unit.html)、[systemd.service](https://www.freedesktop.org/software/systemd/man/latest/systemd.service.html)、[systemd.target](https://www.freedesktop.org/software/systemd/man/latest/systemd.target.html)、[systemd.timer](https://www.freedesktop.org/software/systemd/man/latest/systemd.timer.html)：启动、unit、service、target 与 timer 语义；若网页抓取受限，可用 systemd 官方源码仓库中对应 man 源文件交叉核验；
- Linux Kernel [sysctl admin guide](https://docs.kernel.org/admin-guide/sysctl/index.html) 与 [kernel parameters](https://docs.kernel.org/admin-guide/kernel-parameters.html)：运行时与启动参数边界。

来源不能证明 Rocky Linux 9.8 已安装的具体包版本、现场默认配置、硬件/云覆盖行为、命令执行结果或未来正文正确性。ReaR、XFS 恢复、NetworkManager keyfile 等发行版细节如进入标题必须保持版本中立；动态限制只写报告来源边界。

## 门禁 5：依赖、标签与锚点

- 以 `04.31` 作为阶段共同入口优先；额外跨阶段边逐条证明必要性。
- 章节图必须是必要直接前置的传递约简，不以“相关”代替“前置”。
- 任一必须学章节不得依赖就业后补学；必须学依赖建议学时，报告说明该建议学为何进入就业路线前置闭包，否则调整标签或依赖。
- `CP-05` 用于阶段能力检查；`LAB-L1` 用于单点基础验证，`LAB-L2` 用于跨主题综合验证。概念章节可只挂 `CP-05`。
- 不引用 `PRJ-*`；项目双向映射留 Phase 1 后续波次。

## 门禁 6：允许文件与提交

```text
知识库/05-Linux系统管理/README.md
项目管理/执行报告/P1-02-05-执行报告.md
```

| 顺序 | 提交范围 | 固定提交信息 |
|---:|---|---|
| 1 | 阶段 05 README | `docs: outline stage 05 Linux system administration` |
| 2 | 新执行报告 | `docs: report P1-02-05 outline` |

## 门禁 7：验证、停止与回复

```powershell
$py = 'C:\Users\beiyue\AppData\Local\Temp\cloud-ops-roadmap-uv-0.11.28\python\cpython-3.11.15-windows-x86_64-none\python.exe'
& $py --version
& $py -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py'
& $py -B '脚本资源/项目校验/validate_repository.py' .
& $py -B '脚本资源/项目校验/validate_repository.py' . --outline-gate partial
git diff --check
git status --short
```

另用独立只读图审计解析 00–05，逐边移除后搜索替代路径；预期 partial 阶段清单 6，缺失/自引用/前向引用/环/传递冗余均 0。正式非 README Markdown 必须为 0。

启动基线、来源、范围或验证冲突，出现阶段抢写、必须学依赖就业后补学、图错误、无法证明直接边必要性、需要白名单外写入或外部操作时立即停止。禁止 amend、rebase、reset、push、PR、发布或安全控制变更。

完成回复必须列建议状态、完整提交、章节/标签/type/投入/锚点/边统计、22/22 映射、来源矩阵、全部验证、异常/风险/未决和远程操作，不得更新看板或宣布 `accepted`。
