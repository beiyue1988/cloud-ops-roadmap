# P0-04B-R2 仓库校验器异常边界独立复审任务包

## 任务信息

- **任务编号：** P0-04B-R2
- **任务名称：** 仓库校验器异常边界独立复审
- **所属 Phase：** Phase 0
- **状态：** `ready`
- **执行方式：** 未参与 P0-04-R2 的独立新会话，只读复审
- **制定者：** 主会话
- **R1 修订提交：** `2efbe21f92a647beecf93dda978855614b2ce827`
- **R1 独立复审提交：** `42e4f510c8fbfa6fcc5e4e013cc79a09c097168d`
- **R2 修订提交：** `5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c`
- **R2 执行报告提交：** `44b7fed394f5054ff0b4f76d8c9ef2eaf0b1f2d7`
- **R1 独立复审报告：** [P0-04B-R1 独立复审报告](../验收记录/P0-04B-R1-独立复审报告.md)
- **R2 执行报告：** [P0-04-R2 执行报告](../执行报告/P0-04-R2-执行报告.md)
- **R2 返工任务包：** [P0-04-R2 收窄仓库校验器异常边界](P0-04-R2-收窄仓库校验器异常边界.md)

> 本任务已于 2026-07-11 由主会话解锁。P0-04-R2 已提交，主会话完成提交范围、44 项回归、异常正反探针和 README 固定 blob 初审；现在可以交给一个未参与 P0-04-R2 的新会话执行。该解锁不代表 P0-04 或 P0-04-R2 已被最终接受。

## 解锁记录

### 固定提交链

| 对象 | 完整哈希 | 父提交 | 精确范围 |
|---|---|---|---|
| R2 修订 | `5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c` | `32fdbebb376cfe7ffbcde99629e0cccfd2bdf36d` | 实现与测试两个文件 |
| R2 执行报告 | `44b7fed394f5054ff0b4f76d8c9ef2eaf0b1f2d7` | `5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c` | 仅 `项目管理/执行报告/P0-04-R2-执行报告.md` |

### 固定工具 blob

| 文件 | 当前 blob |
|---|---|
| `脚本资源/项目校验/validate_repository.py` | `74b6c75896117f5680195a72dd010767d345652b` |
| `脚本资源/项目校验/tests/test_validate_repository.py` | `1f429b89d041a2788130f2289f55fb3c0dfe51a5` |
| `脚本资源/项目校验/README.md` | `7476e64b6fd4f5ba3bf0a009e3d267c4c0ad87de` |

### 主会话初审摘要

- R2 修订提交精确包含实现与测试，报告提交精确包含一份 R2 报告；两个提交均通过 `git show --check`，当前实现与测试相对 R2 修订提交无漂移。
- Python 3.11.15 下完整测试 `44/44`；R2 父提交原 40 项零删除，新增 4 项；批准行为映射 `33/33`。
- 临时 `PYTHONPYCACHEPREFIX` 编译退出 0且缓存已清理；当前仓库 CLI 退出 0；含 R2 报告时摘要为 Markdown 116、知识章节 0、本地链接 334、错误 0；双运行退出码 `0/0` 且合并输出逐字符一致。
- 七个公开函数 `7/7`，第三方依赖 0，bare `except` 与 `except Exception` 命中 0；捕获 `(OSError, ValueError)` 的根发现 `try` 中没有内容规则或 `_aggregate_repository_checked()` 调用。
- 独立正反探针 `11/11`：非路径 `ValueError/OSError/RuntimeError` 从 `validate_repository()` 和 `main()` 六个场景原样逃逸；非法根路径、根 `OSError` 在两个公开入口四个场景均转换为脱敏 `IO003`；缺失根目录保持 `IO001`。
- 在内存加载 R2 父提交旧实现后，四个非路径 `ValueError/OSError` 场景仍全部被错误吞成 `IO003`，证明新增测试能识别原缺陷。
- README blob 保持 `7476e64b6fd4f5ba3bf0a009e3d267c4c0ad87de`；工具实现 blob 为 `74b6c75896117f5680195a72dd010767d345652b`，测试 blob 为 `1f429b89d041a2788130f2289f55fb3c0dfe51a5`。
- 工作区在初审结束前干净；未执行 push、Pull Request、网站发布或 GitHub 设置修改。

**初审边界：** 以上只说明 R2 已达到进入独立复审的条件，不替代 P0-04B-R2 的独立判断，也不构成 `accepted`。

## 新会话启动指令

把下面整段原样发送给一个未参与 P0-04-R2 的新 Codex 会话：

```text
请执行 cloud-ops-roadmap 的 P0-04B-R2“仓库校验器异常边界独立复审”任务。

你的身份是独立复审会话，不是 P0-04/P0-04-R1/P0-04-R2 执行者、项目领导者、规格制定者、修复者或最终验收者。你必须没有参与提交 5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c 的实现、测试或 R2 执行报告编写；若参与过 P0-04-R2，立即停止。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap。你只负责 P0-04B-R2，不得修复工具，不得执行 P0-05、P0-06 或其他任务，不得委派，也不得与其他写任务并行。

本任务在当前 main 分支和固定工作目录串行执行，不得另建分支或 Git worktree。开始时按技能规则选择并完整阅读适用技能；提交或声称完成前必须使用 verification-before-completion 取得新鲜证据。出现测试失败或异常行为时，先使用 systematic-debugging 区分探针、环境与交付缺陷，不得把审查脚本缺陷直接判成交付缺陷。若技能一般建议与运行时更高层指令、根 AGENTS.md、本任务包或批准规格冲突，以更高层指令、根 AGENTS.md、本任务包和批准规格为准。

采取任何写操作前，必须完整阅读：
1. 根目录 AGENTS.md；
2. 根目录 CODEX-PRD.md；
3. 根目录 AI-CODEX-RULES.md；
4. 项目管理/任务包/P0-04-仓库校验工具.md；
5. 项目管理/任务包/P0-04-R1-加固仓库校验器边界.md；
6. 项目管理/任务包/P0-04B-R1-仓库校验器边界独立复审.md；
7. 项目管理/任务包/P0-04-R2-收窄仓库校验器异常边界.md；
8. 项目管理/任务包/P0-04B-R2-仓库校验器异常边界独立复审.md 全文；
9. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-设计说明.md；
10. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-实施计划.md；
11. 项目管理/执行报告/P0-04-R1-执行报告.md；
12. 项目管理/验收记录/P0-04B-R1-独立复审报告.md；
13. 项目管理/执行报告/P0-04-R2-执行报告.md；
14. 项目管理/任务看板.md；
15. 三个当前工具文件全文。

唯一目标：独立确认 P0-04-R2 是否把 `IO003` 捕获精确收窄到批准的根路径规范化、检查和 Markdown 发现/排序边界，使非路径 `ValueError/OSError` 从公开 API 与 CLI 入口原样逃逸，并确认原 40 项、33 个行为、M-01/M-02/S-01 和其他规则没有回归。不得信任 R2 报告或主会话初审的成功声明代替复验。

本任务原则上只读。唯一允许创建的文件是 项目管理/验收记录/P0-04B-R2-独立复审报告.md。禁止修改三个工具文件、README、任何执行报告、历史审查报告、任务包、任务看板、设计、计划、PRD、AI 规则、模板、版本基线或其他文件。报告必须使用 apply_patch 创建。发现缺陷只记录证据、级别和建议，不得顺手修复。

启动时必须实际运行：
git status --short --branch
git branch --show-current
git remote get-url origin
python --version
Select-String -LiteralPath '项目管理\任务看板.md' -Pattern '^\| P0-04 .*`rework`','^\| P0-04-R1 .*`rework`','^\| P0-04B-R1 .*`accepted`','^\| P0-04-R2 .*`submitted`','^\| P0-04B-R2 .*`ready`'
git show -s --format='%H%n%P%n%s' 5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c
git show -s --format='%H%n%P%n%s' 44b7fed394f5054ff0b4f76d8c9ef2eaf0b1f2d7
git -c core.quotepath=false diff-tree --no-commit-id --name-only -r 5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c
git -c core.quotepath=false diff-tree --no-commit-id --name-only -r 44b7fed394f5054ff0b4f76d8c9ef2eaf0b1f2d7
git hash-object '脚本资源/项目校验/validate_repository.py'
git hash-object '脚本资源/项目校验/tests/test_validate_repository.py'
git hash-object '脚本资源/项目校验/README.md'
git diff --exit-code 5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c HEAD -- '脚本资源/项目校验/validate_repository.py' '脚本资源/项目校验/tests/test_validate_repository.py'

预期：工作区干净、分支 main、远程为 https://github.com/beiyue1988/cloud-ops-roadmap.git、Python 3.11.15、五项状态正确；R2 修订父提交为 32fdbebb376cfe7ffbcde99629e0cccfd2bdf36d，R2 报告父提交为修订提交；修订提交只含实现与测试，报告提交只含一份 R2 报告；三个 blob 依次为 74b6c75896117f5680195a72dd010767d345652b、1f429b89d041a2788130f2289f55fb3c0dfe51a5、7476e64b6fd4f5ba3bf0a009e3d267c4c0ad87de，实现与测试相对 R2 修订提交无漂移。任一条件不符立即停止，不得猜测、修复或重建被审对象。

你必须完整执行本任务包“必须独立复验的自动检查”和“必须完成的人工审查”，不得只复跑四个新增测试。至少取得以下新鲜证据：
1. 完整 unittest 44/44，R2 父提交原 40 项零删除、新增 4 项、批准行为 33/33；
2. 临时缓存目录中的 py_compile 退出 0且清理完成；
3. 当前仓库 CLI 退出 0，两次合并输出和退出码完全一致；
4. 七个公开函数 7/7、第三方依赖 0、bare except/except Exception 0；
5. 在系统临时目录用 R2 父提交旧实现加当前测试重建红灯，确认恰好四个非路径异常测试失败、原 40 项通过；
6. 独立正向探针证明非法根路径和根 `OSError` 在 `validate_repository()` 与 `main()` 四个场景均转成脱敏 `IO003`；缺失根目录为 `IO001`，非法 UTF-8 为 `IO002`；
7. 独立负向探针证明从内容规则注入的 `ValueError/OSError/RuntimeError` 在两个公开入口六个场景均原类型、原唯一标记逃逸，stdout/stderr 没有伪造 `IO003`；
8. AST 与人工边界证明捕获 `(OSError, ValueError)` 的根发现 `try` 不包含 `_aggregate_repository_checked`、`validate_*`、`_front_matter_diagnostics` 或内容聚合，`main()` 不重复捕获聚合调用；
9. M-01/M-02/S-01、路径、围栏、中文、Secret 脱敏和确定性回归；
10. README 固定 blob 与 R2 报告/历史纠正一致性。

独立探针必须使用 python -B - 的内联脚本和 tempfile/unittest.mock，或等价的系统临时目录方案；不得在仓库创建临时脚本、缓存、快照或输出文件。期望必须基于公开返回值、异常传播和渲染结果，不能调用被测私有函数生成期望。若临时审查脚本自身失败，先记录原始错误并区分探针、环境和产品根因。

问题分为阻塞、必须修改、建议修改和通过项。建议结论只能是 passed、passed-with-notes 或 changes-required：存在任何阻塞或必须修改时必须是 changes-required；只有非阻塞建议时可为 passed-with-notes；没有问题才是 passed。你无权宣布 P0-04、P0-04-R2 或 P0-04B-R2 accepted，也不得解锁 P0-05。

完成复审后，只创建并提交 项目管理/验收记录/P0-04B-R2-独立复审报告.md，提交信息固定为 docs: review repository validator exception boundary。报告必须包含独立性声明、固定提交和 blob、范围与无漂移、重建红灯、完整绿灯、编译、CLI、双运行、接口、依赖、33 项映射、路径正例、非路径负例、异常捕获边界、原缺陷回归、README 与 R2 报告一致性、四级问题、风险、未决问题、建议结论和远程操作声明。禁止 amend、rebase、reset、push、创建 Pull Request、发布网站或修改 GitHub 设置。

结束前再次运行完整 unittest、真实仓库 CLI、git show --check、git diff --check 和 git status --short。最终工作区必须干净。最终回复必须给出复审报告路径、复审提交完整哈希、R2 修订和报告提交、重建红灯、完整绿灯、原 40 项、33/33、路径/非路径异常结果、四级问题数量、建议结论、风险、未决问题和远程操作确认。只能建议结论，不能宣布 accepted。

遇到以下任一情况必须立即停止：当前会话参与过 P0-04-R2；任务不是 ready；工作区不干净；分支、远程、Python、固定提交、父链、范围或 blob 不符；实现/测试相对 R2 修订提交漂移；README blob 变化；需要写入唯一报告外文件；无法在只读边界复验；发现真实疑似凭据；无法保证临时数据清理；同一外部阻塞连续出现三次。
```

## 唯一目标与学习者价值

- **唯一目标：** 独立确认 P0-04-R2 把 `IO003` 捕获精确收窄到批准的根路径/文件发现边界，使非路径 `ValueError/OSError` 从公开 API 与 CLI 入口逃逸，并且没有破坏原 40 项、33 个行为或其他规则。
- **学习者价值：** 防止校验器把自身程序缺陷伪装成根路径故障，保证后续内容门禁的错误信息真实、可定位且可维护。

## 唯一写入白名单

```text
项目管理/验收记录/P0-04B-R2-独立复审报告.md
```

复审会话不得修改工具、README、执行报告、历史审查报告、任务包或任务看板。

## 固定复审范围

复审必须独立验证：

1. 根路径 `ValueError/OSError` 仍稳定转换为脱敏 `IO003`；根不存在仍为 `IO001`，单文件解码错误仍为 `IO002`。
2. 非路径 `validate_placeholders` 注入的 `ValueError/OSError` 从 `validate_repository()` 和 `main()` 原样逃逸，唯一标记不丢失；`RuntimeError` 对照同样逃逸。
3. 聚合级异常捕获没有包住 `_aggregate_repository_checked`、内容规则或最终聚合；`main()` 没有重复吞掉聚合层异常。
4. R2 父提交原 40 个测试全部保留，四个负面对照具有可重建的正确红灯，当前 44/44，33/33 行为不变。
5. 七个公开函数、标准库零依赖、编译、真实仓库、双运行、M-01/M-02/S-01、路径安全、围栏、中文与脱敏无回归。
6. README 保持固定 blob，R2 报告准确纠正 R1 报告且没有修改历史快照。

不得只复跑四个新增测试。必须构造独立临时探针，并使用 R2 父提交旧实现加当前测试重建红灯；所有临时数据必须安全清理。

## 必须独立复验的自动检查

### 1. 固定对象、范围与无漂移

运行启动指令列出的 Git、状态、版本、提交、范围、blob 和差异命令；另外运行：

```powershell
git show --check --oneline 5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c
git show --check --oneline 44b7fed394f5054ff0b4f76d8c9ef2eaf0b1f2d7
```

### 2. 完整测试、红灯与编译

```powershell
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
```

预期 `Ran 44 tests`、`OK`、退出 0。用 AST 比较 R2 父提交测试与当前测试，要求父提交 40、当前 44、删除 0、新增 4；从 P0-04 任务包门禁 8 读取 33 个固定测试名并确认 `33/33`。

在系统临时目录使用 R2 父提交 `32fdbebb376cfe7ffbcde99629e0cccfd2bdf36d` 的旧实现和当前测试运行相同命令，预期 `Ran 44 tests`、退出非 0且只失败四个新增非路径异常测试。清理前必须确认临时绝对路径位于系统临时根。

编译必须设置唯一系统临时 `PYTHONPYCACHEPREFIX`，运行两文件 `python -m py_compile`，随后清理本次缓存并确认仓库缓存为 0。

### 3. CLI、双运行、接口与依赖

```powershell
python -B '脚本资源/项目校验/validate_repository.py' .
```

连续运行两次并分别捕获合并输出与退出码，要求退出码 `0/0` 且输出逐字符相同。用 AST 检查七个公开函数、标准库导入、第三方依赖 0、bare `except` 0、`except Exception` 0。

### 4. 异常边界正反探针

使用不落盘的 `python -B -` 内联程序：

- 正例：`Path(chr(0))` 与 mock `Path.resolve` 的 `OSError`，分别通过 `validate_repository()` 和 `main()` 观察四个 `IO003` 场景；断言返回码、stdout/stderr、无 traceback 和脱敏；
- 负例：从 `validate_placeholders` 分别注入含唯一标记的 `ValueError/OSError/RuntimeError`，分别通过两个公开入口观察六个异常原样逃逸；
- 基础 IO：根缺失为 `IO001`，非法 UTF-8 为 `IO002`；
- 静态边界：聚合根发现 `try` 中不得出现内容规则和 `_aggregate_repository_checked`，`main()` 不得包围聚合调用。

### 5. 原规则、README 与报告

独立复验 M-01 非法/合法标量、M-02 批准路径异常、S-01 第 2/10 行排序，以及路径逃逸、大小写、围栏、中文和运行时虚构 Secret 脱敏。README blob 必须精确为 `7476e64b6fd4f5ba3bf0a009e3d267c4c0ad87de`。逐项核对 R2 报告的哈希、测试数、红绿证据、异常边界、历史纠正、风险和远程操作。

最后运行：

```powershell
git diff --check
git -c core.quotepath=false status --short
```

## 必须完成的人工审查

逐项给出“通过/不通过/需说明”和直接证据：

1. 路径发现与内容聚合是否真正分离；
2. 捕获范围是否只覆盖批准的根路径操作；
3. `validate_repository()` 是否保留路径 `IO003` 且暴露非路径异常；
4. `main()` 是否保留路径 `IO003` 且不重复吞掉聚合异常；
5. `IO001/IO002/IO003` 的边界是否仍真实；
6. 四个新测试是否观察公开接口且不依赖私有函数；
7. 旧实现加当前测试是否产生精确红灯；
8. 原 40 项、33 个行为和 M-01/M-02/S-01 是否无回归；
9. README 是否保持不变且与实现一致；
10. R2 报告是否准确纠正 R1 报告并保持历史不可变；
11. 两个 R2 提交是否满足范围、顺序和不可变性；
12. 是否存在新安全风险、敏感信息、范围漂移或未决问题。

## 问题分级与建议结论

- **阻塞：** 固定对象不明、范围漂移、工具无法复验、安全或敏感信息风险；
- **必须修改：** 任一非路径异常仍被吞掉、批准路径异常回归、捕获范围仍包围内容聚合、原行为回归或报告失真；
- **建议修改：** 不阻塞异常合同，但可改善维护性或诊断体验；
- **通过项：** 有独立命令、临时场景或文件证据支持。

建议结论只允许 `passed`、`passed-with-notes`、`changes-required`。复审会话无权宣布 `accepted`。

## 复审报告要求

`项目管理/验收记录/P0-04B-R2-独立复审报告.md` 必须包含：

1. 独立性声明；
2. R1、R1 独立复审、R2 修订和 R2 报告完整哈希及固定 blob；
3. 提交范围、父链、README 不变与工具无漂移证据；
4. 重建红灯、当前绿灯、编译、CLI、双运行、接口、依赖和 33 行为结果；
5. 路径异常正例、非路径异常负面对照和异常捕获 AST/人工边界；
6. 原 M-01/M-02/S-01 与其他规则回归；
7. R2 报告和历史纠正一致性；
8. 四级问题、风险、未决问题和建议结论；
9. 未执行远程写操作及最终验收权属于主会话的声明。

## 提交与远程限制

只能提交独立复审报告，提交信息固定为：

```text
docs: review repository validator exception boundary
```

禁止修改或重建任何被审提交，禁止 amend、rebase、reset、push、创建 PR、发布网站或修改 GitHub 设置。

## 停止条件

- 当前任务不是 `ready`，或任务包缺少真实 R2 哈希、blob 和完整启动指令；
- 会话参与过 P0-04-R2；
- 状态、工作区、分支、远程、Python、固定提交、父链或工具 blob 不符；
- README blob 变化或需要修改唯一报告外文件；
- 被审实现/测试发生漂移，或无法在只读边界完成复验；
- 发现真实疑似凭据，或无法安全清理系统临时数据；
- 同一外部阻塞连续出现三次。

## 完成后的回复

```text
P0-04B-R2 独立复审已提交。
复审报告：项目管理/验收记录/P0-04B-R2-独立复审报告.md
复审提交：填写 docs: review repository validator exception boundary 的完整哈希
R2 修订提交：5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c
R2 报告提交：44b7fed394f5054ff0b4f76d8c9ef2eaf0b1f2d7
重建红灯：填写测试数、退出码和目标失败摘要
完整绿灯：填写通过数/总数和退出码
原测试：填写保留数/40
行为映射：填写覆盖数/33
路径异常：填写通过数/总数
非路径异常：填写通过数/总数
问题分级：填写阻塞/必须修改/建议修改/通过项数量
建议结论：passed、passed-with-notes 或 changes-required
风险：填写“无”或具体风险
未决问题：填写“无”或具体问题
远程操作：确认未 push、未创建 PR、未发布网站、未修改 GitHub 设置
最终验收：未宣布 accepted，等待主会话裁决
```
