# P0-02A-R1 补全规则优先级任务包

## 任务信息

- **任务编号：** P0-02A-R1
- **所属阶段：** Phase 0
- **状态：** ready
- **执行方式：** 独立新会话，最小范围返工
- **前置任务：** P0-02A `submitted`、P0-02B 独立审查完成、主会话已作返工裁决
- **制定者：** 主会话
- **被修订规则提交：** `bd23ca178eba99fa207a483af9ba9e1386c596e0`
- **P0-02A 报告提交：** `0c2b3761f0fb6737d963599874a4f7c971983537`
- **P0-02B 审查提交：** `a4c594dcda0c9006cf83448a40bf29c5b13ad225`
- **独立审查报告：** [P0-02B 独立审查报告](../验收记录/P0-02B-独立审查报告.md)
- **实施计划：** [AI-CODEX-RULES 规则基线实施计划](../执行计划/2026-07-11-AI-CODEX-RULES-实施计划.md)

## 主会话裁决

P0-02B 给出 `passed-with-notes`，其中 S-01 建议在规则正文显式补全用户当前明确指令的优先级。主会话对照已批准实施计划第 1.1 节后裁决：

1. 实施计划使用“冲突优先级必须是”，不是可选建议；
2. 当前规则完整列出项目文件层级，却遗漏了项目文件之外的当前用户明确指令；
3. 该缺口不构成现有治理倒置，但不满足已批准规则的自包含性；
4. 为避免把用户指令错误表述为高于系统或开发者指令，修订必须同时说明运行时更高层指令；
5. 此项提升为必须修复，但返工只能增加一条明确规则，不得借机改写其他内容。

## 新会话启动指令

把下面整段原样发送给新的 Codex 会话：

```text
请执行 cloud-ops-roadmap 的 P0-02A-R1“补全规则优先级”最小返工任务。

你的身份是独立返工执行会话，不是项目领导者、P0-02B 审查者或最终验收者。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap。你只负责 P0-02A-R1，不得执行 P0-02B-R1、P0-03 或其他任务，不得委派给其他会话。开始前使用 executing-plans 技能；提交前使用 verification-before-completion 技能。本任务经用户批准的实施计划和主会话审查裁决授权，明确在当前 main 分支与固定工作目录串行执行，不创建分支或 Git worktree。

写操作前完整阅读：
1. 根 AGENTS.md；
2. 根 CODEX-PRD.md；
3. 根 AI-CODEX-RULES.md；
4. 项目管理/任务包/P0-02A-R1-补全规则优先级.md 全文；
5. 项目管理/执行计划/2026-07-11-AI-CODEX-RULES-实施计划.md；
6. 项目管理/验收记录/P0-02B-独立审查报告.md；
7. 项目管理/执行报告/P0-02A-执行报告.md；
8. 项目管理/任务看板.md。

唯一目标：只在 AI-CODEX-RULES.md 的“## 2. 规则边界、权威分层与冲突停止”标题之后、现有 AGENTS.md 条目之前，新增这一条完整规则：

- 运行时系统与开发者指令优先于项目文件；在不与这些更高层指令冲突时，用户当前明确指令优先于项目内文件。

除新增上述一行外，不得增加空行，也不得改写、重排、润色或格式化 AI-CODEX-RULES.md 的其他内容。不得修改 AGENTS.md、CODEX-PRD.md、README.md、patterns、任务看板、任务包、独立审查报告或其他文件。

唯一写入白名单：
1. AI-CODEX-RULES.md；
2. 项目管理/执行报告/P0-02A-R1-执行报告.md（规则提交后创建）。

启动时检查工作区干净、分支 main、远程正确、任务看板中 P0-02A 为 rework、P0-02A-R1 为 ready，并核对当前 AI-CODEX-RULES.md 的 Git blob 为 5c13fd95ff60e427476af1e4ff95266ea07bbea3。任一条件不符立即停止。

修改后必须证明：规则相对 bd23ca178eba99fa207a483af9ba9e1386c596e0 只有 AI-CODEX-RULES.md 一行新增；新增句精确命中一次；规则为 227 行；14 个二级主题和顺序不变；17 个必需词仍齐全；六个候选污染字符串、敏感信息和占位符仍为 0；15 个相对链接仍全部有效；git diff --check 通过。必须人工确认新增句没有把用户指令放到系统或开发者指令之上，也没有改变项目内 AGENTS → PRD/ADR/版本基线 → AI 规则 → 模板 → 任务包的层级。

验证通过后，只提交 AI-CODEX-RULES.md，提交信息必须是 docs: clarify AI rules instruction priority，不得 amend、rebase 或重建既有提交。记录完整提交哈希。随后创建 项目管理/执行报告/P0-02A-R1-执行报告.md，只提交该报告，提交信息必须是 docs: add P0-02A-R1 execution report。报告正文不记录自身提交哈希，最终回复提供两个新提交的完整哈希。

不得 push、创建 PR、发布网站或修改 GitHub 设置。最终工作区必须干净。最终回复给出返工报告路径、修订提交哈希、报告提交哈希、实际差异行数、回归验证结果、异常、风险、未决问题、远程操作确认和建议状态 submitted。你无权宣布 accepted 或启动 P0-02B-R1。

遇到以下任一情况必须停止：当前 blob 不符；目标句已存在；需要修改白名单外文件；实际差异超过规定的一行新增；验证失败且无法仅通过撤销本次未提交变更恢复；发现敏感信息；同一外部阻塞连续三次。
```

## 唯一写入白名单

```text
AI-CODEX-RULES.md
项目管理/执行报告/P0-02A-R1-执行报告.md
```

## 精确修改要求

在 `AI-CODEX-RULES.md` 第 2 节标题之后、现有 `AGENTS.md` 条目之前增加：

```markdown
- 运行时系统与开发者指令优先于项目文件；在不与这些更高层指令冲突时，用户当前明确指令优先于项目内文件。
```

除该行外，规则文件不得发生其他变化。

## 必须运行的验证

### 启动检查

```powershell
git status --short --branch
git branch --show-current
git remote get-url origin
Select-String -LiteralPath '项目管理\任务看板.md' -Pattern '^\| P0-02A .*`rework`','^\| P0-02A-R1 .*`ready`'
$blob = git hash-object 'AI-CODEX-RULES.md'
$blob
if ($blob -ne '5c13fd95ff60e427476af1e4ff95266ea07bbea3') { exit 1 }
$existing = Select-String -LiteralPath 'AI-CODEX-RULES.md' -SimpleMatch -Pattern '运行时系统与开发者指令优先于项目文件'
if ($existing) { $existing; exit 1 }
```

### 精确差异检查

```powershell
$changed = @(git -c core.quotepath=false status --short)
$changed
if ($changed.Count -ne 1 -or $changed[0].Substring(3).Replace('\','/') -ne 'AI-CODEX-RULES.md') { exit 1 }

$numstat = @(git diff --numstat -- 'AI-CODEX-RULES.md')
$numstat
if ($numstat.Count -ne 1 -or $numstat[0] -notmatch '^1\s+0\s+AI-CODEX-RULES\.md$') { exit 1 }

$exact = @(Select-String -LiteralPath 'AI-CODEX-RULES.md' -SimpleMatch -Pattern '- 运行时系统与开发者指令优先于项目文件；在不与这些更高层指令冲突时，用户当前明确指令优先于项目内文件。')
if ($exact.Count -ne 1) { exit 1 }
```

### 回归检查

```powershell
$lineCount = (Get-Content -LiteralPath 'AI-CODEX-RULES.md' -Encoding UTF8).Count
if ($lineCount -ne 227) { Write-Error "规则行数不是 227：$lineCount"; exit 1 }

$headings = @(Select-String -LiteralPath 'AI-CODEX-RULES.md' -Pattern '^## \d+\.' -Encoding UTF8)
if ($headings.Count -ne 14) { exit 1 }

$required = @(
  '适用范围','规则边界','零基础','自适应','背景','历史','Mermaid','Draw.io',
  '来源','时效','不得虚构','停止条件','安全','成本','敏感信息','证据','人工审查'
)
foreach ($term in $required) {
  if (-not (Select-String -LiteralPath 'AI-CODEX-RULES.md' -SimpleMatch -Pattern $term)) { exit 1 }
}

$forbidden = @(
  'Kubernetes 初始教学基线：1.35.x',
  'cloud-ops-roadmap-Codex-PRD.md',
  '技术文档/',
  '发现版本冲突时先修正基线',
  '项目 0：',
  '### Phase 6：AI Infra 与 AIOps'
)
foreach ($term in $forbidden) {
  if (Select-String -LiteralPath 'AI-CODEX-RULES.md' -SimpleMatch -Pattern $term) { exit 1 }
}

$secretHits = rg -n --hidden -g '!\.git/**' '(AKIA[0-9A-Z]{16}|BEGIN (RSA |OPENSSH )?PRIVATE KEY|access[_-]?key\s*[:=]\s*[^<\s]+|secret[_-]?key\s*[:=]\s*[^<\s]+)' AI-CODEX-RULES.md
if ($LASTEXITCODE -eq 0) { $secretHits; exit 1 }
if ($LASTEXITCODE -ne 1) { exit $LASTEXITCODE }

$placeholderHits = rg -n 'TODO|TBD|待补充|稍后完善|这里填写' AI-CODEX-RULES.md
if ($LASTEXITCODE -eq 0) { $placeholderHits; exit 1 }
if ($LASTEXITCODE -ne 1) { exit $LASTEXITCODE }

git diff --check
```

还必须复跑 `AI-CODEX-RULES.md` 的相对链接检查，预期 2 个规则内相对链接有效；并核对此前五个规则/治理文件合计 15 个相对链接仍失效 0。链接检查只能使用只读内联命令，不保存脚本。

## 提交要求

1. 规则修订提交：`docs: clarify AI rules instruction priority`，只能包含 `AI-CODEX-RULES.md`；
2. 返工报告提交：`docs: add P0-02A-R1 execution report`，只能包含返工报告；
3. 不得 amend、rebase、reset 或重建 P0-02A/P0-02B 历史；
4. 不得 push。

## 返工报告要求

`项目管理/执行报告/P0-02A-R1-执行报告.md` 必须包含：

- 返工原因与 S-01；
- 基线规则、报告、审查三个完整哈希；
- 精确新增句及行号；
- `1 insertion / 0 deletion / 1 file` 的差异证据；
- 行数、主题、必需词、污染、链接、敏感信息、占位符和空白检查；
- 人工优先级判断；
- 修订提交完整哈希；
- 异常、风险和未决问题；
- 远程操作确认；
- 建议状态 `submitted`。

## 验收标准

1. 只新增指定的一条优先级规则；
2. 运行时系统/开发者指令高于用户指令，用户当前明确指令高于项目文件；
3. 项目文件内部既有层级未改变；
4. 规则 227 行、14 个主题；
5. 其他自动门禁全部保持通过；
6. 两个提交范围准确；
7. 工作区干净，未执行远程写操作。

## 完成后的回复

```text
P0-02A-R1 已提交审查。
返工报告：项目管理/执行报告/P0-02A-R1-执行报告.md
修订提交：填写 docs: clarify AI rules instruction priority 的完整哈希
报告提交：填写 docs: add P0-02A-R1 execution report 的完整哈希
差异：填写实际新增/删除行和文件数
回归验证：填写实际通过项数/总项数，以及行数、主题、污染、失效链接、敏感信息和占位符结果
异常：填写“无”或具体异常
风险：填写“无”或具体风险
未决问题：填写“无”或具体问题
远程操作：确认未 push、未创建 PR、未发布网站、未修改 GitHub 设置
建议状态：submitted
```
