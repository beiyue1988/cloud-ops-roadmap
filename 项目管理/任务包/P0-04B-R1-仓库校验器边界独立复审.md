# P0-04B-R1 仓库校验器边界独立复审任务包

## 任务信息

- **任务编号：** P0-04B-R1
- **任务名称：** 仓库校验器边界独立复审
- **所属 Phase：** Phase 0
- **状态：** `ready`
- **执行方式：** 未参与 P0-04-R1 的独立新会话，只读复审
- **制定者：** 主会话
- **原实现提交：** `08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3`
- **原报告提交：** `d2952cf3ea5216169deaeaa9e320cf3f6d409363`
- **原独立审查提交：** `0bdc8410dec6833c88bdf7afdbf77b29937bdcde`
- **R1 修订提交：** `2efbe21f92a647beecf93dda978855614b2ce827`
- **R1 执行报告提交：** `8dd4c9c06bf138626ab27e6edba7d7ab2fc73447`
- **原独立审查报告：** [P0-04B 独立审查报告](../验收记录/P0-04B-独立审查报告.md)
- **R1 执行报告：** [P0-04-R1 执行报告](../执行报告/P0-04-R1-执行报告.md)
- **返工任务包：** [P0-04-R1 加固仓库校验器边界](P0-04-R1-加固仓库校验器边界.md)

> 本任务已于 2026-07-11 由主会话解锁。P0-04-R1 已提交，主会话完成提交边界、完整回归和缺陷级独立探针初审；现在可以交给一个未参与 P0-04-R1 的新会话执行。该解锁不代表 P0-04 或 P0-04-R1 已被最终接受。

## 解锁记录

### 固定提交链

| 对象 | 完整哈希 | 父提交 | 精确范围 |
|---|---|---|---|
| R1 修订 | `2efbe21f92a647beecf93dda978855614b2ce827` | `bdf8ff351d498b9eddea698ca88441fbec765386` | 三个工具文件 |
| R1 执行报告 | `8dd4c9c06bf138626ab27e6edba7d7ab2fc73447` | `2efbe21f92a647beecf93dda978855614b2ce827` | 仅 `项目管理/执行报告/P0-04-R1-执行报告.md` |

### 固定工具 blob

| 文件 | 当前 blob |
|---|---|
| `脚本资源/项目校验/validate_repository.py` | `ee4b31198f099aecadb8dc8f9d03e56565ce188f` |
| `脚本资源/项目校验/tests/test_validate_repository.py` | `d80de56ac065d7ed6e3b2e88ec9aac6fff75efe3` |
| `脚本资源/项目校验/README.md` | `7476e64b6fd4f5ba3bf0a009e3d267c4c0ad87de` |

### 主会话初审摘要

- 修订提交精确包含三个工具文件，报告提交精确包含一份 R1 报告；两个提交均通过 `git show --check`，当前工具相对修订提交无漂移。
- Python 3.11.15 下完整测试 `40/40` 通过；原 34 个测试零删除，新增 6 个；批准行为映射 `33/33`。
- 临时 `PYTHONPYCACHEPREFIX` 编译退出 0且缓存已清理；真实仓库 CLI 退出 0；当前仓库含 R1 报告时摘要为 Markdown 112、知识章节 0、本地链接 328、错误 0；双运行退出码 `0/0` 且合并输出逐字符一致。
- 七个公开函数 `7/7`，第三方依赖 0，bare `except` 与 `except Exception` 命中 0。
- M-01 独立探针：六种非法场景 `6/6` 报真实行号 `FM008`，四种合法受控标量 `4/4` 通过。
- M-02 独立探针：非法根路径、注入 `OSError` 和公开 `validate_repository()` 均得到脱敏 `IO003`；`main()` 返回 1、stdout 为空、无 traceback、无异常原文。
- S-01 独立探针：同文件第 2 行与第 10 行按数字顺序输出；测试文件没有 `_error_sort_key` 私有期望来源。
- README 已语义覆盖 plain/合法成对单引号与双引号、四类非法 quoted scalar、非完整 YAML 限制和 `IO001`–`IO003`；主会话临时检查曾因强制要求英文词面 `quoted scalar` 产生假失败，按批准的语义合同复核后通过，未修改交付物。
- 工作区在初审结束前干净；未执行 push、Pull Request、网站发布或 GitHub 设置修改。

**初审边界：** 以上只说明 R1 已达到进入独立复审的条件，不替代 P0-04B-R1 的独立判断，也不构成 `accepted`。

## 新会话启动指令

把下面整段原样发送给一个未参与 P0-04-R1 的新 Codex 会话：

```text
请执行 cloud-ops-roadmap 的 P0-04B-R1“仓库校验器边界独立复审”任务。

你的身份是独立复审会话，不是 P0-04/P0-04-R1 执行者、项目领导者、规格制定者、修复者或最终验收者。你必须没有参与提交 2efbe21f92a647beecf93dda978855614b2ce827 的实现、测试、README 或 R1 执行报告编写；若参与过 P0-04-R1，立即停止。工作目录固定为 C:\Workspace\Projects\cloud-ops-roadmap。你只负责 P0-04B-R1，不得修复工具，不得执行 P0-05、P0-06 或其他任务，不得委派，也不得与其他写任务并行。

本任务在当前 main 分支和固定工作目录串行执行，不得另建分支或 Git worktree。开始时按技能规则选择并完整阅读适用技能；提交或声称完成前必须使用 verification-before-completion 取得新鲜证据。若出现测试失败或异常行为，先使用 systematic-debugging 定位根因，不得把审查脚本缺陷直接判成交付缺陷。若技能的一般建议与运行时更高层指令、根 AGENTS.md、本任务包或批准规格冲突，以更高层指令、根 AGENTS.md、本任务包和批准规格为准。

采取任何写操作前，必须完整阅读：
1. 根目录 AGENTS.md；
2. 根目录 CODEX-PRD.md；
3. 根目录 AI-CODEX-RULES.md；
4. 项目管理/任务包/P0-04-仓库校验工具.md；
5. 项目管理/任务包/P0-04B-仓库校验工具独立审查.md；
6. 项目管理/任务包/P0-04-R1-加固仓库校验器边界.md；
7. 项目管理/任务包/P0-04B-R1-仓库校验器边界独立复审.md 全文；
8. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-设计说明.md；
9. 项目管理/执行计划/2026-07-11-P0-04-仓库校验工具-实施计划.md；
10. 项目管理/执行报告/P0-04-执行报告.md；
11. 项目管理/验收记录/P0-04B-独立审查报告.md；
12. 项目管理/执行报告/P0-04-R1-执行报告.md；
13. 项目管理/任务看板.md；
14. 三个当前工具文件全文。

唯一目标：独立确认 P0-04-R1 是否精确关闭 M-01、M-02、S-01，且没有削弱原 33 个行为、路径安全、脱敏、确定性、公开接口或 README 真实性。不得信任 R1 报告或主会话初审的成功声明代替复验。

本任务原则上只读。唯一允许创建的文件是 项目管理/验收记录/P0-04B-R1-独立复审报告.md。禁止修改三个工具文件、任何执行报告、原审查报告、任务包、任务看板、设计、计划、PRD、AI 规则、模板、版本基线或其他文件。报告必须使用 apply_patch 创建。发现缺陷只记录证据、级别和建议，不得顺手修复。

启动时必须实际运行：
git status --short --branch
git branch --show-current
git remote get-url origin
python --version
Select-String -LiteralPath '项目管理\任务看板.md' -Pattern '^\| P0-04 .*`rework`','^\| P0-04B .*`accepted`','^\| P0-04-R1 .*`submitted`','^\| P0-04B-R1 .*`ready`'
git show -s --format='%H%n%P%n%s' 2efbe21f92a647beecf93dda978855614b2ce827
git show -s --format='%H%n%P%n%s' 8dd4c9c06bf138626ab27e6edba7d7ab2fc73447
git -c core.quotepath=false diff-tree --no-commit-id --name-only -r 2efbe21f92a647beecf93dda978855614b2ce827
git -c core.quotepath=false diff-tree --no-commit-id --name-only -r 8dd4c9c06bf138626ab27e6edba7d7ab2fc73447
git hash-object '脚本资源/项目校验/validate_repository.py'
git hash-object '脚本资源/项目校验/tests/test_validate_repository.py'
git hash-object '脚本资源/项目校验/README.md'
git diff --exit-code 2efbe21f92a647beecf93dda978855614b2ce827 HEAD -- '脚本资源/项目校验/validate_repository.py' '脚本资源/项目校验/tests/test_validate_repository.py' '脚本资源/项目校验/README.md'

预期：工作区干净、分支 main、远程为 https://github.com/beiyue1988/cloud-ops-roadmap.git、Python 3.11.15、四项状态正确；R1 修订父提交为 bdf8ff351d498b9eddea698ca88441fbec765386，R1 报告父提交为修订提交；修订提交只含三个工具文件，报告提交只含一份 R1 报告；三个 blob 依次为 ee4b31198f099aecadb8dc8f9d03e56565ce188f、d80de56ac065d7ed6e3b2e88ec9aac6fff75efe3、7476e64b6fd4f5ba3bf0a009e3d267c4c0ad87de，工具相对 R1 修订提交无漂移。任一条件不符立即停止，不得猜测、修复或重建被审对象。

你必须完整执行本任务包“必须独立复验的自动检查”和“必须完成的人工审查”，不得只复跑新增测试。至少取得以下新鲜证据：
1. 完整 unittest 40/40，原 34 项零删除、新增 6 项、批准行为 33/33；
2. 临时缓存目录中的 py_compile 退出 0且清理完成；
3. 当前仓库 CLI 退出 0，两次合并输出和退出码完全一致；
4. 七个公开函数 7/7、第三方依赖 0、bare except/except Exception 0；
5. 独立临时 M-01 探针覆盖未闭合双引号、未闭合单引号、首尾不匹配、闭合后尾随、列表项和 versions 映射，非法 6/6 报真实行号 FM008；plain、成对单引号、成对双引号和合法列表/映射正例通过；
6. 独立 M-02 探针通过公开 main() 观察 chr(0) 导致的 ValueError 和 mock 注入的 OSError：返回 1、stdout 为空、stderr 含 .:0: IO003、无 Traceback、无根路径或异常敏感原文；公开 validate_repository() 同样不得让预期异常逃逸；
7. 独立 S-01 探针观察同一文件第 2 行先于第 10 行，且测试文件不引用 _error_sort_key 或其他被测私有函数构造期望；
8. 在系统临时目录用 R1 父提交的旧实现加当前测试重建红灯，确认失败只对应 M-01/M-02；不得修改工作区，也不得把临时文件留在仓库；
9. README 与 R1 报告逐项对照批准合同和 Git 事实。

独立探针必须使用 python -B - 的内联脚本和 tempfile/unittest.mock，或等价的系统临时目录方案；不得在仓库创建临时脚本、缓存、快照或输出文件。断言必须基于公开返回值和渲染结果，不能调用被测私有解析器或排序函数作为期望来源。若临时审查脚本自身失败，先区分探针缺陷、环境问题和产品缺陷，记录证据后再判断。

问题分为阻塞、必须修改、建议修改和通过项。建议结论只能是 passed、passed-with-notes 或 changes-required：存在任何阻塞或必须修改时必须是 changes-required；只有非阻塞建议时可为 passed-with-notes；没有问题才是 passed。你无权宣布 P0-04、P0-04-R1 或 P0-04B-R1 accepted，也不得解锁 P0-05。

完成复审后，只创建并提交 项目管理/验收记录/P0-04B-R1-独立复审报告.md，提交信息固定为 docs: review repository validator boundary fixes。报告必须包含独立性声明、五个固定提交完整哈希、三个固定 blob、提交范围与无漂移、重建红灯、完整绿灯、编译、CLI、双运行、接口、依赖、33 项映射、M-01/M-02/S-01 独立探针、README 与 R1 报告一致性、四级问题、风险、未决问题、建议结论和远程操作声明。禁止 amend、rebase、reset、push、创建 Pull Request、发布网站或修改 GitHub 设置。

结束前再次运行完整 unittest、真实仓库 CLI、git show --check、git diff --check 和 git status --short。最终工作区只能包含已提交的独立复审报告且状态必须干净。最终回复必须给出复审报告路径、复审提交完整哈希、R1 修订和报告提交、自动复验、测试数、33/33 映射、独立场景、M-01/M-02/S-01 判断、四级问题数量、建议结论、风险、未决问题和远程操作确认。只能建议结论，不能宣布 accepted。

遇到以下任一情况必须立即停止：当前会话参与过 P0-04-R1；任务不是 ready；工作区不干净；分支、远程、Python、固定提交、父链、文件范围或 blob 不符；工具相对 R1 修订提交发生漂移；需要写入唯一报告外文件；无法在只读边界复验；发现真实疑似凭据；无法保证临时数据清理；同一外部阻塞连续出现三次。
```

## 唯一目标与学习者价值

- **唯一目标：** 独立确认 P0-04-R1 精确关闭 M-01、M-02、S-01，并且没有削弱原 33 个行为、路径安全、脱敏、确定性或 README 真实性。
- **学习者价值：** 确保后续内容门禁不会接受畸形元数据，也不会因异常根路径崩溃，同时保持可复现诊断和可信测试。

## 唯一写入白名单

```text
项目管理/验收记录/P0-04B-R1-独立复审报告.md
```

审查会话不得修改工具、测试、README、执行报告、原审查报告、任务包或任务看板。

## 固定复审范围

复审必须独立验证：

1. **M-01：** 未闭合双/单引号、首尾不匹配、尾随内容以及嵌套列表/映射畸形标量均稳定报 `FM008`；合法受控标量不误报；没有扩展成完整 YAML。
2. **M-02：** 根路径 `ValueError` 与 `OSError` 均转为 `.:0: IO003`；`main()` 返回 1、stdout 为空、stderr 无 traceback 和异常原值；没有用 `except Exception` 吞掉编程错误。
3. **S-01：** 排序测试以同一路径第 2/10 行的固定渲染结果为期望，不调用 `_error_sort_key` 或其他被测私有函数。
4. 原 34 个测试全部保留，33/33 行为映射不变，新增测试具有可重建的正确红灯与当前绿灯。
5. 七个公开函数、标准库零依赖、编译、真实仓库、双运行、路径安全、围栏、中文、脱敏均无回归。
6. README 已准确列出 `IO001`–`IO003`，R1 报告明确纠正原报告过宽结论，且 Git 事实一致。

不得只复跑 R1 新增测试。必须构造独立临时探针，所有敏感或异常文本不得回显。

## 必须独立复验的自动检查

### 1. 固定对象、范围与无漂移

运行启动指令列出的 Git、状态、版本、提交、范围、blob 和差异命令；另外运行：

```powershell
git show --check --oneline 2efbe21f92a647beecf93dda978855614b2ce827
git show --check --oneline 8dd4c9c06bf138626ab27e6edba7d7ab2fc73447
```

预期两个提交无空白错误，工具当前内容相对 R1 修订提交无漂移。

### 2. 完整测试与编译

```powershell
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
```

预期 `Ran 40 tests`、`OK`、退出 0。使用 Python AST 独立统计当前测试 40、原实现提交中的测试 34、删除 0、新增 6；再从 P0-04 任务包门禁 8 读取 33 个固定测试名并确认 `33/33` 存在。

编译必须设置唯一系统临时 `PYTHONPYCACHEPREFIX`，运行：

```powershell
python -m py_compile '脚本资源/项目校验/validate_repository.py' '脚本资源/项目校验/tests/test_validate_repository.py'
```

完成后只删除已确认位于系统临时目录内的本次唯一缓存目录，确认仓库没有 `__pycache__` 或 `.pyc`。

### 3. CLI、双运行、接口与依赖

```powershell
python -B '脚本资源/项目校验/validate_repository.py' .
```

连续运行两次并分别捕获合并输出与退出码；要求退出码 `0/0` 且输出逐字符相同。用 AST 检查七个公开函数精确存在，导入只来自 Python 3.11 标准库和 `__future__`，第三方依赖为 0；检查实现不存在 bare `except` 或捕获 `Exception`。

### 4. 重建正确红灯

在系统临时目录构造与仓库相同的 `脚本资源/项目校验/tests/` 层级，将 R1 父提交 `bdf8ff351d498b9eddea698ca88441fbec765386` 中的旧 `validate_repository.py` 与当前测试文件放入临时树，再运行同一 `unittest discover`。要求退出码非 0，原 34 项及合法标量/S-01 测试通过，失败只对应三个 M-01 方法和两个 M-02 方法；记录 `Ran 40 tests` 及失败/错误摘要。退出临时目录后验证绝对路径位于系统临时根，再用 PowerShell `Remove-Item -LiteralPath` 清理该唯一目录。不得在仓库落盘或修改 Git 历史。

### 5. 缺陷级独立探针

使用不落盘的 `python -B -` 内联程序，动态导入当前实现并只观察公开接口：

- M-01：六个非法案例与行号 `2/2/2/2/8/16`，以及四类合法正例；
- M-02：`main([chr(0)])`、对 `Path.resolve` 注入含虚构敏感文本的 `OSError`、`validate_repository(Path(chr(0)))`；
- S-01：临时仓库中同一正式内容文件第 2 行和第 10 行分别放置 `TODO`，要求诊断顺序精确为 `2,10`；
- 回归：至少一个合法临时仓库返回空错误列表。

不得使用 `_parse_scalar`、`_aggregate_repository`、`_error_sort_key` 等私有实现作为期望值来源。

### 6. README、报告和最终状态

逐项核对 README 对普通标量、合法成对单/双引号、非法引号、非完整 YAML、`IO001`–`IO003`、脱敏、无 traceback 和明确限制的说明。英文术语不要求固定词面，语义必须与实现和批准合同一致。核对 R1 报告中的固定哈希、测试数、提交范围、异常、原报告纠正、风险和远程操作与 Git 事实一致。

最后运行：

```powershell
git diff --check
git -c core.quotepath=false status --short
```

## 必须完成的人工审查

逐项给出“通过/不通过/需说明”和直接证据：

1. `_parse_scalar` 是否只实现批准的受控边界，而非伪装成完整 YAML；
2. 顶层、列表和映射是否统一应用非法 quoted scalar 判断；
3. `FM008` 行号是否来自真实输入位置；
4. `IO003` 是否覆盖批准的根路径规范化与遍历异常；
5. 异常捕获是否只限 `ValueError/OSError`，没有吞掉一般编程错误；
6. CLI 与公开 API 是否都脱敏、稳定、无 traceback；
7. 排序断言是否真正独立于私有排序实现；
8. 原 34 项、33 个批准行为、路径/围栏/中文/Secret 规则是否无回归；
9. README 是否与实现一致且不夸大能力；
10. R1 报告是否准确纠正原报告的过宽结论并保持历史快照不可变；
11. 两个 R1 提交是否满足范围、顺序和不可变性；
12. 是否存在新安全风险、敏感信息、未决问题或范围外改动。

## 问题分级与建议结论

- **阻塞：** 固定对象不明、范围漂移、工具无法复验、安全或敏感信息风险；
- **必须修改：** M-01/M-02 未关闭、S-01 仍自证、原行为回归、异常被过度吞噬或文档失真；
- **建议修改：** 不阻塞三项关闭，但可改善维护性或诊断体验；
- **通过项：** 有独立命令、临时场景或文件证据支持。

建议结论只允许 `passed`、`passed-with-notes`、`changes-required`。复审会话无权宣布 `accepted`。

## 复审报告要求

`项目管理/验收记录/P0-04B-R1-独立复审报告.md` 必须包含：

1. 独立性声明；
2. 原实现、原报告、原审查、R1 修订和 R1 报告完整哈希及当前工具 blob；
3. 提交范围与无漂移证据；
4. 重建红灯、完整绿灯、编译、CLI、双运行、依赖和 33 行为结果；
5. 独立临时探针与 M-01/M-02/S-01 逐项关闭判断；
6. README 与 R1 报告一致性；
7. 四级问题、风险、未决问题和建议结论；
8. 未执行远程写操作及最终验收权属于主会话的声明。

## 提交与远程限制

只能提交独立复审报告，提交信息固定为：

```text
docs: review repository validator boundary fixes
```

禁止修改或重建任何被审提交，禁止 amend、rebase、reset、push、创建 PR、发布网站或修改 GitHub 设置。

## 停止条件

- 当前任务不是 `ready`，或任务包缺少真实哈希、blob 和完整启动指令；
- 会话参与过 P0-04-R1；
- 状态、工作区、分支、远程、Python、固定提交、父链或工具 blob 不符；
- 需要修改唯一报告外文件；
- 被审工具发生漂移，或无法在只读边界完成复验；
- 发现真实疑似凭据，或无法安全清理系统临时数据；
- 同一外部阻塞连续出现三次。

## 完成后的回复

```text
P0-04B-R1 独立复审已提交。
复审报告：项目管理/验收记录/P0-04B-R1-独立复审报告.md
复审提交：填写 docs: review repository validator boundary fixes 的完整哈希
R1 修订提交：2efbe21f92a647beecf93dda978855614b2ce827
R1 报告提交：8dd4c9c06bf138626ab27e6edba7d7ab2fc73447
自动复验：填写通过项数/总项数
重建红灯：填写测试数、退出码和目标失败摘要
完整绿灯：填写通过数/总数和退出码
行为映射：填写覆盖数/33
独立场景：填写通过数/总数
M-01/M-02/S-01：填写 closed 或未关闭证据
问题分级：填写阻塞/必须修改/建议修改/通过项数量
建议结论：passed、passed-with-notes 或 changes-required
风险：填写“无”或具体风险
未决问题：填写“无”或具体问题
远程操作：确认未 push、未创建 PR、未发布网站、未修改 GitHub 设置
最终验收：未宣布 accepted，等待主会话裁决
```
