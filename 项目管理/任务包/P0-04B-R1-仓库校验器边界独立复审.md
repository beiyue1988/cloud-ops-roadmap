# P0-04B-R1 仓库校验器边界独立复审任务包

## 当前锁定状态

- **状态：** `planned`
- **当前不得启动：** P0-04-R1 尚未产生并通过主会话初审的修订提交与返工报告提交。
- **解锁责任人：** 主会话必须补入两个真实完整哈希、提交范围、工具 blob、复验摘要和完整启动指令后，才能把本任务改为 `ready`。
- **禁止提前执行：** 不得创建复审报告、修改工具或推测 R1 哈希。

## 任务信息

- **任务编号：** P0-04B-R1
- **任务名称：** 仓库校验器边界独立复审
- **所属 Phase：** Phase 0
- **执行方式：** 未参与 P0-04-R1 的独立新会话，只读复审
- **制定者：** 主会话
- **原实现提交：** `08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3`
- **原报告提交：** `d2952cf3ea5216169deaeaa9e320cf3f6d409363`
- **原独立审查提交：** `0bdc8410dec6833c88bdf7afdbf77b29937bdcde`
- **原独立审查报告：** [P0-04B 独立审查报告](../验收记录/P0-04B-独立审查报告.md)
- **返工任务包：** [P0-04-R1 加固仓库校验器边界](P0-04-R1-加固仓库校验器边界.md)

## 唯一目标与学习者价值

- **唯一目标：** 独立确认 P0-04-R1 精确关闭 M-01、M-02、S-01，并且没有削弱原 33 个行为、路径安全、脱敏、确定性或 README 真实性。
- **学习者价值：** 确保后续内容门禁不会接受畸形元数据，也不会因异常根路径崩溃，同时保持可复现诊断和可信测试。

## 解锁门禁

主会话只有同时确认以下条件才能改为 `ready`：

1. R1 修订提交只包含三个工具文件；
2. R1 报告提交只包含 `项目管理/执行报告/P0-04-R1-执行报告.md`；
3. 两个新提交完整哈希、父提交和当前三个工具 blob 已锁定；
4. 主会话复跑新增测试、完整测试、编译、CLI、双运行、依赖和行为映射；
5. M-01/M-02/S-01 探针均关闭，原 34 项无回归；
6. 工作区干净，任务状态为 P0-04 `rework`、P0-04-R1 `submitted`、P0-04B-R1 `ready`；
7. 本任务包包含可直接复制的完整启动指令。

缺少任一项保持 `planned`。

## 唯一写入白名单

解锁后唯一允许创建：

```text
项目管理/验收记录/P0-04B-R1-独立复审报告.md
```

审查会话不得修改工具、测试、README、执行报告、原审查报告、任务包或任务看板。

## 固定复审范围

复审必须独立验证：

1. **M-01：** 未闭合双/单引号、首尾不匹配、尾随内容以及嵌套列表/映射畸形标量均稳定报 `FM008`；合法受控标量不误报；没有扩展成完整 YAML。
2. **M-02：** 根路径 `ValueError` 与 `OSError` 均转为 `.:0: IO003`；`main()` 返回 1、stdout 为空、stderr 无 traceback 和异常原值；没有用 `except Exception` 吞掉编程错误。
3. **S-01：** 排序测试以同一路径第 2/10 行的固定渲染结果为期望，不调用 `_error_sort_key` 或其他被测私有函数。
4. 原 34 个测试全部保留，33/33 行为映射不变，新增测试真实经历红绿证据。
5. 七个公开函数、标准库零依赖、编译、真实仓库、双运行、路径安全、围栏、中文、脱敏均无回归。
6. README 已准确列出 `IO001`–`IO003`，R1 报告明确纠正原报告过宽结论，且 Git 事实一致。

不得只复跑 R1 新增测试。必须构造独立临时探针，至少覆盖 quoted scalar 四种非法结构、列表/映射位置、根路径两类异常、同文件数字排序和一个合法仓库；所有敏感/异常文本不得回显。

## 问题分级与建议结论

- **阻塞：** 固定对象不明、范围漂移、工具无法复验、安全或敏感信息风险；
- **必须修改：** M-01/M-02 未关闭、S-01 仍自证、原行为回归、异常被过度吞噬或文档失真；
- **建议修改：** 不阻塞三项关闭，但可改善维护性或诊断体验；
- **通过项：** 有独立命令、临时场景或文件证据支持。

建议结论只允许 `passed`、`passed-with-notes`、`changes-required`。复审会话无权宣布 `accepted`。

## 复审报告要求

`项目管理/验收记录/P0-04B-R1-独立复审报告.md` 必须包含：

1. 独立性声明；
2. 原实现、原报告、原审查、R1 修订和 R1 报告完整哈希及当前工具 blob；
3. 提交范围与无漂移证据；
4. 新增测试、完整回归、编译、CLI、双运行、依赖和 33 行为结果；
5. 独立临时探针与 M-01/M-02/S-01 逐项关闭判断；
6. README 与 R1 报告一致性；
7. 四级问题、风险、未决问题和建议结论；
8. 未执行远程写操作及最终验收权属于主会话的声明。

## 提交与远程限制

解锁后只能提交独立复审报告，提交信息固定为：

```text
docs: review repository validator boundary fixes
```

禁止修改或重建任何被审提交，禁止 push、创建 PR、发布网站或修改 GitHub 设置。

## 停止条件

- 本任务仍为 `planned` 或没有真实 R1 哈希和完整启动指令；
- 会话参与过 P0-04-R1；
- 状态、工作区、分支、远程、固定提交或工具 blob 不符；
- 需要修改唯一报告外文件；
- 被审工具发生漂移，或无法在只读边界完成复验；
- 发现真实疑似凭据或同一外部阻塞连续三次。

## 解锁后的完成回复格式

```text
P0-04B-R1 独立复审已提交。
复审报告：项目管理/验收记录/P0-04B-R1-独立复审报告.md
复审提交：填写 docs: review repository validator boundary fixes 的完整哈希
R1 修订提交：填写任务包锁定的完整哈希
R1 报告提交：填写任务包锁定的完整哈希
自动复验：填写通过数/总数
新增测试：填写实际数量
总测试：填写通过数/总数
行为映射：填写覆盖数/33
独立场景：填写通过数/总数
M-01/M-02/S-01：填写 closed 或未关闭证据
问题分级：填写阻塞/必须修改/建议修改/通过项数量
建议结论：passed、passed-with-notes 或 changes-required
风险：填写“无”或具体风险
未决问题：填写“无”或具体问题
远程操作：确认未 push、未创建 PR、未发布网站、未修改 GitHub 设置
最终验收：未宣布 accepted，等待主会话裁决
```
