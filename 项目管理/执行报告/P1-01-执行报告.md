# P1-01 大纲合同与校验门执行报告

## 1. 任务与角色

- 任务编号：P1-01
- 任务名称：大纲合同与校验门
- 执行日期：2026-07-13
- 执行角色：独立执行会话，不是主会话、规格制定者、独立审查者或最终验收者
- 工作目录与分支：`C:\Workspace\Projects\cloud-ops-roadmap`，`main`
- 建议状态：`submitted`

## 2. 范围与文件清单

- 唯一目标：在保持既有仓库校验行为兼容的前提下，为 Phase 1 文件级章节清单、派生视图和实践映射实现 `partial`、`catalogs`、`views`、`complete` 四级机械门禁。
- 范围确认：实现受控 Markdown 表格解析、`OL001`–`OL012`、章节与文件合同、依赖 DAG、十二周闭包、视图引用、检查点/实验/项目锚点和项目双向映射；增加测试并更新工具说明。
- 非范围确认：没有创建或修改阶段章节清单、路线、索引、检查点、实验、项目、故障、面试、配置、脚本示例、图表、PRD、计划、任务包、任务看板、ADR、版本基线或项目记忆。

| 白名单文件 | 处理方式 | 实际结果 | 范围结论 |
|---|---|---|---|
| `脚本资源/项目校验/validate_repository.py` | 修改 | 新增四级 CLI、受控解析、确定性聚合、`OL001`–`OL012` 和统计摘要；默认 CLI 保持原摘要 | 符合 |
| `脚本资源/项目校验/tests/test_validate_repository.py` | 修改 | 原 44 项全部保留，新增 47 项，最终 91 项且无 skip | 符合 |
| `脚本资源/项目校验/README.md` | 修改 | 增加四级门禁、表格合同、规则编号、输出、波次边界和限制说明 | 符合 |
| `项目管理/执行报告/P1-01-执行报告.md` | 创建 | 记录产品提交、红—绿证据、验证、风险和状态建议 | 符合 |

- 白名单缺失：0。
- 白名单越界：0。
- 正式内容越界：九类正式目录中非 `README.md` Markdown 为 0。

## 3. 提交记录

| 提交类型 | 完整提交哈希 | 提交信息 | 文件范围 |
|---|---|---|---|
| 产品提交 | `f9d11fc93de462a7ba55b8cafaaf1b9e95abfb0e` | `feat: add Phase 1 outline validation gate` | `脚本资源/项目校验/validate_repository.py`、`脚本资源/项目校验/tests/test_validate_repository.py`、`脚本资源/项目校验/README.md`，共 3 个文件 |

本报告不预测报告提交自身哈希；报告提交完成后只在执行会话完成回复中提供。

## 4. 验收标准与证据映射

| # | 验收标准 | 实际证据 | 证据等级 | 结论 |
|---:|---|---|---|---|
| 1 | 默认行为兼容、参数边界和只读确定性 | 原 44 项 44/44；默认 CLI 两次退出 0且输出逐字符一致；`--help` 列出四个唯一选项；静态扫描无写文件、联网或子进程入口 | 环境验证 + 静态检查 | 通过 |
| 2 | 章节清单九字段、ID、文件名、字段枚举和受控语法 | `OutlineGateTests` 覆盖合法表、多值/空集合、错误表头/分隔、ID/文件重复与错配、字段非法 | 环境验证 | 通过 |
| 3 | 四级门禁逐级严格 | 四级各有完整通过样例和规定缺失失败样例；当前仓库 `partial` 退出 0；当前尚无 21 个清单时 `catalogs` 稳定退出 1 | 环境验证 | 通过 |
| 4 | 依赖、十二周路线、视图引用和就业闭包 | 覆盖缺失/自引用/前向/循环、主线缺项/多项/重复/乱序、周次乱序、就业标签冲突和不存在的视图章节 | 环境验证 | 通过 |
| 5 | 锚点定义和项目双向映射 | 覆盖 `CP-NN` 完整连续、Level 定义位置、项目里程碑连续、未定义锚点和双向映射两个缺口方向 | 环境验证 | 通过 |
| 6 | 确定性诊断 `OL001`–`OL012` | 每个规则至少一个失败测试；诊断按相对路径、数字行号、规则和消息稳定排序；重复输入结果相同 | 环境验证 | 通过 |
| 7 | 测试数量和旧测试保护 | 启动基线 44；最终 91；新增 47；失败 0、错误 0、skip 0 | 环境验证 | 通过 |
| 8 | README 与限制一致 | README 记录默认兼容、四级职责、精确表头、语法、规则、摘要、波次 3/4 边界和非通用解析限制 | 人工检查 + 仓库校验 | 通过 |

## 5. TDD 红—绿证据

1. 增加第一批 44 项大纲测试后，单独运行原四个测试类：原 44 项仍为 44/44，退出 0。
2. 在修改生产实现前运行 `OutlineGateTests`：44 项全部按预期失败，退出 1；主要原因是缺少 `validate_outline`、`--outline-gate` 和 `OL` 诊断，不是夹具语法错误。
3. 实现最小受控解析和四级门禁后，同一 44 项大纲测试全部通过，退出 0。
4. 自审新增“十二周行顺序”和“检查点连续顺序”两个失败测试；两项在修复前均失败，退出 1，修复后 2/2 通过。
5. 最终测试文件包含原 44 项和新增 47 项，共 91 项；没有删除、重命名、跳过或弱化原测试。

## 6. 实际命令、退出码与关键输出

| # | 实际命令或人工检查 | 退出码 | 关键输出 | 运行环境与日期 |
|---:|---|---:|---|---|
| 1 | 启动 Git、固定提交、看板与 Python 门禁 | 0 | `main`；放行 HEAD `8f2709030468c860bee511a2de3f451cb7f8ffa2`；看板 `assigned`；origin 精确匹配；固定祖先和任务包提交均可达；Python 3.11.15 | Windows / 2026-07-13 |
| 2 | 原四个测试类 | 0 | 44/44，失败 0、错误 0 | Python 3.11.15 / 2026-07-13 |
| 3 | 第一批 `OutlineGateTests` 红灯 | 1 | 44 项失败，均指向缺失大纲 API、CLI 或规则行为 | Python 3.11.15 / 2026-07-13 |
| 4 | 两项连续性红灯 | 1 | 周次乱序和检查点乱序未被拒绝，2/2 失败 | Python 3.11.15 / 2026-07-13 |
| 5 | 内存编译检查 | 0 | `compile_ok`，未在仓库生成缓存文件 | Python 3.11.15 / 2026-07-13 |
| 6 | 完整 `unittest discover -v` | 0 | `Ran 91 tests`，`OK` | 产品提交 `f9d11fc...` / 2026-07-13 |
| 7 | `validate_repository.py --help` | 0 | 显示 `{partial,catalogs,views,complete}` 及各级含义 | 产品提交 `f9d11fc...` / 2026-07-13 |
| 8 | 默认校验连续两次 | 0 | 两次实际退出码 0；`equal=True`；Markdown 142、知识章节 0、本地链接 419、错误 0 | 产品提交 `f9d11fc...` / 2026-07-13 |
| 9 | 当前仓库 `--outline-gate partial` | 0 | 阶段清单 0、章节 0、依赖 0、视图引用 0、锚点 0、错误 0 | 产品提交 `f9d11fc...` / 2026-07-13 |
| 10 | 当前仓库 `--outline-gate catalogs` 预期失败 | 1 | 21 个阶段各报告稳定 `OL001`；总错误 21；无 traceback | 产品提交 `f9d11fc...` / 2026-07-13 |
| 11 | 正式内容、Git 空白和提交范围 | 0 | `formal_non_readme=0`；`git diff/show --check` 通过；产品提交精确 3 文件 | Git / 2026-07-13 |
| 12 | 测试方法和依赖静态检查 | 0 | 总测试 91、新增 47、skip 0；导入仅 Python 标准库 | Python AST / 2026-07-13 |
| 13 | 执行报告创建后默认仓库校验 | 0 | Markdown 143、知识章节 0、本地链接 419、错误 0；工作区仅含本报告 | 产品提交 + 执行报告 / 2026-07-13 |

- 自动验证：17/17 通过，包括启动门、红—绿、编译、测试、帮助、默认确定性、`partial`、预期失败 `catalogs`、正式内容、Git 范围、空白、依赖和报告创建后仓库校验。
- 人工检查：12/12 通过，包括角色范围、默认兼容、表格合同、四级边界、规则归属、路径与行号、脱敏、只读性、依赖闭包、锚点、双向映射、README 和提交拆分。
- 未运行检查：没有运行真实 Phase 1 完整仓库的 `catalogs`、`views` 或 `complete` 正向命令，因为对应 21 个阶段清单和后续波次文件尚未创建；其完整正向行为由临时仓库单元测试覆盖，当前仓库 `catalogs` 按任务包要求必须失败。

## 7. `OL001`–`OL012` 覆盖映射

| 规则 | 主要自动覆盖 |
|---|---|
| `OL001` | 阶段标题/表格缺失、21 阶段缺项、波次 3 视图缺失、Level 文件缺失 |
| `OL002` | 错误表头、错误分隔、列数/受控列表和文本字段 |
| `OL003` | ID 格式、阶段错配、非连续和全局重复 |
| `OL004` | 文件名格式、ID 错配和全局重复 |
| `OL005` | 空标题/目标、非法 `type`、投入和就业标签 |
| `OL006` | 前置缺失、自引用、前向引用和单元格重复 |
| `OL007` | 两节点依赖循环 |
| `OL008` | 不存在的视图章节、路线前置顺序和依赖视图错配 |
| `OL009` | 十二周闭包缺项/多项、周次乱序、就业后补学冲突 |
| `OL010` | 锚点语法、未定义引用、检查点完整连续、Level 位置和项目里程碑连续 |
| `OL011` | 项目到章节、章节到项目两个方向的映射缺口及总视图集合 |
| `OL012` | 主线重复、主线/弹性越界、弹性标签和派生主键重复 |

## 8. 来源、版本、环境与证据等级

- 权威来源：`AGENTS.md`、`CODEX-PRD.md`、P1-01 任务包、Phase 1 设计说明、Phase 1 实施计划、P0-04 验收报告、原校验器 README/实现/测试和执行报告模板。
- 来源核验日期：2026-07-13。
- 版本入口：Python 3.11.15、仅标准库、`unittest`；Phase 0 最终验收提交 `a3c04d28461861bdd166d9f2208201dae3c3011f`；实施计划批准提交 `14ef931ef7fa59915e9f415c236e44f3c99a62d7`；任务包提交 `2ebb32f01c0b9bbc80ebdc3be821d6a8b08da79d`；放行提交 `8f2709030468c860bee511a2de3f451cb7f8ffa2`。
- 验证环境：Windows PowerShell，Python 3.11.15，Git `main`，产品提交 `f9d11fc93de462a7ba55b8cafaaf1b9e95abfb0e`。
- 最高证据等级：临时仓库正反例环境验证 + 当前仓库真实 CLI 验证；适用于大纲机械合同和默认兼容。
- 证据限制：机械门禁不能证明未来章节的教学颗粒度、技术真实性或人工质量；当前仓库尚不具备后三个严格门禁的真实正向输入。

## 9. 异常、权限、风险与未决问题

- 异常：PowerShell 捕获原生 Python 合并输出时中文显示乱码，但直接 CLI 输出正常；同一捕获链路的两次默认输出逐字符一致。Git 仍提示无法读取用户级 `C:\Users\beiyue\.config\git\ignore`，但显式状态、范围、提交和空白检查均成功。
- 权限请求：受沙箱保护的 `.git/index` 需要一次提权以执行任务包已授权的 `git add` 和 `git commit`；请求获准后仅操作三个产品文件，未扩大范围。
- 风险：受控解析器不是通用 Markdown/CommonMark 解析器；`views` 按批准波次 3 不提前要求波次 4 的 `06-贯穿项目演进线.md`，该文件及项目锚点存在性在 `complete` 闭合。两项均已写入 README。
- 未决问题：实现范围内无技术未决问题；需要主会话进行范围初审并签发 P1-01B 独立审查。
- 计划偏离：无范围偏离。测试总数从最低 72 提升到 91，以覆盖所有规则、四级正反例和新增顺序边界。

## 10. 远程操作确认

| 操作 | 是否执行 | 授权依据或未执行确认 |
|---|---|---|
| push | 否 | 任务包未授权，未执行 |
| 创建 Pull Request | 否 | 任务包未授权，未执行 |
| 发布网站或制品 | 否 | 任务包未授权，未执行 |
| 修改 GitHub 设置 | 否 | 任务包未授权，未执行 |

## 11. 状态建议与后续门禁

- 建议状态：`submitted`。
- 建议依据：四个白名单交付物完整；产品提交精确三文件；91/91 测试、默认确定性、当前 `partial`、预期失败 `catalogs`、正式内容边界、Git 范围和空白检查均取得实际证据；实现范围内没有未关闭阻塞。
- 后续门禁：主会话先复核产品提交和本报告的范围，再签发 P1-01B，由独立审查会话检查固定提交；只有独立审查和主会话复验通过后，才能解锁波次 2。
- 责任边界：本报告只建议 `submitted`，不宣布 `accepted`。
