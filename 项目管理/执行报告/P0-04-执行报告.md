# P0-04 仓库校验工具执行报告

## 1. 任务与角色

- **任务编号：** P0-04
- **任务名称：** 仓库校验工具
- **执行日期：** 2026-07-11
- **执行角色：** 独立执行会话，不承担规格制定、独立审查或最终验收
- **工作目录与分支：** `C:\Workspace\Projects\cloud-ops-roadmap`，`main`
- **建议状态：** `submitted`

## 2. 范围与文件清单

- **唯一目标：** 建立仅依赖 Python 3.11 标准库、具有稳定错误编号和完整测试的本地 Markdown 仓库校验器。
- **范围确认：** 完成受控 Front Matter、重复章节编号、本地链接、正式内容占位符、高置信度敏感信息、UTF-8/根目录错误、稳定排序、CLI、测试和使用说明。
- **非范围确认：** 未执行 P0-04B、P0-05、P0-06；未修改学习内容、实验、项目、故障、面试、图表、模板、版本基线、CI、网站或任务看板。

| 白名单文件 | 处理方式 | 实际结果 | 范围结论 |
|---|---|---|---|
| `脚本资源/项目校验/validate_repository.py` | 创建 | 七个公开函数、集中常量、五层实现和 CLI | 符合 |
| `脚本资源/项目校验/tests/test_validate_repository.py` | 创建 | 34 个 `unittest` 测试方法，覆盖规定 33 个行为及 1 个回归行为 | 符合 |
| `脚本资源/项目校验/README.md` | 创建 | 环境、命令、规则、范围、输出、退出码、测试和限制 | 符合 |
| `项目管理/执行报告/P0-04-执行报告.md` | 创建 | 本报告 | 符合 |

- **白名单缺失：** 0。
- **白名单越界：** 0。

## 3. 提交记录

| 提交类型 | 完整提交哈希 | 提交信息 | 文件范围 |
|---|---|---|---|
| 实现提交 | `08f4894deb0d2ee7ddea911c84ea30e53d3f6ea3` | `test: add repository content validator` | 精确为三个 `脚本资源/项目校验/` 工具文件 |

报告正文只记录报告创建前已经产生的实现提交，不预测报告提交自身哈希。

## 4. TDD 红灯与最终绿灯

### 4.1 正确红灯

在 `validate_repository.py` 不存在、测试文件已包含全部规定行为时运行：

```powershell
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
```

- **实际退出码：** 1。
- **关键输出：** `Ran 1 test`，`FAILED (errors=1)`；测试发现阶段加载模块时产生 `FileNotFoundError`，缺失路径为 `脚本资源/项目校验/validate_repository.py`。
- **红灯结论：** 失败原因仅为实现模块不存在；没有测试语法错误或 fixture 错误。

### 4.2 最终绿灯

README 创建后及实现提交前完整运行同一命令：

- **实际退出码：** 0。
- **关键输出：** `Ran 34 tests`，`OK`。
- **测试方法：** 34，满足不少于 24。
- **规定行为映射：** 33/33；额外测试为 `test_link_in_inline_code_is_ignored`，用于修复真实仓库中行内代码示例被误识别为链接的问题。

实现提交后再次完整运行：退出码 0，`Ran 34 tests`，`OK`。

## 5. 33 个行为到实际测试方法的映射

| # | 规定行为 | 实际测试方法 | 具体断言或可观察结果 |
|---:|---|---|---|
| 1 | 合法 Front Matter | `test_extract_valid_front_matter` | 提取 `chapter/status`，`validate_front_matter` 返回空列表 |
| 2 | 知识正文缺 Front Matter | `test_front_matter_required_for_knowledge_article` | `FM001` 与第 1 行 |
| 3 | 未闭合分隔符 | `test_front_matter_requires_closing_delimiter` | `FM001` |
| 4 | 缺少键 | `test_front_matter_reports_missing_key` | `FM002` 与 `status` |
| 5 | 重复键 | `test_front_matter_reports_duplicate_key` | `FM003` 与重复键真实行号 |
| 6 | 空值与空列表 | `test_front_matter_rejects_empty_values` | 至少两条 `FM004` |
| 7 | 非法 `type` | `test_front_matter_rejects_invalid_type` | `FM005` |
| 8 | 非法 `status` | `test_front_matter_rejects_invalid_status` | `FM006` |
| 9 | 非法日期 | `test_front_matter_rejects_invalid_date` | `FM007` |
| 10 | 合法列表与 `versions` | `test_front_matter_accepts_lists_and_versions_map` | 映射文本可提取且无诊断 |
| 11 | 超出受控 YAML 子集 | `test_front_matter_rejects_unsupported_yaml` | `FM008` |
| 12 | 重复 `chapter` 两端报错 | `test_duplicate_chapter_reports_both_files` | 两条 `CH001`，分别指向两个文件 |
| 13 | 有效文件与图片链接 | `test_valid_relative_links_and_images` | 无链接诊断 |
| 14 | 外链、邮件与纯锚点忽略 | `test_external_mail_and_anchor_are_ignored` | 无链接诊断 |
| 15 | 文件加锚点只检查文件 | `test_file_anchor_checks_file_only` | 文件存在时不检查锚点 |
| 16 | 失效链接 | `test_broken_relative_link` | `LINK001` 与真实行号 |
| 17 | 越出根目录 | `test_link_cannot_escape_root` | `LINK002` |
| 18 | 绝对路径与盘符 | `test_absolute_and_drive_links_are_rejected` | 两条 `LINK003` |
| 19 | 大小写不一致 | `test_case_mismatch_is_detected` | `LINK004` |
| 20 | 围栏内链接忽略 | `test_link_in_fence_is_ignored` | 反引号和波浪线围栏均无诊断 |
| 21 | 引用式与尖括号目标 | `test_reference_and_angle_bracket_links` | 含空格目标存在时无诊断 |
| 22 | 正式内容占位符 | `test_formal_content_placeholder_is_rejected` | `PH001` 与 `PH002` |
| 23 | 围栏内占位符忽略 | `test_placeholder_in_fence_is_ignored` | 无占位符诊断 |
| 24 | 排除目录 | `test_placeholder_excluded_directories` | 模板、项目管理和根文件均无占位符诊断 |
| 25 | 模板标记残留正式内容 | `test_template_marker_in_formal_content_is_rejected` | `PH003` 与 `PH004` |
| 26 | 虚构 Secret 检出且不回显 | `test_synthetic_secret_is_rejected_and_redacted` | 存在 `SEC`，诊断不含运行时拼接原值 |
| 27 | 中文路径与 UTF-8 | `test_chinese_paths_and_utf8` | 中文相对链接和正文无诊断 |
| 28 | 非法 UTF-8 | `test_invalid_utf8_reports_io_error` | `IO002`、相对路径和根行号 0 |
| 29 | 稳定数字排序 | `test_errors_are_sorted_deterministically` | 两次结果相同，按 `_error_sort_key` 排序，第 10 行按数字处理 |
| 30 | CLI 成功 | `test_main_returns_zero_on_success` | 返回 0、标准输出含“校验通过”、标准错误为空 |
| 31 | CLI 失败 | `test_main_returns_one_on_failure` | 返回 1、标准错误含 `LINK001` 与失败摘要、无 traceback |
| 32 | 根目录不存在且无 traceback | `test_missing_root_returns_one_without_traceback` | 返回 1、`.:0: IO001`、无 traceback |
| 33 | 真实仓库通过 | `test_current_repository_passes` | 当前根目录诊断精确为空列表 |

## 6. 验收标准与证据映射

| # | 验收标准 | 实际证据 | 证据等级 | 结论 |
|---:|---|---|---|---|
| 1 | 正确 TDD 红灯 | 实现不存在时完整发现退出 1，唯一根因是模块文件缺失 | 环境验证 | 通过 |
| 2 | 七个接口与零第三方依赖 | AST 得到公开函数 7/7；两文件导入第三方数量 0 | 静态检查 | 通过 |
| 3 | 不少于 24 个测试与 33 行为 | 34 个方法，映射 33/33，缺失 0 | 环境验证 | 通过 |
| 4 | 完整绿灯 | `Ran 34 tests`、`OK`、退出 0 | 环境验证 | 通过 |
| 5 | 可编译且无仓库缓存 | 两文件 `py_compile` 退出 0；临时缓存删除；仓库缓存目录 0 | 环境验证 | 通过 |
| 6 | 真实仓库与确定性 | 单次退出 0；双运行 0/0 且逐字符相同 | 环境验证 | 通过 |
| 7 | 规则边界与脱敏 | 自动测试及十项人工检查 | 环境验证与静态检查 | 通过 |
| 8 | README 准确 | 全文复核环境、命令、范围、规则、输出、测试与限制 | 静态检查 | 通过 |
| 9 | 文件边界 | 白名单缺失/越界 0/0，缓存 0，格式检查通过 | 静态检查 | 通过 |
| 10 | 提交边界 | `diff-tree` 精确列出三个工具文件 | Git 静态检查 | 通过 |
| 11 | 远程边界 | 未执行任何远程写操作 | 执行记录 | 通过 |

## 7. 实际命令、退出码与关键输出

| # | 实际命令或人工检查 | 退出码 | 关键输出 | 环境与日期 |
|---:|---|---:|---|---|
| 1 | 启动六项门禁 | 0 | `main`；远程正确；Python 3.11.15；P0-04 `ready`；两个目标不存在 | Windows PowerShell，2026-07-11 |
| 2 | 红灯 `python -B -m unittest discover ... -v` | 1 | `FileNotFoundError` 指向尚不存在的实现模块 | Python 3.11.15，2026-07-11 |
| 3 | 阶段性完整测试 | 1/1/1/0 | Front Matter、链接、占位符/敏感信息依次转绿，任务 7 达到全绿 | Python 3.11.15，2026-07-11 |
| 4 | 最终完整 `unittest` | 0 | `Ran 34 tests`，`OK` | Python 3.11.15，2026-07-11 |
| 5 | 临时 `PYTHONPYCACHEPREFIX` 的 `python -m py_compile` | 0 | 两文件编译成功；`pycache_remaining=False` | Python 3.11.15，2026-07-11 |
| 6 | `python -B '脚本资源/项目校验/validate_repository.py' .` | 0 | `Markdown 107，知识章节 0，本地链接 322；错误 0` | 当前仓库，2026-07-11 |
| 7 | 连续两次真实仓库校验与合并输出比较 | 0 | `first_exit=0 second_exit=0 equal=True` | 当前仓库，2026-07-11 |
| 8 | 实现 AST 导入清单 | 0 | `__future__`、`argparse`、`dataclasses`、`datetime`、`pathlib`、`re`、`sys`、`urllib` | Python 3.11.15，2026-07-11 |
| 9 | 实现与测试 AST 标准库归属 | 0 | `third_party_count=0` | Python 3.11.15，2026-07-11 |
| 10 | 七个公开函数与测试方法统计 | 0 | `public_functions=7/7`，`test_methods=34` | Python 3.11.15，2026-07-11 |
| 11 | 33 个规定方法映射检查 | 0 | `behavior_mapping=33/33`，`missing=0` | PowerShell，2026-07-11 |
| 12 | `--help` | 0 | 显示可选 `root` 与帮助选项 | Python 3.11.15，2026-07-11 |
| 13 | 精确白名单检查 | 0 | `missing=0 extra=0` | Git，2026-07-11 |
| 14 | `git diff --check` / `git diff --cached --check` | 0/0 | 无空白错误 | Git，2026-07-11 |
| 15 | 仓库缓存目录检查 | 0 | `repository_cache_dirs=0` | PowerShell，2026-07-11 |
| 16 | 实现提交前完整测试与真实仓库 | 0/0 | 34/34；真实仓库错误 0 | Python 3.11.15，2026-07-11 |
| 17 | 实现提交后完整测试、真实仓库、状态 | 0/0/0 | 34/34；错误 0；工作区变更 0 | Python 3.11.15 与 Git，2026-07-11 |
| 18 | 十项人工检查 | 不适用 | 10/10 通过，详见下一节 | 全文审阅，2026-07-11 |

- **自动验证：** 规定自动门禁全部通过。
- **人工检查：** 10/10 通过。
- **未运行检查：** 无。

## 8. 十项人工检查

| # | 检查项 | 实际结论 |
|---:|---|---|
| 1 | 规则没有超出批准设计 | 通过；仅增加行内代码不解析为链接的回归保护，属于正确识别 Markdown 链接 |
| 2 | Front Matter 明确拒绝超出子集结构 | 通过；统一使用 `FM008` |
| 3 | 路径不能逃逸根目录 | 通过；解析后比较根目录与父路径，越界为 `LINK002` |
| 4 | Windows 大小写检查不误报正确路径 | 通过；逐段先精确匹配，再 `casefold` 判断差异 |
| 5 | 围栏屏蔽保持行号 | 通过；被屏蔽行保留换行，测试覆盖两类围栏 |
| 6 | 敏感信息消息不回显原值 | 通过；消息为固定类型描述，测试运行时拼接虚构值并做否定断言 |
| 7 | 读取失败不产生 traceback | 通过；聚合层转换为 `IO002`，CLI 测试确认无 traceback |
| 8 | 错误排序使用数字行号 | 通过；`_error_sort_key` 把行号转换为 `int` |
| 9 | README 不夸大能力 | 通过；明确否定外链、锚点、完整 YAML/Markdown、安全审计和替代人工审查 |
| 10 | 没有 CI、第三方依赖或自动修复 | 通过；第三方导入 0，白名单内无 CI/配置/依赖文件，CLI 只读 |

## 9. 来源、版本、环境与证据等级

- **权威来源：** `AGENTS.md`、`CODEX-PRD.md`、`AI-CODEX-RULES.md`、P0-04 任务包、批准设计、批准实施计划、P0-03 验收报告、模板 README、执行报告模板和任务看板。
- **来源核验日期：** 2026-07-11。
- **版本入口：** 本任务运行版本由批准任务包固定为 Python 3.11.15；不修改软件版本基线。
- **验证环境：** Windows PowerShell，Python 3.11.15，Git，`main` 分支，固定本地仓库。
- **最高证据等级：** 环境验证，适用于测试、编译、CLI、真实仓库、双运行和 Git 范围。
- **证据限制：** 本地机械测试不证明完整 Markdown/YAML 兼容性、安全审计、技术真实性、教学质量、外链或锚点正确性，也不替代 P0-04B 与主会话验收。

## 10. 异常、权限、风险与未决问题

- **异常：**
  1. 初次统计必读文件大小的只读 PowerShell 命令因管道语法错误退出 1；修正后成功，不影响必读文件全文读取或交付物。
  2. 仓库持续提示无法读取用户级 `C:\Users\beiyue\.config\git\ignore`；显式状态、白名单与提交范围均正常。
  3. 任务 7 首次真实仓库测试将设计说明行内代码 `[文本](目标)` 误识别为链接；先增加失败回归测试，再在白名单实现内屏蔽行内代码，最终 34/34 通过，未修改设计说明。
  4. 双运行捕获时 PowerShell 对合并的原生输出显示乱码，但单次 CLI 中文输出正常，两次捕获字符串仍逐字符一致；退出码均为 0。
  5. Git 暂存提示 LF 将在工作区被转换为 CRLF；已暂存内容通过空白检查，提交后测试与真实仓库复验通过。
  6. 首次组合报告字段与格式检查错误读取了 PowerShell cmdlet 后的空 `$LASTEXITCODE`，后续检查未在该次命令中执行；改用显式缺失计数后完整重跑，14/14 字段、格式和单文件范围均通过。
- **权限请求：** 第一次 `git add` 因沙箱不能创建 `.git/index.lock` 失败；随后获得仅用于本地暂存/提交 Git 元数据的批准并成功。未请求网络或白名单外文件写权限。
- **风险：** 受控 YAML/Markdown 子集与高置信度 Secret 模式会主动拒绝复杂结构，也可能漏掉未知凭据格式；README 已明确边界，P0-04B 应重点复核假阳性、假阴性、路径与脱敏。
- **未决问题：** P0-04 范围内无实现未决问题；仍需主会话范围初审、P0-04B 独立审查和最终验收。
- **计划偏离：** 精确白名单检查为展开 Git 默认折叠的未跟踪目录，使用 `git status --short --untracked-files=all`；判定逻辑、预期和白名单未改变。除此之外无计划偏离。

## 11. 远程操作确认

| 操作 | 是否执行 | 授权依据或未执行确认 |
|---|---|---|
| push | 否 | 未授权且未执行 |
| 创建 Pull Request | 否 | 未授权且未执行 |
| 发布网站或制品 | 否 | 未授权且未执行 |
| 修改 GitHub 设置 | 否 | 未授权且未执行 |

## 12. 状态建议与后续门禁

- **建议状态：** `submitted`。
- **建议依据：** 四个白名单文件齐备，正确红灯、34/34 绿灯、33/33 行为映射、编译、真实仓库、双运行、依赖、白名单、格式和人工检查均有实际证据；实现提交范围精确。
- **后续门禁：** 主会话先独立确认范围与技术证据，再决定是否解锁 P0-04B；本执行会话不启动 P0-04B、P0-05 或 P0-06。
- **责任边界：** 本报告只建议 `submitted`，未宣布 `accepted`；独立审查和最终验收由 `AGENTS.md` 与任务包指定角色完成。
