# 内容审计

本目录保存阶段性教学、技术、来源、版本、链接和格式审计。记录名称应包含 Phase 或审计范围及日期，内部引用具体任务、章节和提交。

## 允许编辑者

主会话制定审计范围并维护结论；被明确授权的独立审查会话可以创建指定审计记录。普通内容执行会话不得顺手修改审计结论。

## 必需字段

审计记录必须包含编号或范围、抽样方法、基线版本、检查项、证据路径、问题级别、修复建议、复验命令、结论和日期。

## 状态责任

审查会话只提出 `recommend-accept`、`recommend-rework` 或 `blocked` 等建议；主会话负责触发返工并更新正式状态。
