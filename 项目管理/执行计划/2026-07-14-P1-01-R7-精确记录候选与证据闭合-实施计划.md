# P1-01-R7 精确记录候选与证据闭合实施计划

> **面向 AI 代理的工作者：** 必需子技能：在新的独立执行会话中使用 executing-plans 逐任务实施；处理 R6 退回反馈前使用 receiving-code-review，修改产品前使用 test-driven-development，出现非预期失败时使用 systematic-debugging，提交与完成声明前使用 verification-before-completion。不得由 Phase 1 主会话或审查会话实施产品修改。

**目标：** 用带所有者和章节关系的精确记录候选关闭 R6 的跨项目漏诊与不稳定身份级联，保留缺失分隔行后的首行直接诊断，并把 SCC 线性后处理证明提升为受控比较、AST 与输出基数的组合证据。

**架构：** 受控表解析区分合法分隔行、分隔符形态错误和缺失分隔行后的首个数据候选。项目行形成 `_ProjectRecordCandidate`，锚点查询形成带来源和章节关系的 `_AnchorReference`；合法状态优先，非法候选只解释与自身所有者、预期键或已解析章节关系精确匹配的级联。总视图身份无法稳定确定时仅关闭 D=V，其他闭合继续运行。

**技术栈：** Python 3.11.15 标准库、`unittest`、`dataclasses`、`ast`、`hashlib`、Git；不增加第三方依赖、新模块、schema、CI 或配置。

---

## 文件结构与职责

- 修改 `脚本资源/项目校验/tests/test_validate_repository.py`：新增恰好 4 个测试方法并强化现有 SCC 方法，总数固定为 133。
- 修改 `脚本资源/项目校验/validate_repository.py`：修复分隔行候选保留，增加精确项目候选和带上下文的锚点引用，收窄闭合隔离。
- 修改 `脚本资源/项目校验/README.md`：同步分隔行恢复、精确记录候选、身份不稳定 D=V 和 SCC 组合证据合同。
- 创建 `项目管理/执行报告/P1-01-R7-执行报告.md`：记录固定对象、红绿灯、验证、风险和状态建议。

本任务只有两个交付提交：产品三文件一次提交，执行报告一次提交。为保持项目审计链，测试红灯阶段不创建中间提交，不 amend、rebase 或 reset。

### 任务 1：启动门禁与四项写前复现

**文件：**

- 只读：R7 任务包、设计、实施计划、R6 执行报告、三个产品文件、任务看板。

- [ ] **步骤 1：确认固定仓库状态**

运行：

```powershell
git status --short
git branch --show-current
git remote get-url origin
git log -10 --format="%H %s"
python --version
git rev-parse HEAD:'脚本资源/项目校验/README.md'
git rev-parse HEAD:'脚本资源/项目校验/validate_repository.py'
git rev-parse HEAD:'脚本资源/项目校验/tests/test_validate_repository.py'
```

预期：工作区干净、`main`、origin 为 `https://github.com/beiyue1988/cloud-ops-roadmap.git`、Python 3.11.15；三个产品 blob 依次为 `fe3f16dc9d2450e191ae49e689643450d7160abf`、`8ae5ca3cf8593d703d4f68fe4ddf669534db90e4`、`33d5602d98ef3b3a1c50015638bae06e10433e63`。R6 产品、报告、退回和 R7 设计提交均在当前历史中。

- [ ] **步骤 2：运行 129 项基线和仓库门禁**

```powershell
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
python -B '脚本资源/项目校验/validate_repository.py' .
python -B '脚本资源/项目校验/validate_repository.py' . --outline-gate partial
```

预期：129/129、失败 0、错误 0、skip 0；默认和 `partial` 退出 0。不得产生 `__pycache__`。

- [ ] **步骤 3：在系统临时目录复现四项退回问题**

使用现有 `OutlineGateTests` 夹具分别构造：

1. 删除阶段表分隔行，首个数据行标题设为 `<br>`；实际只有分隔 `OL002`，缺少标题 `OL005`。
2. 项目 01 总视图键改为 `PRJ-02-M01`，同时删除项目 02 合法总视图行；项目 02 的真实 D=V `OL011` 被吞掉。
3. 项目 01 总视图键改为 `` `BROKEN` ``；实际出现直接 `OL010` 和派生项目 01 D=V `OL011`。
4. 检查 SCC 现有测试只有 3 节点输入和直接 `sorted(...)` AST 检查，没有受控比较次数。

所有夹具必须位于 `TemporaryDirectory`，退出上下文后确认清理；不得向仓库写探针。

### 任务 2：强化 SCC 对照并增加四项红灯

**文件：**

- 修改：`脚本资源/项目校验/tests/test_validate_repository.py:922-1291`
- 保持不变：README blob `fe3f16dc...`、实现 blob `8ae5ca3c...`

- [ ] **步骤 1：强化现有 SCC 方法，先取得绿色结构对照**

保留方法名 `test_catalog_cycles_has_no_post_normalization_comparison_sort`。方法内使用以下比较类型和 256 节点反序稀疏单环：

```python
class ComparableId(str):
    comparisons = 0

    def __lt__(self, other: object) -> bool:
        type(self).comparisons += 1
        return str.__lt__(self, other)

nodes = [ComparableId(f"N-{index:03d}") for index in range(256)]
graph = {
    node: (nodes[(int(node[-3:]) + 1) % len(nodes)],)
    for node in reversed(nodes)
}
ComparableId.comparisons = 0
first = validator._catalog_cycles(graph)
first_comparisons = ComparableId.comparisons
ComparableId.comparisons = 0
second = validator._catalog_cycles(graph)
second_comparisons = ComparableId.comparisons
```

精确断言：两次结果相等；恰好一个分量；成员 256 个且唯一；首成员为 `N-000`；两次比较次数都等于 255。解析 `_catalog_cycles` AST，并执行以下冻结摘要断言：

```python
function_dump = ast.dump(function_node, include_attributes=False)
self.assertEqual(
    hashlib.sha256(function_dump.encode("utf-8")).hexdigest(),
    "705deb57eb4a09333016b64c8d451d2df363fb74daa073fdafc000033d98d604",
)
```

该 R6 固定值不得随实现修改。继续以 `reverse` 赋值为归一化边界，拒绝边界后的直接 `sorted`、`min`、`max`，方法 `.sort()` 和 `heapq` 属性调用。完整 AST 摘要会使通过内部辅助函数隐藏排序的修改同样失败。不得使用耗时阈值。

运行：

```powershell
python -B -m unittest test_validate_repository.OutlineGateTests.test_catalog_cycles_has_no_post_normalization_comparison_sort -v
```

工作目录切换到 `脚本资源/项目校验/tests` 后运行，或从仓库根目录使用 discovery 过滤等价执行。预期：固定 R6 实现上 PASS，比较次数 255。

- [ ] **步骤 2：增加缺失分隔行红灯**

新增精确方法：

```python
def test_partial_missing_separator_preserves_first_data_row_diagnostics(self) -> None:
```

方法包含两个 `subTest`：

- `missing-separator`：两行阶段数据为 `00.01`（标题 `<br>`）和合法 `00.02`，删除分隔行；断言分隔 `OL002`、标题 `OL005`，并负断言包含“应连续为 00.01”的后续行 `OL003`。
- `malformed-separator-shape`：使用列数正确、全部单元格只含冒号和 1–2 个连字符的非法分隔行，实际数据从下一行开始；断言分隔 `OL002`，且分隔行本身不产生章节字段诊断。

- [ ] **步骤 3：增加总视图 owner 作用域红灯**

新增精确方法：

```python
def test_complete_invalid_view_key_does_not_suppress_foreign_project_gap(self) -> None:
```

调用 `build_complete()`；把项目 01 总视图行的 `PRJ-01-M01` 改为 `PRJ-02-M01`，删除原项目 02 总视图行，保留项目 02 README 合法定义。断言项目 01 行直接 `OL010` 恰好一条，并断言项目 02 `PRJ-02-M01 未出现在贯穿项目视图` 的 `OL011` 恰好一条。

- [ ] **步骤 4：增加项目侧章节关系作用域红灯**

新增精确方法：

```python
def test_complete_invalid_project_key_scopes_chapter_reference_by_relation(self) -> None:
```

方法包含两个 `subTest`，均先固定项目 01 第二行使用错误 `PRJ-02-M02`、章节集合为 `08.01`，并令阶段 `08.01` 引用该键：

- `different-chapter`：阶段 `13.01` 也引用 `PRJ-02-M02`；总视图增加合法“项目 02 / PRJ-02-M02 / 13.01”记录；项目 02 README 不定义 M02。断言项目 01 错误行直接 `OL010`；`08.01` 不产生该键的缺定义/未列回级联；`13.01` 必须产生缺定义 `OL010` 和项目未列回 `OL011`。
- `same-chapter-different-source`：总视图增加合法“项目 02 / PRJ-02-M02 / 08.01”记录；项目 02 README 不定义 M02。断言阶段 `08.01` 的 `source=chapter` 自身级联被隔离，但总视图文件路径上的 `source=view` 缺定义 `OL010` 恰好保留。

所有断言不得依赖诊断总数，而要绑定路径、规则、键、章节和引用来源。两个子例必须使用独立 `TemporaryDirectory`，不得共享变异夹具。

- [ ] **步骤 5：增加不稳定身份红灯**

新增精确方法：

```python
def test_complete_unstable_view_identity_stops_only_view_comparison(self) -> None:
```

调用 `build_complete()`；把项目 01 总视图键替换为 `` `BROKEN` ``；把阶段 `13.01` 的项目锚点改为 `—`，从而让项目 02 M01 继续定义 `13.01` 但章节不反向引用。断言：项目 01 行直接 `OL010` 存在；不存在项目 01 M01“未出现在贯穿项目视图”的 D=V `OL011`；项目 02 M01 列出 `13.01` 但章节未反向引用的独立 `OL011` 仍存在。

- [ ] **步骤 6：运行红灯并分类**

```powershell
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
```

预期：共 133 个唯一测试方法；新增四项因目标缺陷失败；强化 SCC 方法通过；其余 128 个既有方法通过；错误 0、skip 0。失败不得来自语法、导入、路径、夹具或环境。此时只能有测试文件改变。

### 任务 3：保留缺失分隔行后的首个数据候选

**文件：**

- 修改：`脚本资源/项目校验/validate_repository.py:872-958`
- 测试：`test_partial_missing_separator_preserves_first_data_row_diagnostics`

- [ ] **步骤 1：增加分隔符形态判定**

在 `_split_table_cells` 附近增加内部帮助函数，语义固定为每个单元格只含可选冒号和至少一个连字符：

```python
def _looks_like_separator_row(cells: tuple[str, ...]) -> bool:
    return bool(cells) and all(
        re.fullmatch(r":?-+:?", cell) is not None for cell in cells
    )
```

- [ ] **步骤 2：区分合法分隔、非法分隔形态和首个数据候选**

在 `_parse_controlled_table` 中先计算 `separator_valid`。若非法，始终报告分隔 `OL002` 并令 `structure_valid=False`：

```python
separator_is_data = (
    separator is not None
    and len(separator) == len(expected_headers)
    and not _looks_like_separator_row(separator)
)
rows: list[_TableRow] = []
position = 1
if separator_is_data:
    rows.append(_TableRow(separator, separator_index + 1, position))
    position += 1
index = separator_index + 1
```

合法分隔或非法分隔符形态从下一行以位置 1 开始；`separator_is_data` 时保存该行为位置 1，后续行从位置 2 开始。保持整表 `structure_valid=False`，所以这些位置只用于保留原始数据顺序，不触发连续性或集合推导。

- [ ] **步骤 3：运行目标和相关旧测试**

运行缺失分隔行目标、`test_partial_rejects_invalid_separator`、阶段坏表直接诊断和完整 discovery。预期：目标转绿，非法分隔旧测试继续通过，其他三项新功能测试仍红。

### 任务 4：建立项目候选与锚点引用上下文

**文件：**

- 修改：`脚本资源/项目校验/validate_repository.py:257-288, 1707-2003`
- 测试：三项项目闭合新测试及 R6 既有项目测试

- [ ] **步骤 1：替换全局排除结构**

删除 `_ProjectRecordExclusion` 和 `project_record_exclusions`，增加两个冻结内部结构：

```python
@dataclass(frozen=True)
class _ProjectRecordCandidate:
    side: str
    owner: int | None
    raw_key: str | None
    expected_key: str | None
    chapters: frozenset[str] | None
    path: str
    line: int
    closure_fields_valid: bool
    identity_stable: bool


@dataclass(frozen=True)
class _AnchorReference:
    anchor: str
    path: str
    line: int
    source: str
    owner: int | None
    related_chapters: frozenset[str] | None
```

`closure_fields_valid` 只表示闭合相关的所有者、里程碑键和章节集合资格；能力结果、证据类型等非闭合文本仍产生直接诊断，但不得无关扩大闭合失资格。`source` 只允许内部冻结值 `chapter`、`view`、`other`。

`_OutlineState` 使用：

```python
anchor_references: list[_AnchorReference] = field(default_factory=list)
project_record_candidates: list[_ProjectRecordCandidate] = field(default_factory=list)
```

- [ ] **步骤 2：统一锚点引用登记**

增加内部登记函数，所有登记点不得继续直接拼三元组：

```python
def _record_anchor_reference(
    state: _OutlineState,
    anchor: str,
    path: str,
    line: int,
    *,
    source: str,
    owner: int | None = None,
    related_chapters: frozenset[str] | None = None,
) -> None:
    state.anchor_references.append(
        _AnchorReference(anchor, path, line, source, owner, related_chapters)
    )
```

所有调用点必须显式提供来源：阶段章节引用使用 `source="chapter"` 并以当前 `chapter_id` 形成单元素 `related_chapters`；有效项目总视图记录使用 `source="view"`、项目列 owner 和本行章节集合；其他路线、索引、检查点或 LAB 引用使用 `source="other"`，无法提供关系时传 `None`。检查点和 LAB 行为保持。

- [ ] **步骤 3：让项目表和总视图产生候选**

项目 README 和总视图中每个表结构合格的记录都追加候选，明确保存 `closure_fields_valid=True/False`。项目 README 候选：`owner=project`，`expected_key` 来自物理位置，`chapters` 仅在章节集合解析成功时保存。总视图候选：`owner` 来自项目列，`expected_key` 只在 owner 与 `MCC` 后缀都稳定时生成。合格候选继续登记有效锚点、映射、位置或视图记录；非法候选不得登记这些有效状态。表结构不合格时不产生可解释闭合的候选。

能力结果、证据类型等非闭合文本错误不得把 `closure_fields_valid` 置为 false。两个隔离谓词都必须防御性拒绝 `closure_fields_valid=True`，即使调用方误传合法候选也不能用它解释缺口。

- [ ] **步骤 4：停止身份不稳定视图的 D=V**

`_validate_project_view` 维护局部 `milestones_comparable = table.structure_valid`。只要表结构合格的非法候选 `identity_stable=False`，将其置为 false；函数返回该值。字段直接诊断和其他行验证继续执行。

运行不稳定身份目标。预期：该目标转绿；总视图 owner 碰撞和项目侧关系测试仍红。

### 任务 5：按所有者、预期键和章节关系收窄闭合查询

**文件：**

- 修改：`脚本资源/项目校验/validate_repository.py:2005-2087`
- 测试：全部项目闭合新旧测试

- [ ] **步骤 1：实现项目侧引用隔离谓词**

内部谓词接收完整 `_AnchorReference`：

```python
def _candidate_explains_project_reference(
    candidate: _ProjectRecordCandidate,
    reference: _AnchorReference,
) -> bool:
    if candidate.side != "project" or candidate.closure_fields_valid:
        return False
    reference_owner = reference.owner or _project_key_owner(reference.anchor)
    expected_match = (
        candidate.owner is not None
        and reference_owner == candidate.owner
        and reference.anchor == candidate.expected_key
    )
    raw_relation_match = (
        reference.source == "chapter"
        and candidate.raw_key is not None
        and reference.anchor == candidate.raw_key
        and candidate.chapters is not None
        and reference.related_chapters is not None
        and not candidate.chapters.isdisjoint(reference.related_chapters)
    )
    return expected_match or raw_relation_match
```

只有合法定义不存在时才调用此谓词。合法 `anchor_definitions` 和 `project_mappings` 始终优先。

- [ ] **步骤 2：实现总视图缺项隔离谓词**

```python
def _candidate_explains_missing_view(
    candidate: _ProjectRecordCandidate,
    milestone: str,
) -> bool:
    return (
        candidate.side == "view"
        and not candidate.closure_fields_valid
        and candidate.identity_stable
        and candidate.owner is not None
        and candidate.expected_key == milestone
        and _project_key_owner(milestone) == candidate.owner
    )
```

不得用 `raw_key` 解释另一 owner 的 D=V 缺项。

- [ ] **步骤 3：改写闭合循环**

锚点不存在和章节未列回循环使用 `_AnchorReference` 的 source/owner/章节关系，只在没有合法定义或映射时检查项目侧非法候选。`raw_key` 关系只允许解释 `source="chapter"`；`source="view"` 和 `source="other"` 永远不能走该分支。项目映射缺少总视图记录时，只检查 `_candidate_explains_missing_view`。删除 `_project_record_is_excluded` 及任何只按 `side + key` 的全局跳过。

- [ ] **步骤 4：运行全部项目组合矩阵**

运行三项 R7 项目目标（第三项含不同章节和同章节不同来源两个子例）、R6 的 `test_complete_wrong_project_milestone_emits_only_direct_error`、非法兄弟里程碑、PRJ-05 独立缺项、缺/坏总视图、合法 complete 夹具。预期：全部通过，合法 complete 错误 0。

### 任务 6：同步 README 合同

**文件：**

- 修改：`脚本资源/项目校验/README.md`

- [ ] **步骤 1：更新受控表结构说明**

明确缺失分隔行时，列数正确且非分隔符形态的首行仍作为数据候选执行直接诊断；非法分隔符形态不伪装成数据；整表仍不参加连续性、登记或闭合。

- [ ] **步骤 2：更新项目候选与不稳定身份说明**

明确非法记录用来源侧、所有者、预期键和已解析章节关系隔离自身级联；错误原始键不形成全局豁免；无法得到稳定视图身份时只停止 D=V。

- [ ] **步骤 3：更新 SCC 证据说明**

保留一次归一化 + 线性装桶承诺，补充受控比较、AST 和输出基数作为机械证明；不扩大为完整 CommonMark、HTML、教学质量或动态版本承诺。

### 任务 7：完整验证与产品提交

**文件：**

- 修改并提交：README、实现、测试三个产品文件

- [ ] **步骤 1：运行目标、完整和既有行为验证**

必须运行：4/4 新目标；强化 SCC 方法；完整 discovery 133/133；按新增四个方法名过滤后其余 129 个方法全部通过；失败 0、错误 0、skip 0。

- [ ] **步骤 2：运行 CLI 与仓库矩阵**

必须验证：默认两次逐字符一致；`--help`；非法 gate 退出 2；缺失 root 唯一 `IO001` 且退出 1；`partial` 退出 0；`catalogs` 退出 1、恰好 21 个 `OL001`、无 traceback。

- [ ] **步骤 3：运行结构、API 与卫生矩阵**

必须验证：256 节点两次各 255 比较；100/200/300 密集 SCC；5000 深链；2079 单环；2079 稠密 DAG；自环、重复边、多分量；八个公开函数名称与签名不变；第三方 import 0；正式非 README Markdown 0；`__pycache__` 0；临时目录清理；`git diff --check` 退出 0。

- [ ] **步骤 4：确认精确产品范围**

```powershell
git status --short
git diff --check
git diff --name-only
```

预期：只有 README、实现、测试三个产品文件。

- [ ] **步骤 5：创建产品提交**

```powershell
git add -- '脚本资源/项目校验/README.md' '脚本资源/项目校验/validate_repository.py' '脚本资源/项目校验/tests/test_validate_repository.py'
git commit -m 'fix: scope outline record candidates'
```

### 任务 8：执行报告与报告提交

**文件：**

- 创建：`项目管理/执行报告/P1-01-R7-执行报告.md`

- [ ] **步骤 1：按固定结构记录证据**

报告必须包含：角色与范围、固定提交/blob、四项写前复现、SCC 绿色结构对照、四项正确红灯、分隔行分类、项目候选、锚点引用上下文、不稳定身份 D=V、产品提交完整哈希、133/133 与既有 129 行为、CLI、SCC 规模矩阵、README、依赖、公开 API、精确范围、正式内容、缓存、清理、异常、风险、未决、远程确认和状态建议。

- [ ] **步骤 2：验证报告和提交范围**

```powershell
git status --short
git diff --check
git diff --name-only
```

预期：产品提交后只剩 `项目管理/执行报告/P1-01-R7-执行报告.md` 未提交。

- [ ] **步骤 3：创建报告提交**

```powershell
git add -- '项目管理/执行报告/P1-01-R7-执行报告.md'
git commit -m 'docs: report P1-01-R7 scoped candidates'
git status --short
```

预期：第二提交只含执行报告，最终工作区干净。禁止 push、PR、发布、标签、Release、GitHub 设置或云资源修改。
