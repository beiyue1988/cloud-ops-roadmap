# P1-02A-M1 组 A 历史直接前置约简实施计划

> **面向 AI 代理的工作者：** 必需子技能：使用 `subagent-driven-development` 逐任务实现本计划。步骤使用复选框（`- [ ]`）跟踪进度。具体产品必须由新的独立执行会话完成，主会话只签发、审查和验收。

**目标：** 在不改变阶段 01/02 章节设计与学习可达关系的前提下，删除四条已证明存在替代路径的历史直接前置边，使 00–04 累计章节图完成传递约简。

**架构：** Markdown 阶段清单继续作为唯一章节目录源。本任务只改两个已接受阶段 README 的四个“直接前置”单元格；新的执行报告保存差异、图审计和验证证据，原执行报告保持历史原样。产品完成后先独立审查，再由 Phase 1 主会话最终验收。

**技术栈：** Markdown、Python 3.11 标准库、仓库校验器、PowerShell、Git。

---

**状态：** 已批准

**计划日期：** 2026-07-15

**批准规格：** [P1-02A-M1 组 A 历史直接前置约简设计说明](2026-07-15-P1-02A-M1-组A历史前置约简-设计说明.md)

## 1. 文件结构与职责

| 文件 | 操作 | 单一职责 |
|---|---|---|
| `知识库/01-计算机与服务器基础/README.md` | 修改 | 删除 `01.16 -> 01.03`，其余内容冻结 |
| `知识库/02-网络基础/README.md` | 修改 | 删除阶段 02 的三条指定传递冗余边，其余内容冻结 |
| `项目管理/执行报告/P1-02A-M1-执行报告.md` | 创建 | 保存启动门禁、精确差异、累计图审计、验证、风险和状态建议 |

任务包、设计、计划和任务看板由主会话在执行前固定；执行会话不得修改这些治理文件。

## 2. 任务 1：启动门禁与基线快照

**文件：** 只读全部必读文件，不写入仓库。

- [ ] **步骤 1：确认 Git 与任务签发基线**

运行：

```powershell
git branch --show-current
git rev-parse HEAD
git status --short
git remote get-url origin
git log -1 --format=%H -- '项目管理/任务包/P1-02A-M1-约简阶段01与02历史直接前置.md'
git merge-base --is-ancestor f9cc8d5d05ea287d7187f2459c45e93b2fab38b2 HEAD
git merge-base --is-ancestor e9dab91ca6605fccb951ac0599fc44c041b39126 HEAD
git merge-base --is-ancestor 5b8c8379df6c3de76f27e03fe9fcb7fcc0d1da83 HEAD
git merge-base --is-ancestor eb65347ae21634bab556936c546e7a49d7f8e599 HEAD
```

预期：分支 `main`；启动 `HEAD` 等于任务包最近提交；工作区无输出；origin 为既有 GitHub 地址；四个固定提交均在历史中。

- [ ] **步骤 2：完整阅读执行边界**

完整阅读 `AGENTS.md`、`CODEX-PRD.md`、本任务包、批准设计、本实施计划、Phase 1 总设计与总实施计划、`AI-CODEX-RULES.md`、校验器 README、阶段 00–04 README、P1-02-01/02 原任务包与原执行报告、P1-02-04-R1 执行报告和任务看板。发现冲突立即停止。

- [ ] **步骤 3：运行写前机械门禁**

固定解释器：

```powershell
$py = 'C:\Users\beiyue\AppData\Local\Temp\cloud-ops-roadmap-uv-0.11.28\python\cpython-3.11.15-windows-x86_64-none\python.exe'
& $py --version
& $py -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py'
& $py -B '脚本资源/项目校验/validate_repository.py' .
& $py -B '脚本资源/项目校验/validate_repository.py' . --outline-gate partial
```

预期：Python 3.11.15；133/133；默认错误 0；partial 为 5 阶段、108 章、163 依赖、错误 0。

- [ ] **步骤 4：保存只读基线证据**

用 `git show HEAD:<path>` 或等价只读方式保存两份 README 的基线文本/哈希，供产品提交前后做字段级比较。不得创建仓库内临时文件。

## 3. 任务 2：精确修改两个章节清单

**文件：**

- 修改：`知识库/01-计算机与服务器基础/README.md`
- 修改：`知识库/02-网络基础/README.md`

- [ ] **步骤 1：修改阶段 01 唯一单元格**

把 `01.16` 的直接前置从：

```text
01.03, 01.05, 01.08, 01.10, 01.12, 01.14
```

改为：

```text
01.05, 01.08, 01.10, 01.12, 01.14
```

- [ ] **步骤 2：修改阶段 02 三个单元格**

精确改为：

```text
02.14: 02.04
02.21: 02.20
02.23: 02.11, 02.22
```

不得修改其他章节行、其他八列或表外文本。

- [ ] **步骤 3：核对字段级和边集合差异**

以任务签发提交为基线解析两张九列表。预期：只有 `01.16`、`02.14`、`02.21`、`02.23` 的第 6 列发生变化；边集合只有以下四条删除，新增集合为空：

```text
01.16 -> 01.03
02.14 -> 02.02
02.21 -> 02.15
02.23 -> 02.20
```

## 4. 任务 3：验证累计图并提交产品

**文件：** 只验证并提交任务 2 的两份 README。

- [ ] **步骤 1：独立审计 00–04 累计图**

使用只读 Python 3.11 临时审计（如需临时脚本，只能置于系统临时目录并在完成后删除）：解析 00–04 五张九列表，建立“后学章节指向直接前置”的图；检查节点存在、自引用、前向引用和环；对每条直接边临时移除该边，检查是否仍存在到同一前置的替代路径。

预期精确结果：

```text
stages=5
chapters=108
edges=159
stage01_edges=22
stage02_edges=29
missing=0
self_edges=0
forward_edges=0
cycles=0
transitive_redundant_edges=0
```

- [ ] **步骤 2：运行产品门禁**

```powershell
& $py -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py'
& $py -B '脚本资源/项目校验/validate_repository.py' .
& $py -B '脚本资源/项目校验/validate_repository.py' . --outline-gate partial
git diff --check
git status --short
```

预期：133/133；默认错误 0；partial 5/108/159、错误 0；状态只列两份授权 README。

- [ ] **步骤 3：核对不变量**

确认阶段 01/02 仍分别为 16/24 章；标签、知识类型、投入、实践锚点和原范围覆盖计数不变；九类正式内容目录非 README Markdown 为 0；原执行报告哈希不变。

- [ ] **步骤 4：创建产品提交**

```powershell
git add -- '知识库/01-计算机与服务器基础/README.md' '知识库/02-网络基础/README.md'
git diff --cached --check
git commit -m "docs: reduce group A prerequisite edges"
git show --check --stat HEAD
```

预期：提交只含两份阶段 README。

## 5. 任务 4：编写并提交执行报告

**文件：**

- 创建：`项目管理/执行报告/P1-02A-M1-执行报告.md`

- [ ] **步骤 1：编写报告**

报告必须记录：执行角色与 `submitted` 建议；启动 HEAD 与任务签发提交；原阶段产品/报告和维护产品提交；精确四删零增；四条替代路径；字段冻结证据；5/108/159、22/29 和全图传递冗余 0；133/133、默认、partial、正式正文、格式与提交范围；原报告的历史地位；异常、风险、未决和远程操作。

- [ ] **步骤 2：重跑全套门禁**

```powershell
& $py -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py'
& $py -B '脚本资源/项目校验/validate_repository.py' .
& $py -B '脚本资源/项目校验/validate_repository.py' . --outline-gate partial
git diff --check
git show --check HEAD
git status --short
```

预期：测试 133/133；默认错误 0；partial 5/108/159、错误 0；产品提交检查退出 0；状态只列新报告。

- [ ] **步骤 3：创建报告提交**

```powershell
git add -- '项目管理/执行报告/P1-02A-M1-执行报告.md'
git diff --cached --check
git commit -m "docs: report P1-02A-M1 prerequisite reduction"
git show --check --stat HEAD
git status --short
```

预期：报告提交只含一个新报告，最终工作区干净。

## 6. 任务 5：交付与审查检查点

- [ ] **步骤 1：提交完成回复**

回复必须给出建议状态、产品/报告完整提交、四删零增、替代路径、159 条累计边、字段与标签/锚点不变量、全部验证、异常/风险/未决、远程操作。不得宣布 `accepted` 或更新看板。

- [ ] **步骤 2：进入独立审查**

主会话固定两个提交后，另开新的独立审查会话复算差异与全图。审查不修改产品；如有问题，由主会话另签返工任务。

## 7. 计划停止条件

任一门禁与设计不符、出现四删以外差异、替代路径失效、需要新增/重排边、测试或校验失败、存在未知工作区修改、需要白名单外写入或外部写操作时立即停止并回报证据。禁止 amend、rebase、reset、push、PR、发布、凭据写入或安全控制变更。
