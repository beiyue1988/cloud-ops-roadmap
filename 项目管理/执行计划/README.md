# 执行计划

本目录保存由主会话制定、供独立执行会话分任务落实的书面计划。计划文件使用 `YYYY-MM-DD-主题-实现计划.md` 命名，内部任务使用项目统一编号。

## 允许编辑者

只有主会话可以创建或修改执行计划；执行会话必须读取计划中对应任务，但不得修改复选框、顺序或全局规则。

## 必需字段

计划必须包含目标、架构或方法、执行规则、任务依赖、精确文件范围、分步操作、验证命令、预期输出、提交要求和停止条件。

## 状态责任

计划中的完成标记和任务排序由主会话在验收后维护。执行会话只在执行报告中记录实际结果和建议状态。
