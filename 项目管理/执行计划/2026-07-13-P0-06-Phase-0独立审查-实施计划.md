# P0-06 Phase 0 独立审查实施计划

> **面向 AI 代理的工作者：** 必需子技能：使用 `executing-plans` 按任务顺序执行本计划。步骤使用复选框（`- [ ]`）跟踪进度；不得委派、跳步、修复被审对象或自行扩大白名单。

**目标：** 对固定 Phase 0 交付快照和当前集成状态进行一次跨治理、信息架构、规则、模板、校验器、版本、来源、许可与安全边界的独立阶段审查，形成可供主会话最终裁决的单一报告。

**架构：** 使用“固定截止 + 启动 HEAD + 允许治理差异”确定审查对象；用 14 组自动证据证明可机器复验的事实，用八个审查域和 24 项人工矩阵判断跨文件语义；动态版本事实在审查日重新读取官方直接来源。审查会话只提出 `recommend-accept`、`recommend-rework` 或 `blocked`，不修改被审对象、不更新任务状态、不代替主会话验收。

**技术栈：** Markdown、PowerShell、Git、Python 3.11.15 标准库、现有仓库校验器、官方/上游一手资料。

---

**状态：** 已批准

**计划日期：** 2026-07-13

**制定者：** 主会话

**批准设计：** [P0-06 Phase 0 独立审查设计说明](2026-07-13-P0-06-Phase-0独立审查-设计说明.md)

**固定 Phase 0 交付截止：** `1bf564673e68751e6183ab9c2eb4874fda3fbf6e`

## 1. 文件结构与职责

### 创建

| 文件 | 职责 |
|---|---|
| `项目管理/验收记录/Phase-0-独立审查报告.md` | 保存固定审查对象、自动证据、人工判断、动态来源、问题分级、主会话收尾动作与阶段建议结论 |

除该报告和 Git 元数据外，不允许创建或修改其他仓库文件。临时编译缓存、命令输出和探针文件只能进入经过验证的系统临时目录，并在报告提交前清理。

## 2. 固定对象、允许差异与证据层级

### 2.1 固定对象

P0-06 审查的 Phase 0 交付截止固定为：

```text
1bf564673e68751e6183ab9c2eb4874fda3fbf6e
```

执行会话必须记录启动时的实际 `HEAD`，证明固定截止为其祖先，并将两者之间的差异限制为：

```text
项目管理/执行计划/2026-07-13-P0-06-Phase-0独立审查-设计说明.md
项目管理/执行计划/2026-07-13-P0-06-Phase-0独立审查-实施计划.md
项目管理/任务包/P0-06-Phase-0独立审查.md
项目管理/任务看板.md
```

这些文件是 P0-06 治理输入，不是被审 Phase 0 交付成果。若固定截止之后出现任何其他路径，执行会话必须在写报告前停止并交由主会话裁决。

### 2.2 三类证据

1. **当前状态证据：** 审查会话新鲜运行测试、校验器、Git、链接、格式、版本来源和远程检查；
2. **历史链证据：** 审查提交、报告、返工、复审、验收和父链，确认角色与关闭关系；
3. **引用的专项证据：** 对 P0-03 模板逐字段审查、P0-04 历史红灯和异常探针引用已接受报告，只抽查当前 blob 无漂移。

报告必须明确区分以上三类证据，以及审查者推论和未验证边界。

## 3. 执行与独立性规则

1. 工作目录固定为 `C:\Workspace\Projects\cloud-ops-roadmap`；
2. 执行会话必须是未参与 P0-01 至 P0-05 交付、返工或主会话验收的新会话；
3. 分支必须为 `main`，启动工作区必须干净；
4. `origin` 必须精确为 `https://github.com/beiyue1988/cloud-ops-roadmap.git`；
5. P0-05 必须为 `submitted`，P0-06 必须为 `ready`；
6. 完整阅读 `AGENTS.md`、`CODEX-PRD.md`、`AI-CODEX-RULES.md`、批准设计、本计划、P0-06 任务包和直接依赖；
7. 不修改 PRD、AI 规则、模板、校验器、版本基线、ADR、项目记忆、任务看板或历史报告；
8. 不重做没有漂移触发的 P0-03/P0-04 全部历史专项实验；
9. 动态事实使用审查实际日期，不能沿用 P0-05 日期冒充新鲜证据；
10. 远程只允许读取，不允许 push、Pull Request、发布或 GitHub 设置修改；
11. 报告正文不预测或回填报告提交自身哈希；完整报告提交哈希只在最终回复给出；
12. 发现仓库内可修复缺陷时给出 `recommend-rework`，外部关键证据无法取得时给出 `blocked`，不得顺手修复。

## 4. 任务 1：启动门禁、独立性与固定差异

**文件：** 无写入。

- [ ] **步骤 1：声明独立性**

在审查工作记录中明确：当前会话没有参与 P0-01 至 P0-05 的交付物创建、返工或主会话验收。若不满足，立即停止，不创建报告。

- [ ] **步骤 2：确认 Git、环境和任务状态**

运行：

```powershell
git -c core.quotepath=false status --short
git branch --show-current
git remote get-url origin
git rev-parse HEAD
python --version
Select-String -LiteralPath '项目管理\任务看板.md' -Pattern '^\| P0-05 .*`submitted`','^\| P0-06 .*`ready`'
```

预期：工作区无输出；分支为 `main`；远程精确匹配；Python 为 `3.11.15`；两条任务状态各命中一次。用户级 Git ignore 权限警告只可记录为环境噪声，不能掩盖非空工作区或失败退出码。

- [ ] **步骤 3：确认固定截止与启动 HEAD**

运行：

```powershell
$cutoff = '1bf564673e68751e6183ab9c2eb4874fda3fbf6e'
$startHead = git rev-parse HEAD
git cat-file -e "$cutoff^{commit}"
git merge-base --is-ancestor $cutoff $startHead
Write-Output "cutoff=$cutoff"
Write-Output "start_head=$startHead"
```

预期：两个 Git 检查退出 0，并记录两个完整哈希。

- [ ] **步骤 4：验证固定截止后的允许差异**

运行：

```powershell
$allowed = @(
  '项目管理/执行计划/2026-07-13-P0-06-Phase-0独立审查-设计说明.md',
  '项目管理/执行计划/2026-07-13-P0-06-Phase-0独立审查-实施计划.md',
  '项目管理/任务包/P0-06-Phase-0独立审查.md',
  '项目管理/任务看板.md'
)
$changed = @(git -c core.quotepath=false diff --name-only $cutoff $startHead)
$outside = @($changed | Where-Object { $_ -notin $allowed })
$missingGovernance = @($allowed | Where-Object { $_ -notin $changed })
Write-Output "post_cutoff_changed=$($changed.Count)"
Write-Output "post_cutoff_outside=$($outside.Count)"
Write-Output "expected_governance_missing=$($missingGovernance.Count)"
$changed
if ($outside.Count -ne 0 -or $missingGovernance.Count -ne 0) { exit 1 }
```

预期：变更路径精确为四项，越界 0、缺失 0。失败时停止，不把额外改动纳入审查。

## 5. 任务 2：建立任务链、提交链与当前对象矩阵

**文件：** 无写入。

- [ ] **步骤 1：核对任务文件链存在性**

至少核对以下文件组：

```text
P0-01：任务包、执行报告、验收报告
P0-02：任务包、执行报告、验收报告
P0-02A/B/R1：任务包、执行报告、独立审查/复审、验收报告
P0-03/B：任务包、执行报告、独立审查、验收报告
P0-04/B/R1/R2：任务包、三份执行报告、三份独立审查/复审、最终验收报告
P0-05：任务包、执行报告、版本/来源/ADR 交付
P0-06：批准设计、实施计划、任务包
```

对 `项目管理/任务包/`、`项目管理/执行报告/`、`项目管理/验收记录/` 使用显式清单检查，不以目录总数替代缺失检查。预期缺失 0。

- [ ] **步骤 2：复核关键线性提交链**

至少核对下列固定对象为 commit、按表中顺序可达，并抽查父提交：

| 交付 | 固定对象 |
|---|---|
| P0-01 治理/报告 | `a083e5b50b6a19564ac7dffe79b096340b0dd5e3` / `7a4134f7d0bd275abf42026f738a7dd7acd98b3b` |
| P0-02 信息架构/报告 | `cfa63670d546bff30e31bd854c878c124f92a149` / `991d901cbc6821a226a18ea256501412916944a0` |
| P0-02A 最终规则/报告/复审 | `dab65b798a40c594c98d327c672675fd8f7749ec` / `63c86fc10604b6e51fc78ece7766b2afeac40089` / `b61804911af8973d9154a89f221fd71ed42202a3` |
| P0-03 模板/报告/审查 | `9b20ad82212a0c64956cbda8a4bf39160696a04e` / `87080d6c0053a15389be3b75dddc91b22dfc824d` / `a532e2a3cbe3f2a58e8649204def8dc06b4b2e95` |
| P0-04 最终实现/报告/复审 | `5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c` / `44b7fed394f5054ff0b4f76d8c9ef2eaf0b1f2d7` / `1ac95746007de99aa7b479042952fa0794c4a4bb` |
| P0-05 基线/报告/截止 | `5e522d6a472b99ad71ca5bd509d453cfec465155` / `5514b2b73a606e589afc47b5fa436ed1d285d08c` / `1bf564673e68751e6183ab9c2eb4874fda3fbf6e` |

运行 `git cat-file -e <hash>^{commit}`、相邻关键对象的 `git merge-base --is-ancestor`、`git show --no-patch --format=fuller` 和必要的 `git diff-tree`。预期全部可达、顺序不逆转、提交信息与报告一致。

- [ ] **步骤 3：检查空白与提交范围**

运行：

```powershell
git show --check a083e5b50b6a19564ac7dffe79b096340b0dd5e3
git show --check cfa63670d546bff30e31bd854c878c124f92a149
git show --check dab65b798a40c594c98d327c672675fd8f7749ec
git show --check 9b20ad82212a0c64956cbda8a4bf39160696a04e
git show --check 5180f4e34b10bdffc58bb9fd0a79ab1c17f1d41c
git show --check 5e522d6a472b99ad71ca5bd509d453cfec465155
git diff --check $cutoff $startHead
```

预期：当前有效交付和 P0-06 治理差异无空白错误。历史中已记录并由后续提交修正的错误不得被重写；若当前对象仍含错误，列为发现。

- [ ] **步骤 4：人工判断治理链**

确认：执行、独立审查和最终验收角色分离；P0-04-R1 的 `rework` 有 R2 关闭链；执行会话没有自行更新看板或宣布 `accepted`；P0-05 仍等待阶段审查。

## 6. 任务 3：信息架构与 Phase 1 边界

**文件：** 无写入。

- [ ] **步骤 1：检查根目录与阶段目录**

按 PRD 检查根目录职责入口；对 `知识库/` 下 `00-` 至 `20-` 的 21 个阶段目录做精确集合检查。预期缺失 0、越界 0、阶段 README 21。

- [ ] **步骤 2：检查正式内容空边界**

对以下正式内容目录递归检查 Markdown：

```text
学习路线
技术索引
知识库
实验手册
项目实战
故障案例
面试体系
运维思维训练
企业规范
```

除批准的 `README.md` 入口外，非 README 教学文件必须为 0。治理、模板、版本和规则文件不计作教学正文。

- [ ] **步骤 3：检查双导航单一源**

人工阅读学习路线、技术索引、知识库和根 README 的职责说明，确认：学习路线按成长顺序导航、技术索引按技术查询、知识正文只有一个权威来源，不要求 Phase 1 复制正文。

- [ ] **步骤 4：判断 Phase 1 可增量进入**

确认 Phase 1 可创建 README 导航、职业/三个月路线、章节树与依赖关系，而不需要重构 Phase 0 根目录和文件所有权；不得把此判断写成允许批量正文生产。

## 7. 任务 4：AI 内容规则、模板与权威层级

**文件：** 无写入。

- [ ] **步骤 1：检查 AI 规则当前对象无漂移**

运行：

```powershell
git diff --exit-code dab65b798a40c594c98d327c672675fd8f7749ec HEAD -- 'AI-CODEX-RULES.md'
git hash-object 'AI-CODEX-RULES.md'
```

预期：差异退出 0；当前 blob 为 `2aedbd8f855a070b35c802db080c787520889e41`。

- [ ] **步骤 2：人工检查权威层级**

按 `AGENTS.md → CODEX-PRD.md/已批准 ADR/版本基线 → AI-CODEX-RULES.md → 模板 → 任务包` 的职责边界，确认规则没有重建第二份 PRD、版本表或模板全集，并在冲突时要求停止升级。

- [ ] **步骤 3：检查模板集合与无漂移**

运行：

```powershell
git diff --exit-code 9b20ad82212a0c64956cbda8a4bf39160696a04e HEAD -- '模板'
$templateFiles = @(Get-ChildItem -LiteralPath '模板' -File -Filter '*.md')
$templateTypes = @($templateFiles | Where-Object { $_.Name -ne 'README.md' })
Write-Output "template_markdown=$($templateFiles.Count)"
Write-Output "template_types=$($templateTypes.Count)"
```

预期：差异退出 0；Markdown 11、模板类型 10。并比对固定 blob 列表，确认知识、实验、项目、故障、面试、图表、任务包、执行报告、验收报告和 ADR 模板无漂移。

- [ ] **步骤 4：人工抽查高风险字段**

至少抽查：知识模板的九类 `type` 自适应边界；实验的安全/回滚/成本/清理；任务包 11 项门禁；执行报告自身哈希规则；验收报告的独立裁决；ADR 的真实选项与负面后果。不得重新复制 P0-03B 的全部 18 项专项审查。

## 8. 任务 5：仓库校验器当前集成复验

**文件：** 无仓库写入；编译缓存仅进入已验证临时目录。

- [ ] **步骤 1：确认固定 blob 与公开合同**

运行：

```powershell
git hash-object '脚本资源/项目校验/validate_repository.py'
git hash-object '脚本资源/项目校验/tests/test_validate_repository.py'
git hash-object '脚本资源/项目校验/README.md'
```

预期分别为：

```text
74b6c75896117f5680195a72dd010767d345652b
1f429b89d041a2788130f2289f55fb3c0dfe51a5
7476e64b6fd4f5ba3bf0a009e3d267c4c0ad87de
```

确认七个公开函数仍为：`extract_front_matter`、`validate_front_matter`、`validate_local_links`、`validate_placeholders`、`validate_sensitive_information`、`validate_repository`、`main`；顶层导入只来自 Python 标准库与 `__future__`。

- [ ] **步骤 2：运行完整测试**

```powershell
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
```

预期：退出 0，44/44 全部通过。

- [ ] **步骤 3：复核 33 个批准行为映射**

从 `项目管理/任务包/P0-04-仓库校验工具.md` 的“门禁 8：33 个规定行为”提取 33 个方法名，与当前测试文件的测试方法比较：批准方法 33、当前存在 33、缺失 0。新增的四个 R2 测试不替代原映射。

- [ ] **步骤 4：安全编译并清理缓存**

运行：

```powershell
$tempBase = [System.IO.Path]::GetFullPath([System.IO.Path]::GetTempPath())
$tempRoot = Join-Path $tempBase ("cloud-ops-roadmap-p0-06-" + [guid]::NewGuid().ToString('N'))
$resolvedTemp = [System.IO.Path]::GetFullPath($tempRoot)
if (-not $resolvedTemp.StartsWith($tempBase, [System.StringComparison]::OrdinalIgnoreCase)) { throw '临时路径越界' }
New-Item -ItemType Directory -Path $resolvedTemp | Out-Null
$previousPrefix = $env:PYTHONPYCACHEPREFIX
$env:PYTHONPYCACHEPREFIX = Join-Path $resolvedTemp 'pycache'
try {
  python -m py_compile '脚本资源/项目校验/validate_repository.py' '脚本资源/项目校验/tests/test_validate_repository.py'
  if ($LASTEXITCODE -ne 0) { throw "py_compile 失败：$LASTEXITCODE" }
} finally {
  $env:PYTHONPYCACHEPREFIX = $previousPrefix
  $checked = [System.IO.Path]::GetFullPath($resolvedTemp)
  if (-not $checked.StartsWith($tempBase, [System.StringComparison]::OrdinalIgnoreCase)) { throw '拒绝清理越界路径' }
  if (Test-Path -LiteralPath $checked) { Remove-Item -LiteralPath $checked -Recurse -Force }
}
Write-Output "temp_clean=$(-not (Test-Path -LiteralPath $resolvedTemp))"
```

预期：编译退出 0、`temp_clean=True`，仓库内无 `__pycache__` 或 `.pyc` 新文件。

- [ ] **步骤 5：连续运行真实仓库校验**

```powershell
$first = & python -B '脚本资源/项目校验/validate_repository.py' . 2>&1
$firstCode = $LASTEXITCODE
$second = & python -B '脚本资源/项目校验/validate_repository.py' . 2>&1
$secondCode = $LASTEXITCODE
$firstText = [string]::Join("`n", [string[]]$first)
$secondText = [string]::Join("`n", [string[]]$second)
$first
Write-Output "first_exit=$firstCode second_exit=$secondCode equal=$($firstText -ceq $secondText)"
if ($firstCode -ne 0 -or $secondCode -ne 0 -or $firstText -cne $secondText) { exit 1 }
```

预期：两次退出 0、输出完全一致，知识章节为 0、错误为 0；Markdown 与本地链接数字记录实际值，不从本计划抄写旧统计。

## 9. 任务 6：版本、来源、ADR、许可与安全

**文件：** 无写入。

- [ ] **步骤 1：复核版本与来源结构**

检查 `版本记录/软件版本基线.md`、`版本记录/更新日志.md` 和 `参考资料/来源优先级.md`：

- 主表治理状态精确为 7 个 `locked`、12 个 `phase-gated`、1 个 `reference-only`；
- 执行日直接来源记录为 18 条；
- 来源规范覆盖八类事实、七个记录字段、六步冲突流程；
- `source-verified`、`environment-verified`、`not-yet-verified` 没有混用；
- GPU/模型组合仍为阶段门，不提前锁定实例、价格或模型。

- [ ] **步骤 2：复核 ADR-0001 至 ADR-0005**

对五份 ADR 检查状态、背景、至少两个真实选项、决定、正负/中性后果、复审触发和证据。重点确认 ADR-0003 的 VitePress 依赖版本仍阶段门、ADR-0004 的 1.36 教学线、ADR-0005 的 tag/commit/上游/MIT 边界。

- [ ] **步骤 3：在审查日重新核验动态官方事实**

必须直接打开并记录核验日期、支持范围与限制：

1. Rocky Linux 9.8 发布页和版本生命周期页；
2. Kubernetes 1.36 release page、patch release page、container runtime 与 kubeadm 官方页；
3. containerd 官方版本/生命周期页和上游 releases；
4. Calico 官方 system requirements 和 releases；
5. VitePress 官方定位、Front Matter 与搜索页；
6. RuoYi-Vue v3.9.2 release、上游仓库和 LICENSE；
7. 阿里云 GPU ECS 官方产品入口。

若 Kubernetes 1.36.3 或其他新 patch 在审查日已由官方正式发布，而当前基线仍把旧 patch 写为“当前来源核验版本”，记录 `must-fix` 并建议 P0-05 返工。计划日期、搜索摘要或缓存片段不能证明正式发布。

- [ ] **步骤 4：检查许可证与第三方边界**

核对 `LICENSE`、`LICENSE-CONTENT`、`LICENSE-CODE`、`THIRD-PARTY-NOTICES.md`、ADR-0002 和 PRD：原创内容/图表为 CC BY-SA 4.0，原创代码/配置为 Apache License 2.0，第三方材料保持原许可；RuoYi-Vue 不被重新许可，完整上游源码未进入仓库。

- [ ] **步骤 5：检查敏感信息与授权边界**

仓库校验器必须为 0 错误；再对跟踪文件做不回显值的高置信度私钥头/AccessKey 文件名扫描，并人工检查没有密码、Token、Cookie、私钥、云 AccessKey、签名 URL 或个人敏感信息。确认“预算不阻塞”不等于免除计费提示与资源释放，云 GPU 决定不等于提前购买资源。

## 10. 任务 7：远程只读证据与集成就绪度

**文件：** 无写入。

- [ ] **步骤 1：验证只读远程连通**

运行：

```powershell
git ls-remote --heads origin
```

预期：退出 0；可以无分支输出，但必须取得远程命令成功证据。禁止把 `git remote get-url` 成功替代为远程连通。

若因沙箱、网络或权限失败，可以进行最多三次合规尝试，每次记录命令、退出码和原因；不得改用 push、写入凭据、关闭安全控制或修改远程。三次仍无证据时最终建议为 `blocked`。

- [ ] **步骤 2：检查项目记忆与 ADR**

确认 `项目记忆/MEMORY.md` 和 `decisions.md` 没有与 ADR-0001 至 ADR-0005 相反的结论。ADR-0003 至 ADR-0005 尚未加入记忆索引时，记录为 `recommend-accept` 后主会话必须完成的收尾动作，不列为 P0-06 缺陷。

- [ ] **步骤 3：检查看板、入口和未决门**

核对任务看板、版本入口、ADR 索引、README 导航和 `项目记忆/open-questions.md`：唯一未决阶段门为 AI Infra 实验前的 GPU/模型组合；该门不阻塞 Phase 1。

- [ ] **步骤 4：判断 Phase 1 解锁范围**

只有在无 `blocking`、无 `must-fix`、自动证据完整且 24 项人工检查无“不通过”时，才能建议主会话接受 Phase 0。解锁仅限 Phase 1 设计、章节树和任务包制定，不授权直接批量写正文。

## 11. 任务 8：完成 24 项人工审查矩阵

**文件：** 尚不写报告，先形成审查记录。

每项结果只能为 `通过`、`需说明`、`不通过`。`需说明` 必须写影响、责任人和最晚门禁。

| # | 审查域 | 人工检查 |
|---:|---|---|
| 1 | Git 与治理 | 执行、审查、主会话验收角色分离真实 |
| 2 | Git 与治理 | 看板状态、报告与 Git 提交链一致 |
| 3 | Git 与治理 | P0-02A/P0-04 返工历史有清晰关闭链 |
| 4 | 信息架构 | 学习路线与技术索引双导航、知识单一源成立 |
| 5 | 信息架构 | 根目录与阶段目录职责无冲突 |
| 6 | 信息架构 | 无提前正文且 Phase 1 可增量进入 |
| 7 | AI 规则 | 权威层级与冲突升级机制无冲突 |
| 8 | AI 规则 | 研究、背景、写作、可视化、安全、来源和证据边界完整 |
| 9 | AI 规则 | Phase 1 章节树及后续内容任务可被规则约束 |
| 10 | 模板 | 十类模板职责单一且边界清楚 |
| 11 | 模板 | 必填、可选、自适应和非适用规则合理 |
| 12 | 模板 | 高风险字段、执行证据与最终裁决责任分离 |
| 13 | 校验器 | 当前实现、测试、README 与最终固定对象无漂移 |
| 14 | 校验器 | 自动规则没有越权替代教学、来源或安全人工判断 |
| 15 | 校验器 | README 的能力、限制与当前行为一致 |
| 16 | 版本与来源 | 锁定、阶段门、证据和锁定粒度语义准确 |
| 17 | 版本与来源 | 审查日动态事实新鲜且直接来源可追溯 |
| 18 | 版本与来源 | ADR 真实比较选项、取舍、负面后果和复审条件 |
| 19 | 许可与安全 | 双许可证、第三方材料和 RuoYi-Vue 边界一致 |
| 20 | 许可与安全 | 敏感信息、成本和资源释放边界合规 |
| 21 | 许可与安全 | 远程、云与 GPU 授权没有被扩大 |
| 22 | 集成与就绪 | 看板、ADR、版本入口、项目记忆无相反结论 |
| 23 | 集成与就绪 | 主会话最终收尾动作明确且责任正确 |
| 24 | 集成与就绪 | Phase 1 解锁范围没有扩大为正文生产 |

## 12. 任务 9：分级发现并形成唯一报告

**文件：** 创建 `项目管理/验收记录/Phase-0-独立审查报告.md`。

- [ ] **步骤 1：分级所有发现**

使用：

```text
blocking
must-fix
suggestion
passed
```

每个非通过项必须包含：编号、直接证据、影响、责任人、精确文件或外部条件、期望修复/解除条件、复验命令或人工标准。不得用 `suggestion` 隐藏必须修复问题。

- [ ] **步骤 2：选择唯一建议结论**

- `recommend-accept`：无 `blocking`、无 `must-fix`，14 组自动证据通过，24 项人工检查没有“不通过”；
- `recommend-rework`：存在仓库内可修复的 `blocking` 或任一 `must-fix`；
- `blocked`：外部关键证据、远程连通或固定对象无法确定，且无法在只读范围解除。

不得使用 `accepted`、`rework`、`passed` 作为报告的阶段建议结论。

- [ ] **步骤 3：按固定结构写报告**

报告至少包含：

1. 审查对象、日期、独立角色与建议结论；
2. 固定截止、启动 HEAD、允许差异；
3. Phase 0 任务链、提交范围与当前漂移；
4. 14 组自动验证的命令、退出码、关键输出与统计；
5. 八域 24 项人工矩阵；
6. 动态来源、核验日期、适用范围与限制；
7. 四类问题数量、明细与复验要求；
8. 主会话最终验收前的收尾动作；
9. 风险、未决问题、外部阻塞与解除条件；
10. 临时文件清理与远程操作确认；
11. 建议结论及“不宣布 accepted”的声明。

报告正文记录固定截止和启动 HEAD，不记录或预测自身报告提交哈希。

- [ ] **步骤 4：检查报告字段与格式**

运行：

```powershell
Select-String -LiteralPath '项目管理\验收记录\Phase-0-独立审查报告.md' -Pattern '固定.*截止','启动 HEAD','自动验证','24 项','动态','blocking','must-fix','suggestion','passed','主会话','远程','recommend-accept|recommend-rework|blocked'
git diff --check
python -B '脚本资源/项目校验/validate_repository.py' .
```

预期：必需结构全部命中；格式检查和仓库校验退出 0。

## 13. 任务 10：单文件白名单、提交与提交后复验

**文件：** 只处理 `项目管理/验收记录/Phase-0-独立审查报告.md`。

- [ ] **步骤 1：检查提交前白名单**

```powershell
$allowedReport = '项目管理/验收记录/Phase-0-独立审查报告.md'
$changed = @(git -c core.quotepath=false status --short | ForEach-Object { $_.Substring(3).Replace('\','/') })
$outside = @($changed | Where-Object { $_ -ne $allowedReport })
Write-Output "changed=$($changed.Count) outside=$($outside.Count)"
$changed
if ($changed.Count -ne 1 -or $outside.Count -ne 0) { exit 1 }
```

预期：`changed=1 outside=0`。

- [ ] **步骤 2：暂存并检查精确范围**

```powershell
git add -- '项目管理/验收记录/Phase-0-独立审查报告.md'
git -c core.quotepath=false diff --cached --name-only
git diff --cached --check
```

预期：暂存区精确一个报告文件，空白检查退出 0。

- [ ] **步骤 3：提交报告**

```powershell
git commit -m "docs: add phase 0 independent review"
$reportCommit = git rev-parse HEAD
$reportCommit
```

禁止 amend、rebase、reset 或重建任何已引用提交。

- [ ] **步骤 4：提交后完整复验**

重新运行：

```powershell
git -c core.quotepath=false diff-tree --no-commit-id --name-only -r $reportCommit
git show --check $reportCommit
python -B -m unittest discover -s '脚本资源/项目校验/tests' -p 'test_*.py' -v
python -B '脚本资源/项目校验/validate_repository.py' .
git -c core.quotepath=false status --short
```

预期：报告提交只含一个规定文件；测试 44/44；校验退出 0；最终工作区无输出。若提交后动态事实因日期跨越发生变化，报告必须按新鲜事实重做，不能继续声称结论有效。

## 14. 14 组自动证据映射

| # | 证据组 | 主要任务 | 通过条件 |
|---:|---|---|---|
| 1 | Git、启动与固定截止 | 任务 1 | main、干净、远程正确、截止为启动 HEAD 祖先 |
| 2 | 截止后允许差异 | 任务 1 | 四项治理路径，缺失/越界 0/0 |
| 3 | P0-01 至 P0-05 任务链存在 | 任务 2 | 显式清单缺失 0 |
| 4 | 提交链、范围与空白 | 任务 2 | 关键对象可达、顺序正确、当前有效对象检查通过 |
| 5 | 根目录、21 阶段与空正文 | 任务 3 | 缺失/越界 0/0，阶段 README 21，非 README 教学文件 0 |
| 6 | 十类模板与当前 blob | 任务 4 | 模板 10、README 1、相对固定对象无漂移 |
| 7 | Python、测试、33 映射与依赖 | 任务 5 | 3.11.15、44/44、33/33、公开函数 7、第三方依赖 0 |
| 8 | 临时编译缓存 | 任务 5 | `py_compile` 退出 0、临时目录已清理 |
| 9 | 仓库校验双运行 | 任务 5 | 两次退出 0、输出完全一致、错误 0 |
| 10 | 版本、来源与 ADR 结构 | 任务 6 | 7/12/1、18 条来源、8/7/6、ADR 5/5 |
| 11 | 许可证、第三方与敏感信息 | 任务 6 | 文件边界一致、上游源码未复制、无真实敏感信息 |
| 12 | Markdown 链接与格式 | 任务 2、9 | 校验器与 Git 空白检查退出 0 |
| 13 | 远程只读连通 | 任务 7 | `git ls-remote --heads origin` 退出 0 |
| 14 | 报告白名单与最终工作区 | 任务 13 | 单报告提交，提交后测试/校验通过，工作区干净 |

## 15. 官方来源清单

动态复核至少使用以下直接入口；如官方重定向，应记录最终页面和原因：

- <https://rockylinux.org/news/rocky-linux-9-8-ga-release>
- <https://wiki.rockylinux.org/rocky/version/>
- <https://kubernetes.io/releases/1.36/>
- <https://kubernetes.io/releases/patch-releases/>
- <https://kubernetes.io/docs/setup/production-environment/container-runtimes/>
- <https://kubernetes.io/docs/setup/production-environment/tools/kubeadm/install-kubeadm/>
- <https://containerd.io/releases/>
- <https://github.com/containerd/containerd/releases>
- <https://docs.tigera.io/calico/latest/getting-started/kubernetes/requirements>
- <https://github.com/projectcalico/calico/releases>
- <https://vitepress.dev/guide/what-is-vitepress>
- <https://vitepress.dev/guide/frontmatter>
- <https://vitepress.dev/reference/default-theme-search>
- <https://gitee.com/y_project/RuoYi-Vue/releases/tag/v3.9.2>
- <https://gitee.com/y_project/RuoYi-Vue.git>
- <https://gitee.com/y_project/RuoYi-Vue/blob/master/LICENSE>
- <https://www.alibabacloud.com/help/en/ecs/user-guide/gpu-accelerated-compute-optimized-and-vgpu-accelerated-instance-families-1>

搜索摘要、博客、论坛、培训资料和 AI 生成文本只能用于发现线索，不能作为动态版本、许可证、兼容性或云产品结论的唯一证据。

## 16. 停止条件

- 当前会话参与过 P0-01 至 P0-05 的交付、返工或主会话验收；
- 分支、远程、工作区、Python、任务状态或固定截止不符；
- 固定截止不是启动 HEAD 祖先；
- 固定截止后的差异超出四项 P0-06 治理文件；
- 需要创建或修改报告之外的仓库文件；
- 设计、计划、任务包、PRD、AI 规则、ADR 或版本基线存在无法解释的规格冲突；
- 固定 blob、测试、行为映射或校验器当前状态发生漂移；
- 动态官方来源与基线冲突且需要修改基线；
- 发现真实敏感信息或无法保证临时数据安全清理；
- 远程或关键官方来源三次合规尝试仍无法取得证据；
- 无法形成单一报告提交或需要改写历史；
- 用户在执行中改变目标，导致审查对象或范围失效。

停止后：仓库内缺陷形成 `recommend-rework` 报告；外部关键条件导致证据不足形成 `blocked` 报告；若报告本身也无法安全创建，则不写入并直接向主会话报告阻塞。

## 17. 完成后的回复

```text
任务：P0-06 Phase 0 独立审查
建议结论：recommend-accept / recommend-rework / blocked
报告：项目管理/验收记录/Phase-0-独立审查报告.md
固定截止：1bf564673e68751e6183ab9c2eb4874fda3fbf6e
启动 HEAD：填写完整哈希
报告提交：填写 docs: add phase 0 independent review 的完整哈希
自动证据：填写通过组数/14
人工检查：填写通过、需说明、不通过数量/24
发现：填写 blocking/must-fix/suggestion/passed 数量
动态版本：填写审查日期与关键结论
远程只读：填写命令、退出码和结果
临时文件：填写清理结果
异常：无或具体异常
风险：无或具体风险
未决问题：无或具体问题
远程写操作：未 push、未创建 PR、未发布、未修改 GitHub 设置
责任声明：未修改任务看板，未宣布任何任务或 Phase 0 accepted
```

## 18. 主会话后续门禁

本计划不授权执行会话完成以下事项：

1. 接受 P0-05、P0-06 或 Phase 0；
2. 创建 Phase 0 最终验收报告；
3. 更新任务看板或项目记忆；
4. 创建 Phase 1 设计、计划或任务包；
5. 生成任何学习正文；
6. 执行任何远程写操作。

只有 P0-06 报告提交后，主会话才能基于独立复验裁决上述后续动作。
