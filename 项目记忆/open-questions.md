# 决策门与已确认决定

## 唯一未决阶段性决策门

### GPU实例、GPU 型号和目标模型匹配

- 最晚决策节点：AI Infra 实验任务包进入 `ready` 前。
- 需要核定：云上 GPU实例族、具体 GPU 型号与显存、目标模型规模、地域库存和成本提示。
- 当前默认：优先阿里云 GPU ECS，按模型显存需求与地域库存核定，不依赖学习者本地 GPU。
- 状态：非阻塞决策门；不阻塞 Phase 1 验收、Phase 2 任务设计或阶段 00–05 基础内容建设，但在 AI Infra 实验任务进入 `ready` 前必须关闭。

## 已确认的实施决定

1. 文档站采用 VitePress，实际依赖版本在网站任务进入 `ready` 前按官方稳定版核定。
2. 原创文档、图表和学习内容采用 CC BY-SA 4.0；原创脚本、配置和代码采用 Apache License 2.0；第三方内容保持原许可证。
3. 传统项目固定使用 RuoYi-Vue v3.9.2（commit `0e2d75c`），其源码保持 MIT License，默认不提交完整上游源码。
4. 阿里云实验预算不阻塞任务，不设置项目级费用上限；实验仍需提供资源释放步骤。
5. GPU 实验使用云上租用资源，默认优先阿里云 GPU ECS，不要求学习者本地拥有 GPU。

不得在本文件或其他项目记忆文件中保存密码、Token、Cookie、私钥、云 AccessKey、个人敏感信息或其他凭据。
